add_namespace = poland_act

country_event = {
    id = poland_act.1
    title = "The Warsaw Autonomy Act"
    desc = poland_act.1.d

    trigger = {
        or = { original_tag = AUS original_tag = GER }
        has_government = monarchism
        controls_state = 10
        has_idea = service_by_requirement
    }
    fire_only_once = yes

    option = {
        name = "They are a worthy ally."

        release_on_controlled = POL
        set_autonomy = { target = POL autonomous_state = autonomy_dominion }

        every_country = {
            limit = { is_ally_with = ROOT }
            every_controlled_state = { limit = { is_core_of = POL } transfer_state_to = POL }
        }

        hidden_effect = { 
            POL = { 
                load_oob = "POL_1912"
                set_popularities = { communism = 10 democratic = 30 monarchism = 60 }
            }
        }
    }

    option = {
        name = "This is not necessary."
    }
}