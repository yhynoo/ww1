# historical
    add_namespace = SPA_canalejas

    # canalejas
        news_event = {
            id = SPA_canalejas.1
            title = "José Canalejas Assassinated in Spain!"
            desc = SPA_canalejas.1.d

            is_triggered_only = yes
            major = yes

            option = {
                name = "Horrible."
                if = { 
                    limit = { tag = SPA } 
                    retire_character = SPA_jose_canalejas   add_popularity = { ideology = democratic popularity = 0.025 }
                }
            }
        }

# tree
    add_namespace = SPA_government_event
    add_namespace = SPA_manpower_event

    # a fading empire
        country_event = {
            id = SPA_government_event.1
            title = "A Fading Empire"
            desc = SPA_government_event.1.d

            is_triggered_only = yes

            option = {
                name = "Our light is dim, yet still alive."
                add_stability = 0.025        set_country_flag = SPA_monarchist

                # ai
                ai_chance = { modifier = { is_going_democrat = yes factor = 0 } }
            }
            option = {
                name = "Time to modernize."
                add_popularity = { ideology = democratic popularity = 0.05 }        set_country_flag = SPA_democratic

                # ai
                ai_chance = { modifier = { is_going_monarchist = yes factor = 0 } }
            }
        }

    # carribean claims
        # manpower act
        country_event = {
            id = SPA_manpower_event.1
            title = "Claims in the Carribean?"
            desc = SPA_manpower_event.1.d

            trigger = { tag = SPA focus_progress = { focus = XXX_men progress > 0.4 } }
            fire_only_once = yes

            option = {
                name = "We should do that."
                every_state = {
                    or = { state = 315 state = 319 state = 348 }
                    add_claim_by = ROOT
                }

                # event
                hidden_effect = { news_event = { id = SPA_manpower_event.2 hours = 6 } }
            }
            option = {
                name = "Better focus on more attainable goals."
            }
        }

        news_event = {
            id = SPA_manpower_event.2
            title = "[SPA.GetName] Announces Claims in the Carribean"
            desc = SPA_manpower_event.2.d

            is_triggered_only = yes

            option = {
                name = "Worrying."
                if = {
                    limit = { or = { original_tag = FRA original_tag = USA } }
                    answer_warning = yes
                }
            }
        }

# decisions
    add_namespace = SPA_anarchists_quashed
    add_namespace = SPA_weyler

    # anarchists
        country_event = {
            id = SPA_anarchists_quashed.1
            title = "Anarchists Quashed in the Countryside"
            desc = SPA_anarchists_quashed.1.d

            is_triggered_only = yes

            option = {
                name = "Great."
                remove_ideas = idea_anarchists_in_the_countryside
            }
        }

    # weyler
        news_event = {
            id = SPA_weyler.1
            title = "Garrisons in the South Assaulted"
            desc = SPA_weyler.1.d

            is_triggered_only = yes
            fire_only_once = yes

            option = {
                name = "Awful."
                add_stability = -0.05

                hidden_effect = { country_event = { id = SPA_weyler.2 days = 7 } }
            }
        }

        country_event = {
            id = SPA_weyler.2
            title = "Weyler: ‘Salvation Junta’"
            desc = SPA_weyler.2.d

            is_triggered_only = yes

            option = {
                name = "Allow him to seize control."
                promote_character = SPA_valeriano_weyler

                # consequence event; full commitment option
                hidden_effect = { news_event = { id = SPA_weyler.3 hours = 6 } }
            }
            option = {
                name = "We will manage without him."
                if = {
                    limit = { not = { has_government = monarchism } }
                    random = {
                        chance = 50
                        start_civil_war = { ideology = monarchism size = 0.5 }
                    }
                }
                else = {
                    add_political_power = 25
                }
            }
        }

        news_event = {
            id = SPA_weyler.3
            title = "Weyler’s Government Established in Spain"
            desc = SPA_weyler.3.d

            is_triggered_only = yes
            major = yes

            option = {
                name = "Alright."
            }
        }

# other
    add_namespace = SPA_atlas

    # intervene in the atlas
        country_event = {
            id = SPA_atlas.1
            title = "Engage in the French Intervention?"
            desc = SPA_atlas.1.d

            trigger = {
                tag = SPA
                FRA = { has_war_with = MOR }
            }
            fire_only_once = yes

            option = {
                name = "We could get something for ourselves."
                declare_war_on = { target = MOR type = annex_everything }

                hidden_effect = { news_event = { id = SPA_atlas.2 hours = 6 } }

                # ai
                ai_chance = { modifier = { is_defensive = yes factor = 0 } }
            }
            option = {
                name = "We are not interested."
                add_stability = 0.025
            }
        }

        news_event = {
            id = SPA_atlas.2
            title = "Spain Engages in Morocco Again"
            desc = SPA_atlas.2.d

            is_triggered_only = yes

            option = {
                name = "Awful."
            }
        }

# elections
    add_namespace = SPA_elections

    country_event = {
        id = SPA_elections.1
        title = "Elections of [GetYear]"
        desc = SPA_elections.1.d

        is_triggered_only = yes
        trigger = { tag = SPA }
        
        option = {
            name = "[ROOT.GetLeader]."
            add_stability = 0.025
        }

        # choices
        option = {
            trigger = { SPA_jose_canalejas = { can_be_country_leader = yes is_country_leader = no } }
            name = "José Canalejas."   promote_character = SPA_jose_canalejas
        }
        option = {
            trigger = { 
                SPA_alvaro_de_figueroa = { can_be_country_leader = yes is_country_leader = no } 
                SPA_jose_canalejas = { can_be_country_leader = no }
            }
            name = "Álvaro de Figueroa."   promote_character = SPA_alvaro_de_figueroa
        }
        option = {
            trigger = { SPA_eduardo_dato = { can_be_country_leader = yes is_country_leader = no } }
            name = "Eduardo Dato."   promote_character = SPA_eduardo_dato
        }
    }