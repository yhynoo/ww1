
    # stalemate
        country_event = {
            id = armistice_later.1
            title = "The Offensive Effort Stalls"
            desc = armistice_later.1.d

            trigger = {
                longest_war_length > 2          # already after three months
                not = {
                    any_enemy_country = {
                        is_war_target = yes
                        surrender_progress > 0.05
                    }
                }
                not = { has_idea = idea_armistice_later_timeout }
            }
            immediate = { 
                hidden_effect = { add_timed_idea = { idea = idea_armistice_later_timeout days = 180 } } 
            }

            option = {
                name = "We shall ask for an armistice_later."
                every_enemy_country = {
                    limit = { is_war_target = yes }
                    
                    custom_effect_tooltip = tt_ask_for_armistice_later
                    # event
                }
            }
            option = {
                name = "We must fight until we achieve our goals."
                add_political_power = -50
            }
        }

    # opposition
        country_event = {
            id = armistice_later.2
            title = "Opposition Demands an Armistice_later"
            desc = armistice_later.2.d

            trigger = {
                or = {
                    and = { has_war = yes has_war_support < 0.4 longest_war_length > 11 }
                    surrender_progress > 0.15
                }

                check_variable = { highest_party_popularity@exclude_ruling_party > 0.25 }
                not = { has_idea = idea_armistice_later_timeout }
            }
            immediate = { 
                hidden_effect = { add_timed_idea = { idea = idea_armistice_later_timeout days = 180 } } 
            }

            option = {
                name = "They are right."
                every_enemy_country = {
                    limit = { is_diplomatically_able = yes }
                    
                    custom_effect_tooltip = tt_ask_for_armistice_later
                    # event
                }
            }
            option = {
                name = "The war is not over yet."
                add_popularity = { ideology = ROOT popularity = -0.05 }
            }
        }