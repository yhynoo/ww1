add_namespace = x_war_weariness

news_event = {
    id = x_war_weariness.1
    title = "Where Have All the Young Men Gone?"
    desc = x_war_weariness.1.d

    trigger = {
        not = { original_tag = SOV }
        not = { has_country_flag = war_weariness_done }

        has_war = yes
        has_stability < 0.3
        or = { 
            longest_war_length > 18
            has_idea = service_by_requirement has_idea = all_adults_serve has_idea = scraping_the_barrel
        }
    }

    mean_time_to_happen = { days = 28 }

    option = {
        name = "Disturbing."
        add_timed_idea = { idea = idea_war_weariness days = 180 }

        hidden_effect = {
            set_country_flag = war_weariness_done
        }
    }
}

news_event = {
    id = x_war_weariness.2
    title = "Quiet Streets, Empty Towns"
    desc = x_war_weariness.2.d

    is_triggered_only = yes

    option = {
        name = "The tragedy won't end."

        add_war_support = -0.1
        add_ideas = idea_quiet_streets
    }
}