add_namespace = turkey_ararat

country_event = {
    id = turkey_ararat.1
    title = "Tragedy Under Ararat"      # entire companies froze to death due to mismanagement and lack of winter equipment
    desc = turkey_ararat.1.d

    trigger = {
        tag = TUR
        not = { has_country_flag = exhaustion_fired }
        and = { longest_war_length > 18 has_war_support < 0.35 }
    }

    immediate = { 
        hidden_effect = { 
            set_country_flag = exhaustion_fired 
            news_event = { id = turkey_ararat.2 days = 60 random_days = 30 } 
        } 
    }

    option = {
        name = "Silence the matter."
        add_political_power = -50

        hidden_effect = { 
            news_event = { id = turkey_ararat.3 days = 180 } 
        }

        ai_chance = {
            base = 1
            modifier = { has_country_flag = chose_alternative factor = 0 }
        }
    }
    option = {
        name = "Admit the tragedy and improve supplies."
        add_timed_idea = { idea = idea_winter_tragedies years = 1 }
    }
}

news_event = {
    id = turkey_ararat.2
    title = "Mothers' Appeal"
    desc = turkey_ararat.2.d

    is_triggered_only = yes
    trigger = { has_war_support < 0.5 }

    option = {
        name = "A truly haunting memory."
        add_stability = -0.05
    }
}

news_event = {
    id = turkey_ararat.3
    title = "Eight Officers Shot in Undisclosed Garrison"
    desc = turkey_ararat.3.d

    is_triggered_only = yes
    trigger = { has_war_support < 0.5 }

    option = {
        name = "Very disturbing."
        add_ideas = idea_outrage_against_army_staff
    }
}