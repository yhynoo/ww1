add_namespace = france_agadir

country_event = {
    id = france_agadir.1
    title = "The Question of Moroccan Independence"
    desc = france_agadir.1.d

    trigger = {
        tag = FRA
        date > 1912.4.1
        has_idea = idea_unrest_in_morocco
    }
    fire_only_once = yes

    mean_time_to_happen = { 
        months = 6
        modifier = { has_stability < 0.5 factor = 0.5 }
    }

    immediate = { hidden_effect = { remove_ideas = idea_unrest_in_morocco } }

    option = {
        name = "This is a headache we don't need."
        release = MOR
    }
    option = {
        name = "No!"

        hidden_effect = { moroccan_independence = yes }
        declare_war_on = { target = MOR type = annex_everything }
    }
}