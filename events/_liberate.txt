add_namespace = _liberate

country_event = {
    id = _liberate.1
    title = "Our Troops March in Warsaw"
    desc = _liberate.1.d

    trigger = { POL = { exists = no } }
    is_triggered_only = yes
    fire_only_once = yes

    option = {
        name = "8Continue as [POL.GetNameDef]"
        hidden_effect = { 
            release_poland = yes
            set_politics = { ruling_party = ROOT }
        }

        POL = { change_tag_from = ROOT }
        ai_chance = { base = 0 }
    }
    option = {
        name = "Poland shall be a worthy ally."
        hidden_effect = { 
            release_poland = yes
            set_politics = { ruling_party = ROOT }
        }

        ai_chance = { base = 2 }
    }
    option = {
        name = "Nonsense."
        every_controlled_state = { limit = { is_core_of = POL } add_resistance = 10 }

        ai_chance = { base = 1 }
    }
}

country_event = {
    id = _liberate.2
    title = "Our Troops March in Riga"
    desc = _liberate.2.d

    trigger = { UBD = { exists = no } }
    is_triggered_only = yes
    fire_only_once = yes

    option = {
        name = "8Continue as [UBD.GetNameDef]"
        hidden_effect = { 
            release_baltic = yes
            set_politics = { ruling_party = ROOT }
        }

        UBD = { change_tag_from = ROOT }
        ai_chance = { base = 0 }
    }
    option = {
        name = "The Baltics shall be worthy allies."
        hidden_effect = { 
            release_baltic = yes
            set_politics = { ruling_party = ROOT }
        }

        ai_chance = { base = 2 }
    }
    option = {
        name = "Nonsense."
        every_controlled_state = { limit = { is_core_of = UBD } add_resistance = 10 }

        ai_chance = { base = 1 }
    }
}