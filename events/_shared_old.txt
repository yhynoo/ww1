add_namespace = shared

# state
    news_event = {
        id = shared.1
        title = "Army Marches in the Capital"
        desc = shared.1.d

        is_triggered_only = yes

        option = {
            name = "What a sight."
            add_war_support = 0.025
        }
    }

    country_event = {
        id = shared.2
        title = "[FROM.GetName] Demands [demand_target.GetName]"
        desc = shared.2.d

        is_triggered_only = yes

        option = {
            name = "Let them have it."
            FROM = { transfer_state = event_target:demand_target }

            # effect news
            news_event = { id = shared.3 hours = 6 }
        }
        option = {
            name = "Outrageous!"
            FROM = { create_wargoal = { target = ROOT type = take_claimed_state } }
            add_war_support = 0.05
        }
    }

    news_event = {
        id = shared.3
        title = "[FROM.GetName] Agrees!"
        desc = shared.3.d

        is_triggered_only = yes

        option = {
            name = "Wonderful."
            add_political_power = 50
        }
    }

    country_event = {
        id = shared.4
        title = "Agrarians Doubt Government Direction"
        desc = shared.4.d

        is_triggered_only = yes

        option = {
            name = "This is a useful suggestion."
            add_popularity = { ideology = monarchism popularity = 0.025 }
        }
        option = {
            name = "What do they know?"
            add_stability = -0.02
        }
    }

    news_event = {
        id = shared.5
        title = "Workers Demand Change"
        desc = shared.5.d

        is_triggered_only = yes

        option = {
            name = "Worrying."
            add_stability = -0.025
            hidden_effect = { country_event = { id = shared.6 days = 7 } }
        }
    }

    country_event = {
        id = shared.6
        title = "Delegation Arrives at Government Offices"
        desc = shared.6.d

        is_triggered_only = yes

        option = {
            name = "Agree to their demands."
            add_popularity = { ideology = communism popularity = 0.05 }
        }
        option = {
            name = "Send them away."
            add_political_power = -25

            # effect
            hidden_effect = { random = { chance = 50 country_event = { id = shared.7 days = 14 } } }
        }
    }

    country_event = {
        id = shared.7
        title = "Strikes in the Capital!"
        desc = shared.7.d

        is_triggered_only = yes

        option = {
            name = "Send the troops."
            add_popularity = { ideology = ROOT popularity = -0.025 }

            # effect news
            random = { chance = 50 news_event = { id = shared.8 hours = 12 } }
        }
        option = {
            name = "Wait it out."
            add_stability = -0.05
        }
    }

    news_event = {
        id = shared.8
        title = "Socialist Activists Dead in Clashes"
        desc = shared.8.d

        is_triggered_only = yes

        option = {
            name = "Awful."
            add_popularity = { ideology = ROOT popularity = -0.05 }
        }
    }

country_event = {
        id = shared.9
        title = "[FROM.GetName] Asks for Aid"
        desc = shared.9.d

        is_triggered_only = yes

        option = {
            name = "Sounds good."
            add_political_power = -25

            # effect news
            FROM = { news_event = { id = shared.12 hours = 6 } }
        }
        option = {
            name = "We shall not get involved."
            add_war_support = -0.025
        }
    }

    news_event = {
        id = shared.10
        title = "[FROM.GetName] Agrees!"
        desc = shared.10.d

        is_triggered_only = yes

        option = {
            name = "Great."
            add_equipment_to_stockpile = { type = infantry_equipment    amount = 600    producer = ROOT }
        }
    }

    country_event = {
        id = shared.11
        title = "[FROM.GetName] Demands Independence!"
        desc = shared.11.d

        is_triggered_only = yes

        option = {
            name = "We must agree."
            set_autonomy = { target = FROM  autonomous_state = autonomy_free }
            add_war_support = -0.05

            # effect news
            FROM = { news_event = { id = shared.10 hours = 6 } }
        }
        option = {
            name = "This means war!"
            hidden_effect = { set_autonomy = { target = FROM  autonomous_state = autonomy_free } }
            declare_war_on = { target = FROM type = puppet_wargoal_focus }
        }
    }

    news_event = {
        id = shared.12
        title = "[FROM.GetName] Agrees!"
        desc = shared.12.d

        is_triggered_only = yes

        option = {
            name = "Great."
            add_political_power = 100
        }
    }

# army
    news_event = {
        id = shared.13
        title = "Troops Organized into a Modern Army"
        desc = shared.13.d

        is_triggered_only = yes

        option = {
            name = "Wonderful."
            add_political_power = 25

            clr_country_flag = early_military
            mark_focus_tree_layout_dirty = yes
        }
    }

# infrastructure
    news_event = {
        id = shared.14
        title = "Industrialization in [ROOT.GetNameDef]"
        desc = shared.14.d

        is_triggered_only = yes

        option = {
            name = "Amazing."
            add_political_power = 50

            clr_country_flag = pre_industrial_state
            mark_focus_tree_layout_dirty = yes
        }
    }

# uprising
    news_event = {
        id = shared.15      # delayer event.
        hidden = yes

        trigger = {
            or = {
                has_stability < 0.2
                has_civil_war = yes
                and = { has_war = yes surrender_progress > 0.25 stability < 0.4 }
            }
        }

        immediate = { organize_uprising = yes }
    }

    country_event = {
        id = shared.16
        title = "Uprising in [uprising_country.GetNameDef]!"
        desc = shared.16.d

        is_triggered_only = yes
        trigger = { is_ai = no }

        option = {
            name = "To arms!"
            add_war_support = 0.05
        }
        option = {
            name = "Freedom to [uprising_country.GetNameDef]!"
            event_target:uprising_country = { change_tag_from = ROOT }
        }
    }

    news_event = {
        id = shared.17
        title = "[ROOT.GetAdjective] Interim Government Established"
        desc = shared.17.d

        is_triggered_only = yes

        option = {
            name = "Great."
            add_political_power = 100

            remove_ideas = idea_uprising
            remove_ideas = idea_uprising_administration

            clr_country_flag = during_uprising
            mark_focus_tree_layout_dirty = yes
        }
    }

# civil war
    news_event = {
        id = shared.18
        title = "Collapse of the Administration"
        desc = shared.18.d

        is_triggered_only = yes

        option = {
            name = "Awful."
            add_ideas = idea_collapsed_administration       add_ideas = idea_dysfunctional_supply
        }
    }

    news_event = {
        id = shared.19
        title = "Total Erosion of the [ROOT.GetAdjective] State"
        desc = shared.19.d

        is_triggered_only = yes

        option = {
            name = "It's over."
            add_ideas = idea_eroded_state       add_ideas = idea_destroyed_industry
        }
    }

    news_event = {
        id = shared.20
        title = "[ROOT.GetName] United Again?"
        desc = shared.20.d

        is_triggered_only = yes

        option = {
            name = "Let us hope so."
            add_political_power = 50
            clr_country_flag = during_civil_war
        }
    }

    country_event = {
        id = shared.21
        title = "[FROM.GetName] Offers Territorial Adjustments"
        desc = shared.21.d

        is_triggered_only = yes

        option = {
            name = "This is a welcome initiative."

            every_state = {
                limit = { 
                    and = { is_controlled_by = FROM not = { is_claimed_by = FROM } }
                    is_core_of = ROOT
                }
                transfer_state_to = ROOT
            }
            every_state = {
                limit = {
                    and = { is_controlled_by = ROOT not = { is_claimed_by = ROOT } }
                    is_core_of = FROM
                }
                transfer_state_to = FROM
            }

            # effect news
            FROM = { news_event = { id = shared.22 hours = 6 } }
        }
        option = {
            name = "We can’t hold these areas now."
        }
    }

    news_event = {
        id = shared.22
        title = "[FROM.GetName] Accepts Offered Territories"
        desc = shared.22.d

        is_triggered_only = yes

        option = {
            name = "Alright."
            add_stability = 0.025
        }
    }

# opposition - communists
    # struggle
    news_event = {
        id = shared.23
        title = "Unrest Among Workers"
        desc = shared.23.d

        is_triggered_only = yes
        trigger = { 
            and = { not = { has_government = communism } has_stability < 0.6 }
            not = { has_idea = idea_unrest_timeout }
        }

        option = {
            name = "Always more demands."
            add_popularity = { ideology = communism     popularity = 0.025 }

            hidden_effect = {
                add_timed_idea = { idea = idea_unrest_timeout days = 180 }
            }
        }
    }

    # chain
    news_event = {
        id = shared.24
        title = "[ROOT.GetCommunistParty] Gather in the Capital"
        desc = shared.24.d

        trigger = { 
            or = { 
                any_character = { has_trait = communist_agitator is_hired_as_advisor = yes }
                and = { communism > 0.15 has_stability < 0.25 }
            }
            not = { has_government = communism }
            not = { has_country_flag = addressed_masses } 
        }
        immediate = { hidden_effect = { set_country_flag = addressed_masses } }
        mean_time_to_happen = { days = 60 modifier = { has_stability < 0.4 factor = 0.5 } }

        option = {
            name = "Unsettling."
            add_stability = -0.015

            hidden_effect = { country_event = { id = shared.25 days = 7 } }
        }
    }

    country_event = {
        id = shared.25
        title = "[ROOT.GetCommunistLeader] Addresses the Masses"
        desc = shared.25.d

        is_triggered_only = yes

        option = {
            name = "This will dissipate eventually."
            add_popularity = { ideology = communism popularity = 0.05 }
            add_stability = -0.025
        }
        option = {
            name = "Arrest him."
            add_political_power = -50

            random = { chance = 50 news_event = { id = shared.26 days = 2 random_days = 14 } }
        }
    }

    news_event = {
        id = shared.26
        title = "[ROOT.GetCommunistLeader] Dead!"
        desc = shared.26.d

        is_triggered_only = yes

        option = {
            name = "Awful."
            party_leader = { limit = { has_ideology_group = communism } retire = yes }
            add_stability = -0.05
        }
    }

    # anti-war
    news_event = {
        id = shared.27
        title = "Socialist Agitators Against War"
        desc = shared.27.d

        trigger = {
            has_civil_war = no 
            not = { or = { has_government = communism has_country_flag = had_agitators } }
            and = { has_war = yes has_war_support < 0.6 communism > 0.1 }
        }
        immediate = { hidden_effect = { set_country_flag = had_agitators } }

        option = {
            name = "This is treason!"
            add_war_support = -0.025
        }
    }

    # civil war
    country_event = {
        id = shared.28
        title = "Civil War Approaching?"
        desc = shared.28.d

        trigger = {
            not = { has_government = communism }
            not = { has_idea = idea_coup_timeout }      # we can't just spam this.
            has_civil_war = no
            or = {
                and = { has_stability < 0.2 communism > 0.4 }
                communism > 0.5
            }
        }

        option = {
            name = "We must agree."
            set_politics = { ruling_party = communism elections_allowed = no }
        }
        option = {
            name = "Let them come."
            add_political_power = -100

            random = { 
                chance = 50 
                start_civil_war = { ideology = communism size = 0.5 }
                hidden_effect = { add_popularity = { ideology = communism popularity = -0.4 } }
            }

            hidden_effect = {
                add_timed_idea = { idea = idea_coup_timeout days = 60 }
            }
        }
    }

# opposition - democrats
    # struggle
    news_event = {
        id = shared.29
        title = "Liberal Author Asks Questions"
        desc = shared.29.d

        is_triggered_only = yes
        trigger = { 
            and = { not = { has_government = democratic } has_stability < 0.6 }
            not = { has_idea = idea_unrest_timeout }
        }

        option = {
            name = "What is he hinting at?"
            add_popularity = { ideology = democratic     popularity = 0.025 }

            hidden_effect = {
                add_timed_idea = { idea = idea_unrest_timeout days = 180 }
            }
        }
    }

    # chain
    news_event = {
        id = shared.30
        title = "[ROOT.GetDemocraticParty] Calls for Demonstrations"
        desc = shared.30.d

        trigger = { 
            or = { 
                any_character = { has_trait = democratic_advocate is_hired_as_advisor = yes }
                and = { democratic > 0.15 has_stability < 0.25 }
            }
            not = { has_government = democratic }
            not = { has_country_flag = call_for_demos } 
        }
        immediate = { hidden_effect = { set_country_flag = call_for_demos } }
        mean_time_to_happen = { days = 60 modifier = { has_stability < 0.4 factor = 0.5 } }

        option = {
            name = "Worrying."
            add_stability = -0.015

            hidden_effect = { country_event = { id = shared.31 days = 7 } }
        }
    }

    country_event = {
        id = shared.31
        title = "[ROOT.GetDemocraticLeader]'s Appeal to the Citizens"
        desc = shared.31.d

        is_triggered_only = yes

        option = {
            name = "It's all just words."
            add_popularity = { ideology = democratic popularity = 0.05 }
            add_stability = -0.025
        }
        option = {
            name = "Silence him."
            add_political_power = -50

            random = { chance = 50 news_event = { id = shared.32 days = 2 random_days = 14 } }
        }
    }

    news_event = {
        id = shared.32
        title = "Man of His Word"
        desc = shared.32.d

        is_triggered_only = yes

        option = {
            name = "What would he need this for?"
            add_popularity = { ideology = democratic popularity = 0.025 }
            add_political_power = -75
        }
    }

    # anti-war
    news_event = {
        id = shared.33
        title = "End the Slaughter!"
        desc = shared.33.d

        trigger = { 
            has_civil_war = no 
            not = { or = { has_government = democratic has_country_flag = end_slaughter } }
            and = { has_war = yes has_war_support < 0.6 democratic > 0.1 }
        }
        immediate = { hidden_effect = { set_country_flag = end_slaughter } }

        option = {
            name = "Childish idealism."
            add_popularity = { ideology = democratic popularity = 0.025 }
            add_war_support = -0.02
        }
    }

    # civil war
    country_event = {
        id = shared.34
        title = "[ROOT.GetDemocraticLeader] Demands Elections!"
        desc = shared.34.d

        trigger = {
            not = { has_government = democratic }
            not = { has_idea = idea_coup_timeout }      # we can't just spam this.
            has_civil_war = no
            or = {
                and = { has_stability < 0.2 democratic > 0.4 }
                democratic > 0.5
            }
        }

        option = {
            name = "We must step down."
            set_politics = { ruling_party = democratic elections_allowed = yes }
        }
        option = {
            name = "Or what?"
            add_political_power = -100

            random = { 
                chance = 50 
                start_civil_war = { ideology = democratic size = 0.5 }
                hidden_effect = { add_popularity = { ideology = democratic popularity = -0.4 } }
            }

            hidden_effect = {
                add_timed_idea = { idea = idea_coup_timeout days = 60 }
            }
        }
    }

# opposition - monarchists
    # struggle
    news_event = {
        id = shared.35
        title = "Writer Denounces Moral Collapse"
        desc = shared.35.d

        is_triggered_only = yes
        trigger = { 
            and = { not = { has_government = monarchism } has_stability < 0.6 }
            not = { has_idea = idea_unrest_timeout }
        }

        option = {
            name = "It's always the same complaints."
            add_popularity = { ideology = monarchism     popularity = 0.025 }

            hidden_effect = {
                add_timed_idea = { idea = idea_unrest_timeout days = 180 }
            }
        }
    }

    # chain
    news_event = {
        id = shared.36
        title = "Academics Discuss 'Lack of Order'"
        desc = shared.36.d

        trigger = { 
            or = { 
                any_character = { has_trait = conservative_thinker is_hired_as_advisor = yes }
                and = { monarchism > 0.15 has_stability < 0.25 }
            }
            not = { has_government = monarchism }
            not = { has_country_flag = lack_of_order } 
        }
        immediate = { hidden_effect = { set_country_flag = lack_of_order } }
        mean_time_to_happen = { days = 60 modifier = { has_stability < 0.4 factor = 0.5 } }

        option = {
            name = "Annoying."
            add_stability = -0.015
            hidden_effect = { country_event = { id = shared.37 days = 7 } }
        }
    }

    country_event = {
        id = shared.37
        title = "An Open Lecture in the Capital"
        desc = shared.37.d

        is_triggered_only = yes

        option = {
            name = "They are free to say what they want."
            add_popularity = { ideology = monarchism popularity = 0.05 }
            add_stability = -0.025
        }
        option = {
            name = "We must not allow it."
            add_political_power = -50

            random = { chance = 50 news_event = { id = shared.38 days = 2 random_days = 14 } }
        }
    }

    news_event = {
        id = shared.38
        title = "Arrests in the State University"
        desc = shared.38.d

        is_triggered_only = yes

        option = {
            name = "Good."
            add_stability = 0.025
            add_popularity = { ideology = monarchism popularity = -0.025 }
        }
    }

    # anti-war
    news_event = {
        id = shared.39
        title = "War Effort Mismanaged?"
        desc = shared.39.d

        trigger = { 
            has_civil_war = no 
            not = { or = { has_government = monarchism has_country_flag = war_effort_mismanaged } }
            and = { has_war = yes has_war_support < 0.6 monarchism > 0.1 }
        }
        immediate = { hidden_effect = { set_country_flag = war_effort_mismanaged } }

        option = {
            name = "Would they do better?"
            add_popularity = { ideology = monarchism popularity = 0.05 }
            add_stability = -0.025
        }
    }

    # civil war
    country_event = {
        id = shared.40
        title = "Army to Stage a Coup?"
        desc = shared.40.d

        trigger = {
            not = { has_government = monarchism }
            not = { has_idea = idea_coup_timeout }      # we can't just spam this.
            has_civil_war = no
            or = {
                and = { has_stability < 0.2 monarchism > 0.4 }
                monarchism > 0.5
            }
        }

        option = {
            name = "They will win anyway."
            set_politics = { ruling_party = monarchism elections_allowed = no }
        }
        option = {
            name = "The government will stand!"
            add_political_power = -100

            random = { 
                chance = 50 
                start_civil_war = { ideology = monarchism size = 0.4 army_ratio = 0.65 }
                hidden_effect = { add_popularity = { ideology = monarchism popularity = -0.4 } }
            }

            hidden_effect = {
                add_timed_idea = { idea = idea_coup_timeout days = 60 }
            }
        }
    }

# social unrest
    news_event = {
        id = shared.41
        hidden = yes

        trigger = {
            stability < 0.4
            not = { has_idea = idea_unrest_timeout }
        }
        immediate = {
            add_timed_idea = { idea = idea_unrest_timeout days = 60 }
            random_list = {
                1 = {
                    news_event = { id = shared.42 random_days = 14 }
                }
                1 = {
                    random_owned_controlled_state = {
                        limit = { infrastructure > 2 }  save_event_target_as = target_state
                        random_list = {
                            1 = { news_event = { id = shared.43 random_days = 14 } }
                            1 = { news_event = { id = shared.44 random_days = 14 } }
                        }
                    }
                }
                4 = { }
            }
        }
    }

    news_event = {
        id = shared.42
        title = "Anti-Government Slogans in the Capital"
        desc = shared.42.d

        is_triggered_only = yes

        option = {
            name = "Annoying vandalism."
            add_popularity = { ideology = ROOT popularity = -0.02 }
        }
    }
    news_event = {
        id = shared.43
        title = "Arsons in the [target_state.GetName] Countryside"
        desc = shared.43.d

        is_triggered_only = yes

        option = {
            name = "Annoying vandalism."
            add_popularity = { ideology = ROOT popularity = -0.015 }

            event_target:target_state = {
                damage_building = { type = infrastructure damage = 1 }
            }
        }
    }
    news_event = {
        id = shared.44
        title = "Strikes in [target_state.GetCapitalVictoryPointName]"
        desc = shared.44.d

        is_triggered_only = yes

        option = {
            name = "Ok."
            event_target:target_state = {
                add_dynamic_modifier = { modifier = state_strikes days = 60 }
            }
        }
    }

# army
    country_event = {
        id = shared.45
        title = "Tradition or Modernity?"
        desc = shared.45.d

        is_triggered_only = yes

        option = {
            name = "We must maintain our traditions."
            add_tech_bonus = { bonus = 0.5  uses = 1   category = artillery_tech }
        }
        option = {
            name = "The war changes all the time."
            add_tech_bonus = { bonus = 0.5  uses = 1   category = mechanized_tech }
        }
    }

    news_event = {
        id = shared.46
        title = "Naval Album Already Sold Out?"
        desc = shared.46.d

        trigger = {
            has_war = yes
            and = { has_navy_size = { size > 6 } has_war_support > 0.6 }
            not = { has_country_flag = album_sold_out }
        }
        immediate = { hidden_effect = { set_country_flag = album_sold_out } }
        mean_time_to_happen = { days = 60  }

        option = {
            name = "Wonderful."
            add_war_support = 0.015
        }
    }

    news_event = {
        id = shared.47
        hidden = yes

        immediate = {
            if = {
                limit = { has_deployed_air_force_size = { size > 6 } }
                random = { chance = 5 news_event = shared.48 }
            }
            if = {
                limit = { has_war = yes has_war_support < 0.4 }
                random = { chance = 15 news_event = shared.49 }
            }
        }
    }

    news_event = {
        id = shared.48
        title = "Army Airplanes Over the Capital!"
        desc = shared.48.d

        is_triggered_only = yes

        option = {
            name = "Wonderful."
            add_war_support = 0.015
        }
    }

    news_event = {
        id = shared.49
        title = "Desertions in the Army"
        desc = shared.49.d

        is_triggered_only = yes
        trigger = { not = { has_idea = idea_desertion_timeout } }

        option = {
            name = "Worrying."
            add_war_support = -0.01

            hidden_effect = {
                add_timed_idea = { idea = idea_desertion_timeout days = 180 }
            }
        }
    }

# war enthusiasm and exhaustion
    news_event = {
        id = shared.50
        title = "War Enthusiasm"
        desc = shared.50.d

        trigger = {
            has_war = yes
            longest_war_length < 2

            has_civil_war = no
            has_war_support > 0.65
            and = { not = { has_idea = idea_enthusiasm_timeout } not = { has_idea = idea_war_exhaustion } }
        }

        option = {
            name = "Great."
            add_timed_idea = { idea = idea_war_enthusiasm months = 6 }

            hidden_effect = {
                add_timed_idea = { idea = idea_enthusiasm_timeout years = 5 }
            }
        }
    }
    news_event = {
        id = shared.51
        title = "War Exhaustion"
        desc = shared.51.d

        trigger = {
            has_war = yes
            longest_war_length > 24

            has_war_support < 0.6
            or = { has_idea = all_adults_serve has_idea = scraping_the_barrel has_idea = war_economy }
            not = { has_idea = idea_war_exhaustion }
        }

        option = {
            name = "We must keep going."
            add_ideas = idea_war_exhaustion
        }
    }

    country_event = {
        id = shared.52
        title = "Spectre of Shortages"
        desc = shared.52.d

        trigger = {
            has_war = yes
            longest_war_length > 18

            or = { has_idea = partial_economic_mobilisation has_idea = war_economy }
            not = { has_idea = hunger_timeout }
        }
        immediate = { hidden_effect = { add_timed_idea = { idea = hunger_timeout days = 240 } } }

        option = {
            name = "Demobilize the economy."
            add_ideas = civilian_economy

            custom_effect_trigger = tt_demobilized_economy
            hidden_effect = { add_timed_idea = idea_demobilized_economy }
        }
        option = {
            name = "We must endure."
            random = { chance = 50 news_event = { id = shared.53 days = 28 } }
        }
    }
    news_event = {
        id = shared.53
        title = "Widespread Hunger"
        desc = shared.53.d

        is_triggered_only = yes

        option = {
            name = "Horrible."
            add_timed_idea = { idea = idea_widespread_hunger days = 120 }
            hidden_effect = { chance = 50 news_event = { id = shared.54 days = 14 } }
        }
    }
    news_event = {
        id = shared.54
        title = "Army Trains Looted"
        desc = shared.54.d

        is_triggered_only = yes

        option = {
            name = "These are difficult times."
            add_stability = -0.025
        }
    }

#