add_namespace = holland_withdraw

country_event = {
    id = holland_withdraw.1
    title = "[HOL.GetName] Asks Us to Take Over"
    desc = holland_withdraw.1.d

    is_triggered_only = yes

    option = {
        name = "This is a good deal."
        FROM = { 
            news_event = { id = holland_withdraw.2 hours = 6 random_hours = 6 }
            remove_ideas = idea_after_the_intervention
        }

        6 = { transfer_state_to = FRA }

        ai_chance = { base = 2 }
    }
    option = {
        name = "We are not interested."
        ai_chance = { base = 1 }
    }
}

news_event = {
    id = holland_withdraw.2
    title = "[HOL.GetName] Withdraws"
    desc = holland_withdraw.2.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "Interesting."
    }
}

news_event = {
    id = holland_withdraw.3
    title = "[HOL.GetName] Annexes Flanders"
    desc = holland_withdraw.3.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "A dramatic move."

        if = {
            limit = { or = { tag = ENG tag = FRA } }
            add_opinion_modifier = { target = HOL modifier = opinion_annexed_flanders }
        }

        if = {
            limit = { tag = HOL }
            6 = { add_core_of = HOL }
        }
    }
}