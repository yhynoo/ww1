add_namespace = _align

country_event = {
    id = _align.1
    title = "[FROM.GetName] Demands Alignment"
    desc = _align.1.d

    is_triggered_only = yes

    option = {
        name = "We must agree to this."

        FROM = {
            news_event = { id = _align.2 hours = 6 random_hours = 6 }
            puppet = ROOT
        }

        ai_chance = { 
            base = 1
            modifier = { strength_ratio = { tag = FROM ratio > 2 } factor = 2 }
        }
    }

    option = {
        name = "Outrageous!"
        add_war_support = 0.1

        FROM = { create_wargoal = { target = ROOT type = puppet_wargoal_focus } }

        ai_chance = {
            base = 2
            modifier = { strength_ratio = { tag = FROM ratio < 0.5 } factor = 0.5 }
        }
    }
}

news_event = {
    id = _align.2
    title = "[FROM.GetName] Aligns with [ROOT.GetNameDef]"
    desc = _align.2.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "A diplomatic triumph of [ROOT.GetNameDef]."
    }
}