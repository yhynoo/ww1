add_namespace = thr_autonomy
add_namespace = thr_guarantees

# autonomy
    country_event = {
        id = thr_autonomy.1
        title = "[THR.GetName] Asserts Autonomy"

        is_triggered_only = yes
        immediate = {
            set_autonomy = { target = THR autonomous_state = autonomy_free }
        }

        option = {
            name = "Whatever."
            
            # event
            news_event = { id = thr_autonomy.2 hours = 6 }
        }
        option = {
            name = "No."
            declare_war_on = { target = THR type = annex_everything }

            # ai
            ai_chance = { modifier = { is_historical_focus = yes } factor = 0 }
        }
    }

    news_event = {
        id = thr_autonomy.2
        title = "Thrace Slips Away from the Turks"

        is_triggered_only = yes
        major = yes

        option = {
            name = "Ok."
        }
    }

# guarantees
    country_event = {
        id = thr_guarantees.1
        title = "Thrace Asks for Guarantees"

        is_triggered_only = yes
        fire_only_once = yes

        option = {
            name = "Yes."
            give_guarantee = THR
        }
        option = {
            name = "No."

            # ai
            ai_chance = { modifier = { is_historical_focus = yes } factor = 0 }
        }
    }