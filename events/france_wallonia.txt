add_namespace = france_wallonia

country_event = {
    id = france_wallonia.1
    title = "An Opportunity to Attack?"     # France hasn't followed up on their ultimatum: they must be weak, so why don't we strike first?
    desc = france_wallonia.1.d

    is_triggered_only = yes

    option = {
        name = "Let us get them now!"
        declare_war_on = { target = FRA type = puppet_wargoal_focus }

        ai_chance = { 
            base = 4 
            modifier = { has_government = democratic factor = 0 }
        }
    }

    option = {
        name = "Why would we start a war now?"
        add_war_support = -0.2

        ai_chance = { base = 1 }
    }
}

news_event = {
    id = france_wallonia.2
    hidden = yes

    trigger = {
        tag = FRA
        FRA = { controls_state = 6 controls_state = 34 }
    }
    fire_only_once = yes

    immediate = { remove_ideas = idea_echoes_of_agadir }
}