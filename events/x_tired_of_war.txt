add_namespace = x_tired_of_war

country_event = {
    id = x_tired_of_war.1
    title = "War Exhaustion"
    desc = x_tired_of_war.1.d

    trigger = {
        not = { 
            tag = ENG
            tag = GER
            tag = SOV
            tag = USA
        }

        not = { has_country_flag = exhaustion_fired }
        and = { longest_war_length > 18 has_war_support < 0.35 }
    }

    immediate = { hidden_effect = { set_country_flag = exhaustion_fired } }

    option = {
        name = "Appeal to carry on — and end the war soon."
        add_political_power = -150

        hidden_effect = { news_event = { id = x_tired_of_war.2 days = 180 } }
    }
    option = {
        name = "Adopt a defensive approach."
        add_ideas = idea_defensive_approach
    }
}

news_event = {
    id = x_tired_of_war.2
    title = "Army Unwilling to Fight Anymore?"
    desc = x_tired_of_war.2.d

    is_triggered_only = yes
    trigger = { has_war_support < 0.5 }

    option = {
        name = "Very disturbing."
        add_ideas = idea_army_is_done
    }
}