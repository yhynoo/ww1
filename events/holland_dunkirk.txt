add_namespace = holland_dunkirk

news_event = {
    id = holland_dunkirk.1
    title = "Explosion in Dunkirk"
    desc = holland_dunkirk.1.d

    is_triggered_only = yes
    major = yes
    fire_only_once = yes

    option = {
        name = "Horrible."

        if = {
            limit = { tag = FRA }

            add_stability = -0.025
            damage_building = { type = infrastructure state = 29 damage = 1 }

            hidden_effect = {
                HOL = { add_named_threat = { name = "Explosion in Dunkirk" threat = 2 } }
            }
        }
    }
}