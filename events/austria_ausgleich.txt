add_namespace = austria_ausgleich

news_event = {
    id = austria_ausgleich.1
    title = "The ‘Universal Ausgleich’"      # unifying the houses of parliaments, ending any special rules for hungary   
    desc = austria_ausgleich.1.d

    is_triggered_only = yes

    option = {
        name = "Let us hope for the best."
    }
}

country_event = {
    id = austria_ausgleich.2
    title = "The Hungarian Ultimatum"      # unless a compromise is made, hungarian aristocracy starts a civil war.   
    desc = austria_ausgleich.2.d

    is_triggered_only = yes
    trigger = {
        any_owned_controlled_state = { is_core_of = HUN }
        has_stability < 0.6
    }

    option = {
        name = "We must withdraw."
        add_political_power = -100
        
        add_ideas = idea_ausgleich
        hidden_effect = { drop_cosmetic_tag = yes }

        if = {
            limit = {
                or = { has_idea = partial_economic_mobilisation has_idea = war_economy }
                add_ideas = low_economic_mobilisation
            }
        }
        else = {
            custom_effect_tooltip = "The new Ausgleich agreement will prevent us from mobilizing the economy for war production."
        }
    }

    option = {
        name = "Let us end this once and for all."
        
        hidden_effect = { hungarian_independence = yes }
        declare_war_on = { target = HUN type = annex_everything }
    }
}