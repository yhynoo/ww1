add_namespace = holland_belgian_crisis

country_event = {
    id = holland_belgian_crisis.1
    title = "[HOL.GetName] Urges Us to Act"         # Ask France to take over Flanders
    desc = holland_belgian_crisis.1.d

    is_triggered_only = yes

    option = {
        name = "It is high time."

        add_stability = -0.05
        every_state = {
            limit = { is_claimed_by = FRA is_owned_by = HOL }
            transfer_state_to = FRA
        }

        HOL = { remove_ideas = sww1_idea_accommodate_belgian_refugees }

        ai_chance = { base = 3 }
    }

    option = {
        name = "We cannot do it now."
        HOL = {
            add_opinion_modifier = { target = FRA modifier = medium_decrease }
        }
    }
}

news_event = {
    id = holland_belgian_crisis.2
    title = "[FRA.GetName] Takes Over in Flanders"
    desc = holland_belgian_crisis.2.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "Finally."
    }
}

news_event = {
    id = holland_belgian_crisis.3
    title = "[FRA.GetName] Fails to Act"
    desc = holland_belgian_crisis.3.d

    is_triggered_only = yes

    option = {
        name = "This is disturbing."
    }
}

country_event = {
    id = holland_belgian_crisis.4
    title = "Shall We Abandon Our Neutrality?"          # If we decided to stay in Flanders (aggressive, anti-French move)
    desc = holland_belgian_crisis.4.d

    is_triggered_only = yes

    option = {
        trigger = { 
            has_war = no
            ENG = { exists = yes not = { has_war_with = HOL } }
        }
        name = "Ask [ENG.GetNameDef] for guarantees."

        ENG = { country_event = { id = holland_belgian_crisis.5 hours = 12 random_hours = 6 } }

        ai_chance = {
            base = 1
            modifier = { FRA = { is_in_faction = no } factor = 3 }
        }
    }

    option = {
        trigger = { 
            GER = { exists = yes not = { has_war_with = HOL } }
            is_in_faction = no
        }
        name = "Seek alliance with [GER.GetNameDef]."

        GER = { country_event = { id = holland_belgian_crisis.8 hours = 12 random_hours = 6 } }

        ai_chance = { 
            base = 3
            modifier = { FRA = { is_ally_with = ENG } factor = 3 }
        }
    }

    option = {
        name = "We shall stay neutral."

        ai_chance = { base = 0 }
    }
}

country_event = {
    id = holland_belgian_crisis.5
    title = "[HOL.GetName] Seeks Our Guarantees"
    desc = holland_belgian_crisis.5.d

    is_triggered_only = yes

    option = {
        name = "Grant guarantees."
        give_guarantee = HOL

        HOL = { news_event = { id = holland_belgian_crisis.6 hours = 12 random_hours = 6 } }
        ai_chance = { base = 3 }
    }

    option = {
        name = "We have no interest there."
        HOL = { news_event = { id = holland_belgian_crisis.7 hours = 12 random_hours = 6 } }

        ai_chance = { base = 1 }
    }
}

news_event = {
    id = holland_belgian_crisis.6
    title = "[ENG.GetName] Grants Guarantees to [HOL.GetNameDef]"
    desc = holland_belgian_crisis.6.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "A success for [HOL.GetNameDef]."
    }
}

news_event = {
    id = holland_belgian_crisis.7
    title = "[ENG.GetName] Not Interested"
    desc = holland_belgian_crisis.7.d

    is_triggered_only = yes

    option = {
        name = "A major setback."
    }
}

country_event = {
    id = holland_belgian_crisis.8
    title = "[HOL.GetName] Seeks Alliance"
    desc = holland_belgian_crisis.8.d

    is_triggered_only = yes

    option = {
        name = "This is a great opportunity."
        
        if = {
            limit = { is_in_faction = no }
            create_faction = "Central Powers"
        }
        faction_leader = { add_to_faction = HOL }

        HOL = { news_event = { id = holland_belgian_crisis.9 hours = 12 random_hours = 6 } }
        
        ai_chance = { 
            base = 3
            modifier = { num_faction_members < 3 factor = 3 }
        }
    }

    option = {
        name = "They are not a worthy ally."
        HOL = { news_event = { id = holland_belgian_crisis.10 hours = 12 random_hours = 6 } }

        ai_chance = { base = 1 }
    }
}

news_event = {
    id = holland_belgian_crisis.9
    title = "[GER.GetName] and [HOL.GetNameDef] Allied!"
    desc = holland_belgian_crisis.9.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "[HOL.GetNameDef] abandons neutrality."
    }
}

news_event = {
    id = holland_belgian_crisis.10
    title = "[ENG.GetName] Not Interested"
    desc = holland_belgian_crisis.10.d

    is_triggered_only = yes

    option = {
        name = "A major setback."
    }
}