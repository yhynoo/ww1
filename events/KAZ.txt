add_namespace = kaz_uprising

# uprising
    news_event = {
        id = kaz_uprising.1
        title = "Alash Autonomy Established!"

        is_triggered_only = yes
        major = yes

        immediate = {
            BUK = {
                news_event = { id = buk_liberals.1 days = 12 }
            }
            KAZ = {
                every_core_state = { PREV = { set_state_owner = PREV } }
                load_oob = KAZ

                set_province_controller = 12594
                set_province_controller = 1591  set_province_controller = 13148

                set_province_controller = 4867  set_province_controller = 4595 set_province_controller = 1840  
                set_province_controller = 4651  set_province_controller = 7711 set_province_controller = 4224 

                add_victory_points = { province = 12594 value = 2 }

                every_country = {
                    limit = { original_tag = RUS } 
                    declare_war_on = { target = PREV type = annex_everything }
                }
            }
            RUS = {
                news_event = { id = tms_uprising.1 days = 7 }
            }
        }

        option = {
            name = "Ok."
        }
    }