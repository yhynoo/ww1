add_namespace = italy_workers

news_event = {
    id = italy_workers.1
    title = "‘Avanti!’ Against the War"
    desc = italy_workers.1.d

    fire_only_once = yes
    trigger = {
        tag = ITA
        AND = { 
            is_in_faction = no
            FRA = { has_war_with = GER }
        }
    }

    mean_time_to_happen = { months = 3 }

    option = {
        name = "These cowards."

        add_popularity = { ideology = communism popularity = 0.05 }
        add_war_support = -0.05
    }
}