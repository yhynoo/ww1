add_namespace = communism

# - diplomacy
    # short: decision + atmosphere event
        country_event = {
            id = communism.1
            title = "Withdrawal from Treaties"
            desc = communism.1.d

            is_triggered_only = yes

            option = {
                name = "We are not subjects to old treaties."

                if = {
                    limit = { is_faction_leader = yes }
                    dismantle_faction = yes
                }
                else = {
                    if = {
                        limit = { is_in_faction = yes }
                        leave_faction = yes
                    }
                }

                every_country = {
                    limit = { is_guaranteed_by = ROOT }
                    ROOT = { diplomatic_relation = { country = PREV relation = guarantee active = no } }
                }
                every_country = {
                    limit = { has_non_aggression_pact_with = ROOT }
                    ROOT = { diplomatic_relation = { country = PREV relation = non_aggression_pact active = no } }
                }

                # atmosphere event; can be gated as a "gamble outcome" with random = { chance = 50 ... }
                news_event = { id = communism.2 hours = 6 }
            }
            option = {
                name = "Why would we do that?"       # safe option

                # AI block
                ai_chance = { }
            }
        }

        news_event = {
            id = communism.2
            title = "[FROM.GetName] Withdraws from Treaties"
            desc = communism.2.d

            is_triggered_only = yes
            major = yes

            option = {
                name = "As expected."
                
                if = {
                    limit = { has_guaranteed = FROM }
                    diplomatic_relation = { country = FROM relation = guarantee active = no }
                }
                if = {
                    limit = { has_non_aggression_pact_with = FROM }
                    diplomatic_relation = { country = FROM relation = non_aggression_pact active = no }
                }
            }
        }