add_namespace = holland_shell 

country_event = {
    id = holland_shell.1
    title = "[HOL.GetName] Asks for Khuzestan Oil"
    desc = holland_shell.1.d

    is_triggered_only = yes

    option = {
        name = "We must agree to this."
        FROM = { news_event = { id = holland_shell.2 hours = 6 random_hours = 6 } }

        give_resource_rights = { receiver = HOL state = 413 }

        ai_chance = { 
            base = 1
        }
    }
    option = {
        name = "Ignore them."
        HOL = { create_wargoal = { target = PER type = puppet_wargoal_focus } }

        ai_chance = { base = 1 }
    }
}

news_event = {
    id = holland_shell.2
    title = "[HOL.GetName] Accesses Khuzestan Oil"
    desc = holland_shell.2.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "Interesting."
    }
}