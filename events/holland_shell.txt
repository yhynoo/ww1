add_namespace = holland_shell 

country_event = {
    id = holland_shell.1
    title = "[HOL.GetName] Asks for Iraqi Oil"
    desc = holland_shell.1.d

    is_triggered_only = yes

    option = {
        name = "We must agree to this."
        FROM = { news_event = { id = holland_shell.2 hours = 6 random_hours = 6 } }

        give_resource_rights = { receiver = HOL state = 291 }
        manage_turkish_debt = yes

        ai_chance = { 
            base = 0
            modifier = { 
                or = { has_idea = idea_amassed_foreign_debt has_idea = idea_significant_foreign_debt } 
                add = 2 
            }
        }
    }
    option = {
        name = "Ignore them."
        ai_chance = { base = 1 }
    }
}

news_event = {
    id = holland_shell.2
    title = "Dutch Shell Accesses Iraqi Oil"
    desc = holland_shell.2.d

    is_triggered_only = yes
    major = yes

    option = {
        name = "Interesting."
    }
}