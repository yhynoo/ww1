add_namespace = syr_uprising

# uprising
    news_event = {
        id = syr_uprising.1
        title = "Uprising in Syria!"

        is_triggered_only = yes
        major = yes

        immediate = {
            SYR = {
                every_core_state = { PREV = { set_state_owner = PREV } }
                load_oob = SYR

                set_province_controller = 4111
                set_province_controller = 12473

                declare_war_on = { target = PREV type = independence_wargoal }
            }
        }

        option = {
            name = "Ok."
        }
    }