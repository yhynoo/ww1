add_namespace = other

# army
    news_event = {
        id = other.4
        hidden = yes

        is_triggered_only = yes

        option = {
            clr_country_flag = early_military
            mark_focus_tree_layout_dirty = yes
        }
    }

    news_event = {
        id = other.5
        title = "Army Marches in the Capital"
        desc = other.5.d

        is_triggered_only = yes

        option = {
            name = "What a sight."
            add_war_support = 0.025
        }
    }

# industrialization
    news_event = {
        id = other.6
        hidden = yes

        is_triggered_only = yes

        option = {
            clr_country_flag = pre_industrial_state
            mark_focus_tree_layout_dirty = yes
        }
    }
