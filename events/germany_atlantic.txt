add_namespace = germany_atlantic

country_event = {
    id = germany_atlantic.1
    title = "Navy to Expand Submarine Warfare?"
    desc = germany_atlantic.1.d

    trigger = {
        tag = GER
        war_length_with = { tag = ENG months > 6 }
    }
    fire_only_once = yes

    option = {
        name = "Gott Strafe England!"
        add_timed_idea = { idea = idea_atlantic_submarine_warfare years = 2 }

        hidden_effect = { 
            random = { 
                chance = 80 
                USA = { news_event = { id = germany_atlantic.2 days = 60 random_days = 60 } }
            }

            random = { 
                chance = 20
                SPA = { news_event = { id = germany_atlantic.3 days = 60 random_days = 60 } }
            }

            random = { 
                chance = 20
                ARG = { news_event = { id = germany_atlantic.3 days = 60 random_days = 60 } }
            }
        }
        ai_chance = { base = 3 }
    }
    option = {
        name = "This is too risky."
        add_political_power = -25

        ai_chance = { base = 1 }
    }
}

news_event = {
    id = germany_atlantic.2
    title = "Lusitania Lost in a Submarine Attack!"
    desc = germany_atlantic.2.d

    is_triggered_only = yes
    major = yes

    trigger = { not = { USA = { has_war_with = GER } } }

    option = {
        name = "Horrible."

        if = {
            limit = { has_war_with = GER }
            add_war_support = 0.05
        }

        if = {
            limit = { tag = USA }
            add_war_support = 0.1
        }
    }
}

news_event = {
    id = germany_atlantic.3
    title = "Attacks on [ROOT.GetAdjective] Trade Fleet"
    desc = germany_atlantic.3.d

    is_triggered_only = yes
    trigger = { not = { has_war_with = GER } }

    option = {
        name = "Outrageous!"
        create_wargoal = { target = GER type = topple_government }
    }
}