custom_original_question = {
    if = {
        limit = { original_tag = GER }
        
        add_political_power = 50
        every_state = { limit = { state = 34 } add_core_of = GER }
    }
    else = {
        add_political_power = 100
    }
}