setup_civil_war = {
    set_country_flag = during_civil_war

    # can't join factions
    set_rule = { can_join_factions = no }
    if = { limit = { has_government = communism } set_cosmetic_tag = generic_communism }

    if = {
        limit = { any_country = { original_tag = ROOT has_country_flag = after_civil_war } }
        news_event = { id = unrest.13 hours = 6 }   # total collapse
    }
    else = {
        news_event = { id = unrest.12 hours = 6 }
    }
}