setup_civil_war = {
    set_country_flag = during_civil_war
    _manage_advisors = yes

    # can't join factions
    set_rule = { can_join_factions = no }

    # if we're subjects
    every_country = {
        limit = { has_subject = PREV }
        country_event = { id = dominion.21 days = 2 }
    }

    if = {
        limit = { any_country = { original_tag = ROOT has_country_flag = after_civil_war } }
        country_event = { id = civil_war.3 hours = 6 }   # total collapse
    }
    else = {
        country_event = { id = civil_war.2 hours = 6 }
    }
}