adriatic_unrest = {
    leave_serbia = {
        icon = generic_form_nation
        cost = 150
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 105 state = 107 state = 108 }
                is_owned_and_controlled_by = TUR
            }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            every_state = {
                limit = {
                    or = { state = 105 state = 107 state = 108 }
                    is_owned_and_controlled_by = TUR
                }
                transfer_state_to = SER
            }

            if = {
                limit = { has_idea = idea_unrest_in_the_adriatic }
                swap_ideas = {
                    remove_idea = idea_unrest_in_the_adriatic
                    add_idea = idea_unrest_in_greece
                }
            }
            if = {
                limit = { has_idea = idea_unrest_in_serbia }
                remove_ideas = idea_unrest_in_serbia
            }
        }
    }
    greece_settlements = {
        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                is_core_of = GRE
                is_owned_and_controlled_by = TUR
            }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            hidden_effect = { country_event = turkey_greece.1 days = 14 random_days = 28 }

            if = {
                limit = { has_idea = idea_unrest_in_the_adriatic }
                swap_ideas = {
                    remove_idea = idea_unrest_in_the_adriatic
                    add_idea = idea_unrest_in_serbia
                }
            }
            if = {
                limit = { has_idea = idea_unrest_in_greece }
                remove_ideas = idea_unrest_in_greece
            }
        }
    }
    turanist_movement = {
        icon = generic_prepare_civil_war

        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 405 state = 584 state = 585 state = 587 }
                is_owned_and_controlled_by = SOV
            }
        }

        ai_will_do = {
            base = 0
            modifier = { and = { date > 1915.1.1 has_war = no } add = 9 }
        }

        complete_effect = {
            SOV = {
                add_timed_idea = { idea = idea_unrest_in_central_asia years = 1 }
                hidden_effect = { country_event = { id = soviet_central_asia.1 hours = 6 random_hours = 6 } }
            }
        }
    }
}

turkey_alliances = {
    tur_offer_alliance = {
        cost = 75
        fire_only_once = yes

        targets = { }
        available = { }

        complete_effect = {
            FROM = { country_event = { id = x_alliance.2 hours = 6 random_hours = 6 } }
        }
    }
    tur_join_alliance = {
        cost = 100
        fire_only_once = yes

        targets = { ENG FRA GER }
        available = { 
            is_in_faction = no
            FROM = { 
                not = { has_war_with = ROOT }
                or = { is_in_faction = no is_faction_leader = yes } 
            }
        }

        ai_will_do = { 
            base = 0
            modifier = { threat > 0.2 add = 9 }

            modifier = { and = { FROM = { tag = FRA } has_completed_focus = TUR_on_our_own } add = 6 }      # we prefer the French
            modifier = { FROM = { is_ally_with = SOV } factor = 0 }                                         # but we'll never ally with Russia.
        }

        complete_effect = {
            FROM = { country_event = { id = x_alliance.1 hours = 6 random_hours = 6 } }
        }
    }
}

align_with_french = {
    french_air_experts = {
        cost = 50
        fire_only_once = yes

        complete_effect = {
            air_experience = 50
            FRA = { add_opinion_modifier = { target = TUR modifier = invited_experts } }
        }
    }
    invite_french_investors = {
        icon = generic_construction

        fire_only_once = yes
        cost = 75

        available = { controls_state = 341 }
        complete_effect = {
            341 = {
                add_building_construction = { type = arms_factory level = 2 instant_build = yes }
                add_extra_state_shared_building_slots = 2
            }
        }
    }
}

tackle_the_debt = {
    sell_egypt = {
        fire_only_once = yes
        cost = 75

        available = { 
            not = { has_war_with = ENG }
            every_state = {
                limit = { or = { state = 447 state = 452 state = 456 state = 457 state = 551 state = 552 } }
                is_owned_and_controlled_by = TUR
            }
        }

        complete_effect = {
            ENG = { country_event = { id = turkey_egypt.1 hours = 6 random_hours = 6 } }
        }
    }
    develop_aegean = {
        icon = generic_construction

        fire_only_once = yes
        cost = 75

        available = { controls_state = 339 }
        complete_effect = {
            if = {
                limit = { has_idea = idea_amassed_foreign_debt }
                swap_ideas = {
                    remove_idea = idea_amassed_foreign_debt
                    add_idea = idea_significant_foreign_debt
                }
            }
            if = {
                limit = { has_idea = idea_significant_foreign_debt }
                remove_ideas = idea_significant_foreign_debt
            }

            339 = {
                add_building_construction = { type = dockyard level = 2 instant_build = yes }
                add_extra_state_shared_building_slots = 2
            }
        }
    }
}