dc_sarajevo_declaration = {
    withdraw_from_serbia = {
        cost = 150
        fire_only_once = yes

        available = {
            or = {
                has_idea = sww1_idea_unrest_in_the_adriatic
                has_idea = sww1_idea_unrest_in_serbia
            }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            release = SER

            if = {
                limit = { has_idea = sww1_idea_unrest_in_the_adriatic }
                swap_ideas = {
                    remove_idea = sww1_idea_unrest_in_the_adriatic
                    add_idea = sww1_idea_unrest_in_greece
                }
                else = { remove_ideas = sww1_idea_unrest_in_serbia }
            }

            hidden_effect = {
                news_event = { id = turkey_sarajevo_declaration.1 hours = 12 random_hours = 6 }

                104 = { remove_claim_by = TUR }
                804 = { remove_claim_by = TUR }

                104 = { add_claim_by = SER }
                802 = { add_claim_by = SER }
                803 = { add_claim_by = SER }
                804 = { add_claim_by = SER }

                SER = { load_oob = "SER_1912" }
            }
        }
    }
    call_for_the_liberation_of_crimea_and_caucasus = {
        icon = generic_prepare_civil_war

        cost = 150
        fire_only_once = yes

        days_remove = 90
        war_with_target_on_remove = yes
        
        available = { can_declare_war_on = SOV }

        complete_effect = {
            news_event = { id = turkey_sarajevo_declaration.2 hours = 12 random_hours = 6 }
        }

        ai_will_do = {
            base = 6
            modifier = { is_in_faction = yes factor = 2 }
            modifier = { and = { date > 1916.1.1 has_war = no } factor = 3 }
        }

        remove_effect = {
            set_country_flag = liberate_caucasus
            create_wargoal = {
                target = SOV
                type = liberate_wargoal
            }
        }
    }
}

dc_turkey_alliances = {
    turkey_england = {
        targets = { ENG }
        available = {
            is_in_faction = no
            has_completed_focus = TUR_sarajevo_declaration

            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        ai_will_do = {
            base = 2
            modifier = { has_country_flag = we_do_our_thing factor = 12 }
            modifier = { OR = { ENG = { is_ally_with = ITA } ENG = { is_ally_with = SOV } ENG = { has_opinion = { target = TUR value < 25 } } } factor = 0 }
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = turkey_england.1 hours = 12 random_hours = 6 } }
            hidden_effect = { set_country_flag = stop_influencing_uk }
        }
    }
    turkey_france = {
        targets = { FRA }
        available = {
            is_in_faction = no
            has_completed_focus = TUR_alafranga
            
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = turkey_france.1 hours = 12 random_hours = 6 } }
        }
    }
    turkey_germany = {
        targets = { GER }
        available = {
            is_in_faction = no
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        cost = 75
        fire_only_once = yes

        ai_will_do = {
            base = 9
            modifier = { has_country_flag = we_do_our_thing factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = turkey_germany.1 hours = 12 random_hours = 6 } }
        }
    }
}

dc_alafranga = {
    invite_french_aircraft = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            797 = { add_building_construction = { type = arms_factory level = 2 instant_build = yes } }
        }
    }
    invite_aviation_experts = {
        cost = 75
        fire_only_once = yes 

        ai_will_do = { base = 9 }

        complete_effect = {
            air_experience = 50
            add_tech_bonus = {
				name = fighter_bonus
				bonus = 1.0
				uses = 2
				category = air_equipment
			}
        }
    }
}

dc_jihad = {
    extend_draft_in_arab_lands = {
        icon = generic_prepare_civil_war
        cost = 50

        days_remove = 90
        available = { not = { has_national_idea = sww1_looming_arab_revolt } }

        ai_will_do = {
            base = 1
            modifier = { has_war = yes factor = 6 }
        }

        modifier = {
            conscription = 0.01
            training_time_factor = -0.05
        }
    }
    develop_iraq = {
        icon = generic_industry
        cost = 100

        fire_only_once = yes

        ai_will_do = { base = 9 }

        available = {
            any_state = {
                or = { state = 291 state = 675 state = 676 state = 986 state = 987 }
                is_controlled_by = TUR
            }
        }

        complete_effect = {
            random_owned_controlled_state = {
                limit = { or = { state = 291 state = 675 state = 676 state = 986 state = 987 } }
                
                add_extra_state_shared_building_slot = 1
                add_building_construction = { type = industrial_complex level = 1 instant_build = yes }
            }
            random_owned_controlled_state = {
                limit = { or = { state = 291 state = 675 state = 676 state = 986 state = 987 } }
                
                add_extra_state_shared_building_slot = 1
                add_building_construction = { type = arms_factory level = 1 instant_build = yes }
            }
        }
    }
    support_greek_albanians = {
        icon = generic_civil_support
        cost = 100

        fire_only_once = yes

        available = {
            or = {
                has_idea = sww1_idea_unrest_in_the_adriatic
                has_idea = sww1_idea_unrest_in_serbia
            }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            add_stability = 0.05
            country_event = { id = turkey_unrest_in_greece.1 hours = 12 random_hours = 6 }
        }
    }
}

dc_tackle_the_debt = {
    offer_egypt_to_uk = {
        visible = {
            or = {
                has_idea = sww1_idea_amassed_foreign_debt
                has_idea = sww1_idea_significant_foreign_debt
            }
        }

        cost = 150
        fire_only_once = yes

        available = {
            ENG = {
                exists = yes
                not = { has_war_with = TUR }
            }

            owns_state = 447
            owns_state = 452
            owns_state = 456
            owns_state = 457
            owns_state = 551
            owns_state = 552
            owns_state = 767
            owns_state = 907
        }

        ai_will_do = {
            base = 6
            modifier = { has_idea = sww1_idea_amassed_foreign_debt factor = 3 }
        }

        complete_effect = {
            custom_effect_tooltip = "If accepted, this decision will clear all our debt."
            ENG = { country_event = { id = turkey_tackle_the_debt.1 hours = 12 random_hours = 6 } }
        }
    }
    develop_shipbuilding_in_aegean = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { 
            base = 3
            modifier = { or = { has_idea = sww1_idea_amassed_foreign_debt has_idea = sww1_idea_significant_foreign_debt } factor = 5 }
        }

        complete_effect = {
            if = {
                limit = { has_idea = sww1_idea_amassed_foreign_debt }
                swap_ideas = {
                    remove_idea = sww1_idea_amassed_foreign_debt
                    add_idea = sww1_idea_significant_foreign_debt
                }
            }
            else_if = {
                limit = { has_idea = sww1_idea_significant_foreign_debt }
                remove_ideas = sww1_idea_significant_foreign_debt
            }
            else = {
                339 = {
                    add_building_construction = { type = dockyard level = 2 instant_build = yes }
                }
            }
        }
    }
    invite_austrian_investors = {
        cost = 100
        fire_only_once = yes

        available = {
            AUS = {
                exists = yes
                not = { has_war_with = TUR }
            }
        }

        ai_will_do = { 
            base = 3
            modifier = { or = { has_idea = sww1_idea_amassed_foreign_debt has_idea = sww1_idea_significant_foreign_debt } factor = 3 }
        }

        complete_effect = {
            if = {
                limit = { has_idea = sww1_idea_amassed_foreign_debt }
                swap_ideas = {
                    remove_idea = sww1_idea_amassed_foreign_debt
                    add_idea = sww1_idea_significant_foreign_debt
                }
            }
            else_if = {
                limit = { has_idea = sww1_idea_significant_foreign_debt }
                remove_ideas = sww1_idea_significant_foreign_debt
            }
            else = {
                340 = {
                    add_building_construction = { type = industrial_complex level = 2 instant_build = yes }
                }
            }
        }
    }
}