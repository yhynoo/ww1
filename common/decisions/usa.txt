monroe_doctrine = {
    demand_colombia_withdraw = {
        icon = border_war
        fire_only_once = yes
        cost = 50

        available = {
            any_state = { is_core_of = MEX is_controlled_by = COL }
        }

        ai_will_do = {
            base = 0
            modifier = { not = { has_completed_focus = USA_adjust_the_government } add = 6 }
        }

        complete_effect = {
            COL = { country_event = { id = usa_withdraw.1 hours = 6 random_hours = 6 } }
        }
    }
    andes_for_protection = {
        icon = chromium
        fire_only_once = yes
        cost = 50
        
        available = {
            506 = {
                custom_trigger_tooltip = {
                    tooltip = tt_resource_rights
                    owner = {
                        not = { has_war_with = ROOT }
                        controls_state = 506 has_resources_rights = { state = 506 resources = { chromium } }
                    }
                }
            }
        }

        ai_will_do = { 
            factor = 9
            modifier = { has_war = yes add = 6 }
        }

        complete_effect = { 506 = { owner = { country_event = { id = usa_andes.1 hours = 6 random_hours = 6 } } } }
    }
    align_mexico = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 75

        targets_dynamic = yes
        target_trigger = { FROM = { original_tag = MEX has_government = democratic } }
        
        available = {
            can_declare_war_on = FROM
            FROM = { exists = yes is_subject = no }
        }

        target_non_existing = yes
        ai_will_do = {
            factor = 0
            modifier = { not = { has_government = democratic } add = 9 }
        }

        complete_effect = { FROM = { country_event = { id = _align.1 hours = 6 random_hours = 6 } } }
    }
}

across_the_pacific = {
    liberate_australia = {
        icon = generic_political_discourse
        cost = 75

        targets = { AST }
        available = { FROM = { is_subject = yes not = { has_war_with = ROOT } } }

        ai_will_do = { 
            base = 6
            modifier = { not = { is_ally_with = ENG } add = 3 }
        }

        complete_effect = { FROM = { news_event = { id = _autonomy.1 hours = 6 random_hours = 6 } } }
    }
    assert_claims_in_oceania = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   {
            can_declare_war_on = FROM 
            any_state = { or = { state = 581 state = 636 state = 639 state = 643 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 581 state = 636 state = 639 state = 643 } is_owned_and_controlled_by = FROM } }

        ai_will_do = { factor = 6 }

        complete_effect = { 
            every_state = {
                limit = { or = { state = 581 state = 636 state = 639 state = 643 }  is_owned_and_controlled_by = FROM }
                add_claim_by = ROOT
            }
            FROM = { country_event = { id = _demand.1 hours = 6 } } 
        }
    }
}