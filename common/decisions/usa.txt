monroe_doctrine = {
    align_mexico = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 150

        available = {
            any_country = { original_tag = MEX has_government = democratic }
        }

        ai_will_do = { 
            base = 0
            modifier = {
                MEX = { has_civil_war = no has_government = democratic }
                add = 9
            }
            modifier = { has_completed_focus = USA_on_our_own add = 3 }
        }

        complete_effect = {
            MEX = { country_event = { id = _align.1 hours = 6 random_hours = 6 } }
        }
    }
    demand_colombia_withdraw = {
        icon = border_war
        fire_only_once = yes
        cost = 100

        available = {
            any_state = { 
                is_core_of = MEX 
                is_controlled_by = COL 
            }
        }

        ai_will_do = {
            base = 0
            modifier = { not = { has_completed_focus = USA_adjust_the_government } add = 6 }
        }

        complete_effect = {
            COL = { country_event = { id = usa_withdraw.1 hours = 6 random_hours = 6 } }
        }
    }
    andes_for_protection = {
        icon = chromium
        fire_only_once = yes
        cost = 75

        available = {
            any_state = {
                or = { state = 279 state = 506 }
                is_owned_and_controlled_by = ADE
            }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            ADE = { country_event = { id = usa_andes.1 hours = 6 } }
        }
    }
}

adapt_wilsonianism = {
    liberate_australia = {
        icon = generic_political_discourse
        cost = 75
        days_remove = 90

        available = {
            AST = { is_subject = yes }
        }

        ai_will_do = { 
            base = 6
            modifier = { not = { is_ally_with = ENG } add = 3 }
        }

        complete_effect = {
            AST = { news_event = { id = usa_australia.1 hours = 6 random_hours = 6 } }
        }
    }
    aid_to_eastern_europe = {
        icon = generic_industry
        cost = 50
        days_remove = 90

        targets = { POL UKR }
        target_non_existing = yes

        ai_will_do = {
            base = 0
            modifier = { FROM = { has_government = ROOT } add = 6 }
        }

        available = { 
            FROM = { exists = yes } 
            has_equipment = { infantry_equipment > 599 }
        }

        complete_effect = {
            add_war_support = 0.025
            send_equipment = { equipment = infantry_equipment amount = 600 target = FROM }
        }
    }
}