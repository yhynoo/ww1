dc_monroe_doctrine = {
    aid_for_mexico = {
        icon = generic_industry
        cost = 100
        fire_only_once = yes

        available = {
            any_country = {
                original_tag = MEX
                has_government = democratic
            }
        }

        ai_will_do = { 
            base = 6
            modifier = { has_completed_focus = USA_the_arsenal_of_liberty factor = 3 }
        }

        complete_effect = {
            random_country = {
                limit = { original_tag = MEX has_government = democratic }
                    
                country_event = { id = usa_aid_for_mexico.1 hours = 12 random_hours = 6 }
                add_opinion_modifier = { target = USA modifier = medium_increase }
            }
        }
    }
    intervene_in_central_america = {
        icon = generic_prepare_civil_war
        cost = 150
        fire_only_once = yes

        available = { 
            MEX = { has_war_with = COL }
            has_war_support > 0.3
        }

        complete_effect = {
            country_event = { id = usa_intervene_in_central_america.1 hours = 3 }
        }
    }
}

dc_usa_alliances = {
    usa_australia = {
        targets = { AST }
        available = {
            is_in_faction = no
            has_government = democratic
            
            FROM = { 
                is_subject = no
                has_government = democratic
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = usa_australia.1 hours = 12 random_hours = 6 } }
        }
    }
    usa_england = {
        targets = { ENG }
        available = {
            is_in_faction = no
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        ai_will_do = {
            base = 6
            modifier = { has_completed_focus = USA_not_our_war factor = 0 }

            modifier = { threat > 0.5 factor = 2 }
            modifier = { and = { ENG = { has_war_with = GER } date > 1917.4.1 } factor = 12 }
        }

        cost = 75
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = usa_england.1 hours = 12 random_hours = 6 } }
        }
    }
    usa_france = {
        targets = { FRA }
        available = {
            is_in_faction = no
            has_government = democratic

            FROM = {
                has_government = democratic
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        ai_will_do = {
            base = 3
            modifier = { has_completed_focus = USA_not_our_war factor = 0 }
            modifier = { and = { FRA = { has_war_with = GER } date > 1915.4.1 } factor = 12 }
        }

        cost = 75
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = usa_france.1 hours = 12 random_hours = 6 } }
        }
    }
}

dc_north_atlantic_expansion = {
    claim_greenland = {
        icon = generic_civil_support
        cost = 100
        fire_only_once = yes

        available = {
            not = { has_war_with = DEN }
            any_state = {
                or = { state = 100 state = 101 }
                is_owned_by = DEN
            }
        }

        ai_will_do = {
            base = 3
            modifier = { has_war_with = GER factor = 3 }
        }

        complete_effect = {
            DEN = { country_event = { id = usa_north_atlantic_expansion.1 hours = 12 random_hours = 6 } }
        }
    }
    develop_iceland = {
        icon = generic_construction
        cost = 100
        fire_only_once = yes

        available = { 100 = { is_owned_and_controlled_by = USA } }

        ai_will_do = { base = 6 }

        complete_effect = {
            100 = { 
                add_building_construction = { type = infrastructure level = 1 instant_build = yes }
                add_building_construction = { type = naval_base province = 12674 level = 3 instant_build = yes }
            }
        }
    }
}

dc_american_industries = {
    introduce_tariffs = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            add_ideas = sww1_idea_isolationist_economy
        }
    }
    encourage_racial_integration = {
        cost = 75
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            add_stability = -0.05
            add_popularity = { ideology = authoritarianism popularity = -0.05 }

            hidden_effect = {
                country_event = { id = usa_embers_of_the_past.1 days = 7 random_days = 14 }
            }
        }
    }
    challenge_the_unions = {
        cost = 50
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { has_stability > 0.8 factor = 2 }
        }

        modifier = {
            stability_weekly = -0.05
            production_speed_buildings_factor = 0.1
        }
    }
}