MAN_decisions_warlordism = {
    empower_old_aristocracy = {
        icon = generic_nationalism
        cost = 25
        days_remove = 90

        ai_will_do = { modifier = { has_country_leader = { character = MAN_yuan_shikai ruling_only = yes } add = 9 } }

        modifier = {
            monarchism_drift = 0.02
            political_power_factor = 0.15
        }
    }
    invest_in_administration = {
        icon = generic_civil_support
        cost = 25
        days_remove = 90
        
        ai_will_do = { modifier = { has_country_leader = { character = MAN_sun_yat_sen ruling_only = yes } add = 9 } }

        modifier = {
            stability_weekly = 0.005
        }
    }
    hunt_warlord_militias = {
        icon = generic_prepare_civil_war
        cost = 50
        days_remove = 90

        modifier = {
            supply_factor = 0.1 
        }

        remove_effect = {
            add_stability = 0.025
        }
    }

    # extras
    support_civil_warlord = {
        visible = { has_country_flag = additional_warlord_decisions }
        icon = generic_political_discourse
        cost = 50
        days_remove = 90

        available = { has_government = democratic }
        ai_will_do = { modifier = { has_country_leader = { character = MAN_sun_yat_sen ruling_only = yes } add = 9 } }

        targets_dynamic = yes
        target_trigger = { 
            FROM = {
                and = { is_chinese_warlord = yes    is_subject_of = MAN }
                democratic < 0.5
            }
        }

        remove_effect = {
            FROM = {
                add_popularity = { ideology = democratic popularity = 0.05 }
                add_autonomy_ratio = { value = -0.1 }
            }
        }
    }
    eradicate_warlord = {
        visible = { has_country_flag = additional_warlord_decisions }
        icon = oppression
        cost = 75
        fire_only_once = yes

        targets_dynamic = yes
        target_non_existing = yes

        ai_will_do = { add = 9 }

        target_trigger = { 
            FROM = {
                is_chinese_warlord = yes
                has_country_flag = warlord_defeated
            }
        }

        complete_effect = {
            add_stability = 0.05
        }
    }
}

MAN_decisions_coronation = {
    coronation_timeout = {
        icon = generic_nationalism
        days_mission_timeout = 180
        is_good = yes

        activation = { has_country_flag = yuan_shikai_coronation }
        
        # early completion trigger
        available = {
            always = no
        }

        # cancellation trigger
        cancel_trigger = {
            not = { has_country_leader = { character = MAN_yuan_shikai ruling_only = yes } }
            clr_country_flag = yuan_shikai_coronation
        }

        # effect at all cases
        timeout_effect = {
            country_event = { id = manchukuo.14 hours = 6 }
            clr_country_flag = yuan_shikai_coronation
        }
    }
    corrupt_local_warlords = {
        icon = generic_civil_support
        cost = 25

        days_remove = 60
        modifier = {
            consumer_goods_factor = 0.05
            monarchism_drift = 0.025
        }
        cancel_trigger = {
            not = { has_active_mission = coronation_timeout }
        }
    }
    tighten_control = {
        icon = oppression
        cost = 25

        days_remove = 60
        ai_will_do = { add = 6 }

        modifier = {
            monarchism_drift = 0.02
        }
        cancel_trigger = {
            not = { has_active_mission = coronation_timeout }
        }
    }
    expose_army_scandals = {
        icon = generic_political_discourse
        cost = 25

        days_remove = 60
        modifier = {
            stability_weekly = -0.005
            monarchism_drift = -0.04
        }
        cancel_trigger = {
            not = { has_active_mission = coronation_timeout }
        }
    }
}