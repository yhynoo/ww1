contain_eastern_threats = {
    expand_defenses_in_carpathians = {
        icon = border_war
        fire_only_once = yes
        cost = 75

        available = {
            controls_state = 76
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            71 = { add_building_construction = { type = bunker province = 3581 level = 2 instant_build = yes } }
            75 = { add_building_construction = { type = bunker province = 6590 level = 2 instant_build = yes } }
        }
    }
    establish_ukraine = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 50

        available = { 
            UKR = { exists = no }
            controls_state = 202
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            release_puppet_on_controlled = UKR
            news_event = { id = austria_ukraine.1 hours = 6 random_hours = 6 }

            # allies help
            hidden_effect = {
                every_allied_country = {
                    every_controlled_state = { limit = { is_core_of = UKR } transfer_state_to = UKR }
                }
            }
        }
    }
}

second_vienna_treaty = {
    demand_serbia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = { 
            any_state = {
                is_core_of = SER

                can_declare_war_on = TUR
                is_owned_and_controlled_by = TUR
            }
        }

        ai_will_do = { 
            base = 9
            modifier = { TUR = { is_in_faction = yes } factor = 0.5 } 
        }

        complete_effect = {
            every_state = {
                limit = { is_core_of = SER }
                add_claim_by = AUS
            }

            TUR = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    two_sicilies = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 100

        available = {
            TTS = { exists = no }

            can_declare_war_on = ITA
            any_state = { is_core_of = TTS is_controlled_by = ITA }
        }

        ai_will_do = { 
            base = 6
            modifier = { is_ally_with = FRA factor = 1.5 }
        }

        days_remove = 180
        remove_effect = {
            puppet = TTS
            hidden_effect = { 
                start_sicilian_independence_war = yes
                news_event = { id = austria_sicily.1 hours = 6 random_hours = 6 }
            }
        }
    }
}

equal_development = {
    galician_oil = {
        icon = oil
        cost = 75
        fire_only_once = yes

        available = { controls_state = 89 }
        ai_will_do = { base = 9 }

        complete_effect = {
            89 = { add_resource = { type = oil amount = 12 } }
        }
    }
    tatra_trucks = {
        icon = generic_industry
        cost = 75
        fire_only_once = yes
        
        available = { controls_state = 75 }
        ai_will_do = { base = 12 }

        complete_effect = {
            75 = {
                add_building_construction = { type = industrial_complex level = 2 instant_build = yes }
                add_extra_state_shared_building_slots = 2
            }
        }
    }
}

