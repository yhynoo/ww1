contain_eastern_threats = {
    demand_serbia = {
        visible = { SER = { exists = no } }

        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   {
            can_declare_war_on = FROM 
            any_state = { or = { state = 105 state = 107 state = 108 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 105 state = 107 state = 108 } is_owned_and_controlled_by = FROM } }

        ai_will_do = {
            factor = 3
            modifier = {
                and = { has_war = no date > 1915.4.1 }
                factor = 3
            }
        }

        complete_effect = { 
            every_state = {
                limit = { or = { state = 105 state = 107 state = 108 } is_owned_and_controlled_by = FROM }
                add_claim_by = ROOT
            }
            FROM = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } } 
        }
    }
    align_serbia = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 75

        targets = { SER }
        available = {
            can_declare_war_on = FROM
            FROM = { exists = yes is_subject = no }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { is_in_faction = no } add = 6 }
        }

        complete_effect = { FROM = { country_event = { id = _align.1 hours = 6 random_hours = 6 } } }
    }
    arm_the_ukrainians = {
        icon = generic_industry
        fire_only_once = yes
        cost = 75

        targets = { UKR }
        available = { 
            FROM = { exists = yes } 
            has_equipment = { infantry_equipment > 899 }
        }

        target_non_existing = yes
        ai_will_do = { factor = 9 }

        complete_effect = {
            send_equipment = { equipment = infantry_equipment amount = 900 target = FROM }
            hidden_effect = { FROM = { news_event = { id = _equipment.1 hours = 6 } } }
        }
    }
    defenses_in_the_east = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 75

        ai_will_do = {
            factor = 0
            modifier = { has_war_with = SOV add = 9 }
        }

        complete_effect = {
            if = {
                limit = { controls_state = 76 }
                custom_effect_tooltip = tt_defenses_east

                hidden_effect = {
                    76 = { add_building_construction = { type = supply_node level = 1 province = 6711 instant_build = yes } }
                    build_railway = { start_province = 9660 target_province = 6711 }
                }
            }
            add_timed_idea = { idea = idea_defenses_in_the_east years = 1 }
        }
    }
}

denounce_adventurism = {
    use_church_influence = {
        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            factor = 0
            modifier = { has_war_with = GER add = 9 }
        }

        modifier = {
            war_support_weekly = 0.01
            army_core_defence_factor = 0.05
        }
    }
    guarantees_to_france = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        targets = { FRA }
        target_non_existing = yes

        available = {
            FROM = { exists = yes }
            and = { not = { has_war_with = FROM } not = { is_ally_with = FROM } }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { has_government = ROOT } add = 9 }
        }

        complete_effect = {
            FROM = { country_event = { id = _guarantees.1 hours = 6 random_hours = 6 } }
        }
    }
}