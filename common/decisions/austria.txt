contain_eastern_threats = {
    arm_the_ukrainians = {
        icon = generic_industry
        fire_only_once = yes
        cost = 75

        targets = { UKR }
        available = { 
            FROM = { exists = yes } 
            has_equipment = { infantry_equipment > 899 }
        }

        target_non_existing = yes
        ai_will_do = { factor = 9 }

        complete_effect = {
            send_equipment = { equipment = infantry_equipment amount = 900 target = FROM }
            hidden_effect = { FROM = { news_event = { id = _equipment.1 hours = 6 } } }
        }
    }
    align_serbia = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 75

        targets = { SER }
        available = {
            can_declare_war_on = FROM
            FROM = { exists = yes is_subject = no }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { is_in_faction = no } add = 6 }
        }

        complete_effect = { FROM = { country_event = { id = _align.1 hours = 6 random_hours = 6 } } }
    }
    romanian_oil = {
        icon = oil
        cost = 50
        fire_only_once = yes
        
        targets = { ROM }
        available = {
            ROM = {
                not = { has_war_with = ROOT }
                controls_state = 46 has_resources_rights = { state = 46 resources = { oil } }
            }
        }

        ai_will_do = { 
            factor = 9
            modifier = { has_war = yes add = 6 }
        }

        complete_effect = { 46 = { owner = { country_event = { id = aus_oil.1 hours = 6 random_hours = 6 } } } }
    }
}

denounce_adventurism = {
    use_church_influence = {
        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            factor = 0
            modifier = { has_war_with = GER add = 9 }
        }

        modifier = {
            war_support_weekly = 0.01
            army_core_defence_factor = 0.05
        }
    }
    guarantees_to_france = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        targets = { FRA }
        target_non_existing = yes

        available = {
            FROM = { exists = yes }
            and = { not = { has_war_with = FROM } not = { is_ally_with = FROM } }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { has_government = ROOT } add = 9 }
        }

        complete_effect = {
            FROM = { country_event = { id = _guarantees.1 hours = 6 random_hours = 6 } }
        }
    }
}