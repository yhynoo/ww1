dc_contain_russian_expansion = {
    declare_support_poland = {
        visible = {
            not = { controls_state = 10 }
        }

        cost = 75
        fire_only_once = yes

        available = {
            POL = { exists = no }
        } 

        ai_will_do = { 
            base = 3
            modifier = { is_in_faction = yes factor = 2 }
            modifier = { has_war_with = SOV factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_contain_russian_expansion.4 hours = 12 random_hours = 6 }
            set_country_flag = austria_likes_poland
        }
    }
    declare_support_ukraine = {
        visible = {
            not = { controls_state = 202 }
        }

        cost = 75
        fire_only_once = yes

        available = {
            UKR = { exists = no }
        } 

        ai_will_do = { 
            base = 3
            modifier = { is_in_faction = yes factor = 2 }
            modifier = { has_war_with = SOV factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_contain_russian_expansion.7 hours = 12 random_hours = 6 }
            set_country_flag = austria_likes_ukraine
        }
    }
}

dc_austria_alliances = {
    austria_afghanistan = {
        targets = { AFG }
        available = { 
            FROM = { is_in_faction = no is_subject = no }

            has_completed_focus = AUS_contain_russian_expansion
            or = { has_idea = sww1_idea_slavic_unrest has_wargoal_against = SOV }
        }

        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 6
            modifier = { has_war = yes factor = 2 }
            modifier = { has_completed_focus = AUS_second_vienna_treaty factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = austria_afghanistan.1 hours = 12 random_hours = 6 } }
        }
    }
    austria_france = {
        targets = { FRA }
        available = { 
            FROM = { is_in_faction = no is_subject = no has_government = monarchism }
            has_completed_focus = AUS_second_vienna_treaty
        }

        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 9
            modifier = { has_war = yes factor = 2 }
        }

        complete_effect = {
            FROM = { country_event = { id = austria_france.1 hours = 12 random_hours = 6 } }
        }
    }
    austria_germany = {
        targets = { GER }
        available = {
            is_in_faction = no
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        cost = 75
        fire_only_once = yes

        ai_will_do = {
            base = 6

            modifier = { has_idea = sww1_idea_slavic_unrest factor = 2 }
            modifier = { has_completed_focus = AUS_second_vienna_treaty factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = austria_germany.1 hours = 12 random_hours = 6 } }
        }
    }
}

dc_second_vienna_treaty = {
    liberate_southern_lands = {
        icon = generic_prepare_civil_war

        cost = 150
        fire_only_once = yes
        days_remove = 90

        available = { can_declare_war_on = TUR }
        war_with_on_remove = TUR

        complete_effect = {
            news_event = { id = austria_second_vienna_treaty.1 hours = 12 random_hours = 6 }
            every_state = {
                limit = {
                    or = {
                        state = 105 state = 107 state = 108 state = 802 state = 803 # Serbia + Kosovo
                        state = 44 state = 805 state = 934 # Albania

                        state = 106 state = 970 # Macedonia
                        state = 46 state = 48 state = 81 state = 801
                    }
                    is_owned_by = TUR
                }
                add_claim_by = AUS
            }
        }

        ai_will_do = {
            base = 2
            modifier = { TUR = { is_in_faction = no } factor = 3 }
        }

        remove_effect = {
            create_wargoal = { target = TUR type = liberate_wargoal }
        }
    }
    assert_claims_in_veneto = {
        icon = generic_prepare_civil_war
        
        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 159 state = 160 }
                is_owned_by = ITA
            }
        }

        ai_will_do = {
            base = 3
            modifier = { ITA = { is_in_faction = no } factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_second_vienna_treaty.2 hours = 12 random_hours = 6 }
            ITA = { add_opinion_modifier = { target = AUS modifier = medium_decrease } }

            every_state = {
                limit = {
                    or = { state = 159 state = 160 }
                }
                add_claim_by = AUS
            }
        }
    }
    align_low_countries = {
        icon = generic_prepare_civil_war
        
        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 6 state = 7 state = 35 state = 36 state = 977 }
                is_owned_by = HOL
            }
        }

        ai_will_do = {
            base = 2
            modifier = { HOL = { is_in_faction = no } factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_second_vienna_treaty.3 hours = 12 random_hours = 6 }
            HOL = { add_opinion_modifier = { target = AUS modifier = medium_decrease } }

            every_state = {
                limit = {
                    or = { state = 6 state = 7 state = 35 state = 36 state = 977 }
                }
                add_claim_by = AUS
            }
        }
    }
}

dc_austro-slavism = {
    establish_the_kingdom_of_croatia = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { 
            base = 6
            modifier = { and = { date > 1916.1.1 has_government = monarchism } factor = 2 }
        }

        complete_effect = {
            country_event = { id = austria_kingdom_of_croatia.1 hours = 3 }
        }
    }
    intensify_language_training = {
        icon = generic_prepare_civil_war

        cost = 75
        days_remove = 90

        ai_will_do = { base = 6 }

        modifier = {
            experience_gain_army_factor = 0.2
            coordination_bonus = 0.1
        }
    }
    appease_the_church = {
        cost = 50
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { has_war_support < 0.3 factor = 3 }
        }

        modifier = { war_support_weekly = 0.01 }
    }
}

dc_review_the_ausgleich_agreement = {
    address_magyarization = {
        icon = decision_generic_political_discourse

        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {             ## prioritize Hungary (+ 2 civs) or equal development (+ 1 civ, 1 mil, 1 airfield)
            country_event = { id = austria_review_ausgleich_agreement.1 hours = 2 }
        }        
    }
    invest_in_tatra = {
        icon = decision_generic_trucks

        cost = 100
        fire_only_once = yes
        available = { 74 = { is_owned_and_controlled_by = AUS } }

        ai_will_do = { base = 6 }

        complete_effect = {
            74 = {
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
        }
    }
    develop_galician_oil = {
        icon = oil

        cost = 100
        fire_only_once = yes
        available = { 89 = { is_owned_and_controlled_by = AUS } }

        ai_will_do = { base = 6 }

        complete_effect = {
            add_resource = {
                type = oil
                amount = 24
                state = 89
            }
            if = {
                limit = { has_tech = synth_oil_0 }
                89 = {
                    add_building_construction = {
                        type = oil_refinery
                        level = 1
                        instant_build = yes
                    }
                }
            }
        }
    }
}