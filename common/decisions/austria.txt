contain_eastern_threats = {
    demand_serbia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = { 
            can_declare_war_on = TUR
            has_war_support > 0.5
            
            any_state = {
                is_core_of = SER
                is_owned_and_controlled_by = TUR
            }
        }

        ai_will_do = { 
            base = 6
            modifier = { 
                or = {
                    has_war = yes
                    TUR = { 
                        or = { is_in_faction = yes has_country_flag = cotton_deal }
                    }
                }
                factor = 0.5 
            } 
            modifier = {
                GRE = {
                    exists = yes
                    is_in_faction = no
                }
                add = 1.5
            }
            modifier = { has_completed_focus = AUS_adjust_the_government factor = 0.25 }
        }

        complete_effect = {
            every_state = {
                limit = { is_core_of = SER }
                add_claim_by = AUS
            }

            TUR = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    support_ukrainian_independence = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 75

        available = { 
            UKR = { exists = no }
        }

        ai_will_do = {
            base = 6
            modifier = { has_war_with = SOV add = 6 }
        }

        complete_effect = {
            SOV = { add_timed_idea = { idea = idea_unrest_in_ukraine months = 6 } }
            news_event = { id = austria_ukraine.1 hours = 3 }

            hidden_effect = {
                SOV = { country_event = { id = austria_ukraine.2 days = 30 random_days = 60 } }
            }
        }
    }
    arm_ukrainians = {
        icon = generic_industry
        days_remove = 90
        cost = 50

        available = {
            UKR = { exists = yes }
            has_equipment = { infantry_equipment > 599 }
        }

        ai_will_do = {
            base = 0
            modifier = { UKR = { has_war_with = SOV } add = 9 }
        }

        complete_effect = {
            add_war_support = 0.025
            send_equipment = { equipment = infantry_equipment amount = 600 target = UKR }
        }
    }
}

denounce_adventurism = {
    guarantees_to_france = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 100

        available = {
            not = { has_war_with = FRA }
            not = { is_ally_with = FRA }
        }

        ai_will_do = {
            base = 0
            modifier = { FRA = { has_government = ROOT } }
        }

        complete_effect = {
            FRA = { country_event = { id = austria_france.1 hours = 6 random_hours = 6 } }
        }
    }
    expand_landesschutze = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 75

        ai_will_do = {
            base = 9
        }

        complete_effect = {
            add_timed_idea = { idea = idea_landesschutze months = 6 }

            add_tech_bonus = {
				name = special_forces_bonus bonus = 1.0 uses = 1
				technology = mountaineers_2
                technology = mountaineers_3
			}

            hidden_effect = {
                add_ai_strategy = { type = role_ratio id = mountaineers value = 30 }
            }
        }
    }
}

equal_development = {
    galician_oil = {
        icon = oil

        fire_only_once = yes
        cost = 75

        ai_will_do = { base = 9 }

        available = { controls_state = 89 }
        complete_effect = {
            89 = { add_resource = { type = oil amount = 24 } }
        }
        
    }
    tatra_trucks = {
        fire_only_once = yes
        cost = 75

        ai_will_do = { base = 12 }

        available = { controls_state = 75 }
        complete_effect = {
            75 = {
                add_building_construction = { type = industrial_complex level = 2 instant_build = yes }
                add_extra_state_shared_building_slots = 2
            }
        }
    }
}

