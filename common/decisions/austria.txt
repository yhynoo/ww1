dc_contain_russian_expansion = {
    mountain_alliance = {
        cost = 150
        fire_only_once = yes

        available = {
            AFG = { 
                is_subject = no 
                is_in_faction = no
            }
        }

        ai_will_do = { 
            base = 1
            modifier = { has_idea = sww1_idea_slavic_unrest factor = 3 }
            modifier = { or = { has_country_flag = we_rejected_germany has_war = yes } factor = 5 }
        }

        complete_effect = {
            AFG = { country_event = { id = austria_contain_russian_expansion.1 hours = 12 random_hours = 6 } }
        }
    }
    invite_turkey = {
        cost = 150
        fire_only_once = yes

        available = {
            has_idea = sww1_idea_slavic_unrest
            TUR = { 
                is_in_faction = no
                has_completed_focus = TUR_sarajevo_memorandum
            }
        }

        ai_will_do = {
            base = 6
            modifier = { threat > 0.1 factor = 3 }
        }

        complete_effect = {
            TUR = { country_event = { id = austria_contain_russian_expansion.4 hours = 12 random_hours = 6 } }
        }
    }
    declare_support_poland = {
        cost = 100
        fire_only_once = yes

        available = {
            POL = { exists = no }
        } 

        ai_will_do = { 
            base = 2
            modifier = { has_war_with = SOV factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_contain_russian_expansion.7 hours = 12 random_hours = 6 }
            set_country_flag = austria_likes_poland
        }
    }
}

dc_second_vienna_treaty = {
    liberate_southern_lands = {
        icon = generic_prepare_civil_war

        cost = 150
        fire_only_once = yes
        days_remove = 90

        available = {
            can_declare_war_on = TUR
        }

        war_with_on_remove = TUR

        complete_effect = {
            news_event = { id = austria_second_vienna_treaty.1 hours = 12 random_hours = 6 }
            every_state = {
                limit = {
                    or = {
                        state = 105     # Serbia + Kosovo
                        state = 107
                        state = 108
                        state = 802
                        state = 803

                        state = 44      # Albania
                        state = 805
                        state = 934

                        state = 106     # Macedonia
                        state = 970

                        state = 46
                        state = 48
                        state = 81
                        state = 801
                    }
                    is_owned_by = TUR
                }
                add_claim_by = AUS
            }
        }

        ai_will_do = {
            base = 3
            modifier = {
                and = { date > 1915.1.1 has_war = no }
                factor = 2
            }
            modifier = { TUR = { is_in_faction = no } factor = 3 }
        }

        remove_effect = {
            create_wargoal = {
                target = TUR
                type = liberate_wargoal
            }
        }
    }
    assert_claims_in_veneto = {
        icon = generic_prepare_civil_war
        
        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = {
                    state = 159
                    state = 160
                }
                is_owned_by = ITA
            }
        }

        ai_will_do = {
            base = 3
            modifier = { ITA = { is_in_faction = no } factor = 3 }
            modifier = { or = { is_ally_with = FRA TTS = { exists = yes } } factor = 3 }
        }

        complete_effect = {
            news_event = { id = austria_second_vienna_treaty.2 hours = 12 random_hours = 6 }
            ITA = {
                add_opinion_modifier = {
                    target = AUS
                    modifier = medium_decrease
                }
            }

            159 = { add_claim_by = AUS }
            160 = { add_claim_by = AUS }
        }
    }
    invite_france = {
        icon = decision_generic_political_discourse

        cost = 100
        fire_only_once = yes
        available = { FRA = { has_government = monarchism is_in_faction = no } }

        ai_will_do = {
            base = 6
            modifier = { threat > 0.2 factor = 2 }
            modifier = { has_war = yes factor = 3 }
        }

        complete_effect = {
            FRA = { country_event = { id = austria_second_vienna_treaty.3 hours = 12 random_hours = 6 } }
        }
    }
    invite_spain = {
        icon = decision_generic_political_discourse

        cost = 100
        fire_only_once = yes
        available = { SPR = { has_government = monarchism is_in_faction = no } }

        ai_will_do = {
            base = 5
            modifier = { threat > 0.2 factor = 2 }
            modifier = { has_war = yes factor = 3 }
        }

        complete_effect = {
            SPR = { country_event = { id = austria_second_vienna_treaty.6 hours = 12 random_hours = 6 } }
        }
    }
}

dc_austro-slavism = {
    establish_the_kingdom_of_croatia = {
        cost = 200
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            news_event = { id = austria_austro_slavism.1 hours = 12 random_hours = 6 }

            add_stability = -0.15                   # The Hungarians really hate it.
            every_state = {
                limit = {
                    or = {
                        state = 103         # Croatia
                        state = 109
                        state = 163

                        state = 104         # Bosnia
                        state = 804
                    }
                }
                add_core_of = AUS
            }
        }
    }
    intensify_language_training = {
        icon = generic_prepare_civil_war

        cost = 100
        days_remove = 90

        ai_will_do = { base = 6 }

        modifier = {
            experience_gain_army_factor = 0.2
            coordination_bonus = 0.1
        }
    }
    appease_the_church = {
        cost = 75
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { has_war_support < 0.3 factor = 3 }
        }

        modifier = { war_support_weekly = 0.01 }
    }
}

dc_review_the_ausgleich_agreement = {
    address_magyarization = {
        icon = decision_generic_political_discourse

        cost = 150
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {             ## prioritize Hungary (+ 2 civs) or equal development (+ 1 civ, 1 mil, 1 airfield)
            country_event = { id = austria_review_ausgleich_agreement.1 hours = 2 }
        }        
    }
    invest_in_tatra = {
        icon = decision_generic_trucks

        cost = 100
        fire_only_once = yes
        available = { 74 = { is_owned_and_controlled_by = AUS } }

        ai_will_do = { base = 6 }

        complete_effect = {
            74 = {
                add_building_construction = {
                    type = arms_factory
                    level = 1
                    instant_build = yes
                }
            }
        }
    }
    develop_galician_oil = {
        icon = oil

        cost = 100
        fire_only_once = yes
        available = { 89 = { is_owned_and_controlled_by = AUS } }

        ai_will_do = { base = 6 }

        complete_effect = {
            add_resource = {
                type = oil
                amount = 24
                state = 89
            }
            if = {
                limit = { has_tech = synth_oil_0 }
                89 = {
                    add_building_construction = {
                        type = oil_refinery
                        level = 1
                        instant_build = yes
                    }
                }
            }
        }
    }
}