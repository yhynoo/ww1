SER_austrian_ultimatum = {
    SER_timeout = {
        days_mission_timeout = 28
        available = { 
            or = { 
                is_subject_of = AUS 
                AUS = { not = { can_declare_war_on = SER } } 
            } 
        }
        activation = { 
            AUS = { has_country_flag = assassination_crisis }
            or = { 
                tag = AUS   is_ally_of = AUS 
                tag = SER   is_ally_of = SER    has_guaranteed = SER
            }
        }

        timeout_effect = {
            if = {
                limit = { tag = SER }
                AUS = {
                    news_event = { id = austria.5 hours = 6 }
                    declare_war_on = { target = SER type = annex_everything }
                }
            }
        }
    }
    SER_accept_ultimatum = {
        allowed = { tag = SER }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            news_event = { id = austria.4 hours = 6 }
            AUS = { 
                puppet = SER 
                clr_country_flag = assassination_crisis
            }
        }
    }
}