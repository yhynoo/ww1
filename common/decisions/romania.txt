towards_liberation = {
    organize_the_liberation_alliance = {
        icon = generic_prepare_civil_war
        cost = 75
        fire_only_once = yes

        available = {
            is_subject = yes
            or = { GRE = { exists = yes } SER = { exists = yes } }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            every_country = {
                limit = { or = { tag = GRE tag = SER } exists = yes }
                country_event = { id = rom_alliance.1 hours = 6 }
            }
        }
    }
    order_french_aircraft = {
        icon = generic_air
        cost = 50
        fire_only_once = yes

        targets = { FRA }
        available = { 
            FROM = { 
                not = { has_war_with = ROOT }
                has_equipment = { fighter_equipment_0 > 11 }
            } 
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            FROM = { country_event = { id = rom_aircraft.1 hours = 6 } }
        }
    }
    demand_chisinau = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   {
            can_declare_war_on = FROM 
            any_state = { or = { state = 78 state = 442 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 78 state = 442 } is_owned_and_controlled_by = FROM } }
 
        ai_will_do = {
            factor = 0
            modifier = { and = { has_government = monarchism is_in_faction = yes } add = 6 }
        }

        complete_effect = { FROM = { country_event = { id = _demand.1 hours = 6 } } }
    }
}