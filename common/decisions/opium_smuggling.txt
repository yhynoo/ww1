dc_opium_smuggling = {
    opium_smuggling_ask_british = {
        cost = 75
        visible = {
            NOT = { has_country_flag = fra_belgian_crisis_alone }
        }
        available = {
            NOT = { 
                has_war_with = ENG
                has_war_with = HOL
            }
        }
        fire_only_once = yes

        complete_effect = {
            set_country_flag = flag_belgian_crisis_together
            ENG = { country_event = { id = opium_smuggling.2 hours = 12 random_hours = 6 } }
        }
    }
    opium_smuggling_do_on_your_own = {
        cost = 150
        visible = {
            NOT = { has_country_flag = flag_belgian_crisis_together }
        }
        available = {
            NOT = {
                has_war_with = HOL
            }
        }
        fire_only_once = yes

        complete_effect = {
            set_country_flag = fra_belgian_crisis_alone
            HOL = { country_event = { id = opium_smuggling.9 hours = 12 random_hours = 6 } }
        }
    }
}