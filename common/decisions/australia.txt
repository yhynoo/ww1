oceanian_dominion = {
    expand_naval_infrastructure_ast = {
        icon = generic_naval
        cost = 75
        fire_only_once = yes

        ai_will_do = { base = 12 }

        complete_effect = {
            random_owned_controlled_state = { 
                limit = { is_core_of = ROOT is_in_home_area = yes is_coastal = yes }

                add_building_construction = { type = dockyard level = 1 instant_build = yes }
                add_extra_state_shared_building_slots = 1
            }
            random_owned_controlled_state = { 
                limit = { is_core_of = ROOT is_in_home_area = yes is_coastal = yes }

                add_building_construction = { type = dockyard level = 1 instant_build = yes }
                add_extra_state_shared_building_slots = 1
            }
        }
    }
    expand_marine_troops = {
        icon = generic_military_support
        cost = 50
        fire_only_once = yes

        ai_will_do = { base = 12 }

        complete_effect = {
            army_experience = 50
            add_tech_bonus = {
				name = special_forces_bonus bonus = 1.0 uses = 1
				technology = marines_1
                technology = marines_2
			}

            hidden_effect = {
                add_ai_strategy = { type = role_ratio id = marines value = 10 }
            }
        }
    }
    demand_oceanian_islands = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   {
            can_declare_war_on = FROM 
            any_state = { or = { state = 635 state = 641 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 635 state = 641 } is_owned_and_controlled_by = FROM } }

        ai_will_do = { 
            factor = 0 
            modifier = { is_subject = no add = 6 }
        }

        complete_effect = { 
            every_state = {
                limit = { or = { state = 635 state = 641 }  is_owned_and_controlled_by = FROM }
                add_claim_by = ROOT
            }
            FROM = { country_event = { id = _demand.1 hours = 6 } } 
        }
    }
}
towards_the_equator = {
    support_india = {
        icon = generic_political_discourse
        cost = 75

        targets = { RAJ }
        available = { FROM = { is_subject = yes not = { has_war_with = ROOT } } }

        ai_will_do = { 
            base = 6
            modifier = { not = { is_ally_with = ENG } add = 3 }
        }

        complete_effect = { FROM = { news_event = { id = _autonomy.1 hours = 6 random_hours = 6 } } }
    }
    demand_east_indies = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   {
            can_declare_war_on = FROM 
            any_state = { or = { state = 222 state = 237 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 222 state = 237 } is_owned_and_controlled_by = FROM } }

        ai_will_do = { 
            factor = 0 
            modifier = { is_subject = no add = 6 }
        }

        complete_effect = { 
            every_state = {
                limit = { or = { state = 222 state = 237 }  is_owned_and_controlled_by = FROM }
                add_claim_by = ROOT
            }
            FROM = { country_event = { id = _demand.1 hours = 6 } } 
        }
    }
}