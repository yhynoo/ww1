warsaw_act = {
    establish_client_state = {
        icon = generic_form_nation
        visible = { FROM = { exists = no } }

        available = {
            custom_trigger_tooltip = {
                tooltip = tt_client_state_trigger
                FROM = { 
                    or = {
                        and = { tag = BLR   206 = { is_fully_controlled_by = ROOT } }
                        and = { tag = EST   13 = { is_fully_controlled_by = ROOT } }
                        and = { tag = LAT   12 = { is_fully_controlled_by = ROOT } }
                        and = { tag = LIT   11 = { is_fully_controlled_by = ROOT } }
                        and = { tag = POL   10 = { is_fully_controlled_by = ROOT } }
                        and = { tag = UKR   202 = { is_fully_controlled_by = ROOT } }
                    }
                }
            }
        }

        targets = { BLR EST LAT LIT POL UKR }
        target_non_existing = yes
        ai_will_do = { modifier = { always = yes add = 12 } }

        complete_effect = {
            custom_effect_tooltip = tt_client_state_effect
            hidden_effect = {
                FROM = {
                    every_state = {
                        limit = {
                            or = { is_core_of = PREV is_claimed_by = PREV }
                            and = {
                                is_fully_controlled_by = ROOT
                                not = { or = { is_core_of = ROOT is_claimed_by = ROOT } }
                            }
                        }
                        transfer_state_to = PREV
                    }
                    ROOT = { 
                        puppet = PREV
                        every_allied_country = {
                            every_controlled_state = { 
                                limit = { 
                                    or = { is_core_of = PREV.PREV.PREV is_claimed_by = PREV.PREV.PREV }
                                    not = { or = { is_core_of = PREV is_claimed_by = PREV } }
                                } 
                                transfer_state_to = PREV.PREV.PREV
                            }
                        }
                    }
                } 
            }           
        }
    }
}