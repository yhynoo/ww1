orthodox_unrest = {
    sov_demand_galicia = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger = {
            can_declare_war_on = FROM 
            any_state = { or = { state = 80 state = 89 state = 91 } is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 80 state = 89 state = 91 } is_owned_and_controlled_by = FROM } }
 
        available = { POL = { exists = no } }
        
        ai_will_do = {
            factor = 3
            modifier = { FROM = { surrender_progress > 0.05 } factor = 3 }
        }

        complete_effect = { FROM = { country_event = { id = _demand.1 hours = 6 } } }
    }
    support_armenia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 75

        targets = { ARM }
        target_non_existing = yes

        available = {
            FROM = { exists = no }
            354 = { owner = { controls_state = PREV has_stability < 0.5 } }
        }
        cancel_trigger = { 354 = { owner = { has_stability > 0.5 } } }

        days_remove = 90
        remove_effect = { 
            hidden_effect = {
                give_guarantee = FROM
                FROM = { news_event = { id = _uprising.1 hours = 6 } }
                354 = {
                    owner = {
                        armenian_independence = yes
                        declare_war_on = { target = FROM type = annex_everything }
                    }
                }
            }
            custom_effect_tooltip = tt_armenia
        }
    }
    support_romanian_independence = {
        icon = generic_political_discourse
        cost = 75

        targets = { ROM }
        available = { FROM = { is_subject = yes not = { has_war_with = ROOT } } }

        ai_will_do = { 
            factor = 3
            modifier = { date > 1912.12.1 factor = 3 }
        }

        complete_effect = { FROM = { news_event = { id = _autonomy.1 hours = 6 random_hours = 6 } } }
    }
}

far_east_affairs = {
    avenge_1905 = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger = {
            can_declare_war_on = FROM 
            72 = { is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { state = 72 } }

        ai_will_do = {
            factor = 2
            modifier = { FROM = { has_war = yes factor = 6 } }
        }

        complete_effect = { 
            FROM = { country_event = { id = _demand.1 hours = 6 } } 
        }
    }
    support_mongolia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 75

        targets = { MON }
        target_non_existing = yes

        available = {
            FROM = { exists = no }
            330 = { owner = { controls_state = PREV has_stability < 0.5 } }
        }
        cancel_trigger = { 330 = { owner = { has_stability > 0.5 } } }

        days_remove = 90
        remove_effect = { 
            hidden_effect = {
                give_guarantee = FROM
                FROM = { news_event = { id = _uprising.1 hours = 6 } }
                330 = {
                    owner = {
                        mongolian_independence = yes
                        declare_war_on = { target = FROM type = annex_everything }
                    }
                }
            }
            custom_effect_tooltip = tt_mongolia
        }
    }
}