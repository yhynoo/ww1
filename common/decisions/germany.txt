dc_oriental_investments = {
    establish_kaiserliche_eisenbahn = {
        icon = decision_generic_trains

        cost = 75
        fire_only_once = yes
        available = { TUR = { has_war = no } }

        ai_will_do = { base = 12 }

        complete_effect = {
            TUR = { country_event = { id = germany_oriental.1 hours = 12 random_hours = 6 } }        }
        }

    invest_arabian_oil = {
        icon = oil

        cost = 100
        fire_only_once = yes
        available = { 291 = { is_owned_by = TUR } }

        ai_will_do = { 
            base = 3
            modifier = { has_war = yes factor = 3 }
        }

        complete_effect = {
            TUR = { country_event = { id = germany_oriental.8 hours = 12 random_hours = 6 } }
        }
    }
}

dc_place_in_the_sun = {
    demand_south_africa = {
        icon = decision_generic_pickelhaube
        
        cost = 200
        fire_only_once = yes

        available = { 
            can_declare_war_on = ENG
            ENG = {
                owns_state = 542        # Bechuanaland

                owns_state = 275        # actual South Africa
                owns_state = 681
                owns_state = 719
            }
        }

        ai_will_do = { 
            base = 3
            modifier = { 
                or = { is_ally_with = AUS is_ally_with = MAN }
                factor = 3 
            }
        }

        complete_effect = {
            country_event = { id = germany_place_in_the_sun.1 hours = 2 }
        }
    }

    alliance_with_austria = {
        icon = decision_generic_political_discourse
        
        cost = 100
        fire_only_once = yes

        available = { 
            or = {
                is_faction_leader = yes
                is_in_faction = no
            }
            AUS = {
                exists = yes
                is_in_faction = no
                not = { has_war_with = GER }
            }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            AUS = { country_event = { id = germany_place_in_the_sun.9 hours = 12 random_hours = 6 } }
        }
    }

    askari_troops = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            # no event.
            add_ideas = sww1_idea_askari_troops
        }
    }
}

dc_look_to_the_east = {
    offer_qing_alliance = {
        icon = generic_prepare_civil_war
        cost = 150 
        available = { MAN = { is_in_faction = no } }

        ai_will_do = { 
            base = 3
            modifier = { has_war = yes factor = 6 }
        }

        complete_effect = {
            MAN = { country_event = { id = germany_look_to_the_east.1 hours = 12 random_hours = 6 } }
        }
    }
    invite_qing_to_share_research = {
        icon = generic_research
        cost = 100

        available = {
            MAN = { exists = yes }
        }

        ai_will_do = { base = 12 }

        complete_effect = {
            news_event = { id = germany_look_to_the_east.4 hours = 12 random_hours = 6 }
            add_to_tech_sharing_group = german_research_group
        }
    }
    develop_industry_in_east_asia = {
        icon = generic_construction
        cost = 75

        ai_will_do = { 
            base = 1
            modifier = { is_ally_with = MAN factor = 6 }
        }

        remove_days = 120
        modifier = {
            civilian_factory_use = 2
        }

        remove_effect = {
            MAN = {
                random_owned_controlled_state = {
                    add_extra_state_shared_building_slots = 1
                    add_building_construction = {
                        type = industrial_complex
                        level = 1
                        instant_build = yes
                    }
                }
            }
        }
    }
}

dc_heil_dir_im_siegerkranz = {
    develop_ersatzprodukte = {
        icon = generic_research
        cost = 75

        available = { has_completed_focus = GER_ersatz }
        ai_will_do = { base = 6 }

        remove_days = 90
        modifier = {
            war_support_weekly = -0.01
            consumer_goods_factor = -0.1
        }
    }

    encourage_vocational_training = {
        cost = 75
        fire_only_once = yes
        ai_will_do = { base = 6 }

        modifier = {
            industrial_capacity_factory = 0.1
        }
    }

    all_for_the_front = {
        cost = 100

        available = {
            surrender_progress > 0.2
            has_war_support > 0.5
        }

        remove_days = 90
        ai_will_do = { base = 12 }

        modifier = {
            stability_weekly = -0.01
            fuel_gain_factor = 0.1
            supply_factor = 0.1
        }
    }
}

dc_weimar_constitution = {
    women_in_workforce = {
        cost = 150
        fire_only_once = yes

        ai_will_do = { base = 12 }
        complete_effect = {
            add_ideas = sww1_idea_women_in_the_workforce
        }
    }

    establish_afrika_amt = {
        cost = 150
        fire_only_once = yes

        ai_will_do = { base = 9 }
        complete_effect = {
            news_event = { id = afrika_amt.1 hours = 12 random_hours = 6 }
            add_ideas = sww1_idea_afrika_amt
        }
    }
}
