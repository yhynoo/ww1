aftermath_of_antwerp = {
    support_transvaal = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            SAF = { exists = no }
            ENG = { has_stability < 0.5     controls_state = 275 }
        }
        cancel_trigger = { ENG = { has_stability > 0.5 } }

        ai_will_do = { 
            base = 3
            modifier = { 
                or = {
                    has_war_with = ENG
                    ENG = { has_idea = idea_mansion_house }
                } 
                factor = 2
            }
        }

        days_remove = 90
        complete_effect = { news_event = { id = holland_south_africa.1 hours = 6 } }

        remove_effect = { 
            custom_effect_tooltip = tt_transvaal
            hidden_effect = { 
                ENG = { news_event = { id = holland_south_africa.2 hours = 6 random_hours = 6 } }
            }
        }
    }
    invite_the_french = {
        cost = 75
        fire_only_once = yes
        icon = generic_form_nation

        available = {
            not = { has_war_with = FRA }
            controls_state = 6
        }

        ai_will_do = { 
            base = 3
            modifier = { has_completed_focus = HOL_traditionalism add = 9 }
        }

        complete_effect = {
            FRA = { country_event = { id = holland_withdraw.1 hours = 6 random_hours = 6 } }
        }
    }
    escalate_tensions = {
        icon = generic_prepare_civil_war
        cost = 50
        days_remove = 90

        ai_will_do = {
            base = 0
            modifier = { 
                and = { has_completed_focus = HOL_adjust_the_government not = { has_government = monarchism } } 
                add = 6
            }
        }

        modifier = {
            war_support_weekly = 0.01
            monarchism_drift = 0.05
        }

        complete_effect = {
            hidden_effect = {
                random = { chance = 25 news_event = { id = holland_dunkirk.1 random_days = 60 } }
            }
        }
    }
}

voc_mentaliteit = {
    support_transvaal = {
        icon = generic_prepare_civil_war
        cost = 100
        fire_only_once = yes

        available = {
            275 = { is_owned_and_controlled_by = ENG }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            ENG = {
                add_stability = -0.025
                hidden_effect = { country_event = { id = holland_south_africa.1 days = 7 random_hours = 6 } }
            }
        }
    }
    koninklijke_shell = {
        icon = oil

        cost = 75
        fire_only_once = yes

        available = {
            not = { has_war_with = TUR }

            TUR = {
                controls_state = 291
                has_resources_rights = { state = 291 resources = { oil } }
            }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            TUR = { country_event = { id = holland_shell.1 hours = 6 random_hours = 6 } }
        }
    }
    develop_den_helder = {
        icon = generic_construction
        fire_only_once = yes
        cost = 75

        ai_will_do = { base = 9 }

        available = { controls_state = 7 }
        complete_effect = {
            7 = {
                add_building_construction = { type = naval_base province = 3314 level = 2 instant_build = yes }
                add_building_construction = { type = dockyard level = 2 instant_build = yes }

                add_extra_state_shared_building_slots = 2
            }
        }
    }
}