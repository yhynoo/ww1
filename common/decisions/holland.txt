aftermath_of_antwerp = {
    invite_the_french = {
        cost = 75
        fire_only_once = yes
        icon = generic_form_nation

        available = {
            not = { has_war_with = FRA }
            controls_state = 6
        }

        ai_will_do = { 
            base = 3
            modifier = { has_completed_focus = HOL_traditionalism add = 9 }
        }

        complete_effect = {
            FRA = { country_event = { id = hol_antwerp.1 hours = 6 random_hours = 6 } }
        }
    }
    escalate_tensions = {
        icon = generic_prepare_civil_war
        cost = 50
        days_remove = 90

        ai_will_do = {
            base = 0
            modifier = { 
                and = { has_completed_focus = HOL_adjust_the_government not = { has_government = monarchism } } 
                add = 6
            }
        }

        modifier = {
            war_support_weekly = 0.01
            monarchism_drift = 0.05
        }

        complete_effect = {
            hidden_effect = {
                random = { chance = 50 news_event = { id = hol_dunkirk.1 random_days = 60 } }
            }
        }
    }
    seek_uk_guarantees = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        targets = { ENG }
        target_non_existing = yes

        available = {
            FROM = { exists = yes }
            and = { not = { has_war_with = FROM } not = { is_ally_with = FROM } }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { has_government = ROOT } add = 9 }
        }

        complete_effect = { FROM = { country_event = { id = _guarantees.1 hours = 6 random_hours = 6 } } }
    }
}

voc_mentaliteit = {
    support_transvaal = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 75

        targets = { SAF }
        target_non_existing = yes

        available = {
            FROM = { exists = no }
            275 = { owner = { controls_state = PREV has_stability < 0.5 } }
        }
        cancel_trigger = { 275 = { owner = { has_stability > 0.5 } } }

        days_remove = 90
        remove_effect = { 
            hidden_effect = {
                give_guarantee = FROM
                FROM = { news_event = { id = _uprising.1 hours = 6 } }
                275 = {
                    owner = {
                        south_african_independence = yes
                        declare_war_on = { target = FROM type = annex_everything }
                    }
                }
            }
            custom_effect_tooltip = tt_south_africa
        }
    }
    demand_east_frisia = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger = {
            can_declare_war_on = FROM 
            56 = { is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { state = 56 } }

        complete_effect = { 
            56 = { add_claim_by = ROOT }
            FROM = { country_event = { id = _demand.1 hours = 6 } } 
        }
    }
}