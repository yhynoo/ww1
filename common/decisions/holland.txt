aftermath_of_antwerp = {
    support_transvaal = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            SAF = { exists = no }
            ENG = { has_stability < 0.5     controls_state = 275 }
        }
        cancel_trigger = { ENG = { has_stability > 0.5 } }

        ai_will_do = { 
            base = 3
            modifier = { 
                or = { has_war_with = ENG   ENG = { has_idea = idea_mansion_house } } 
                factor = 2
            }
        }

        days_remove = 90
        complete_effect = { news_event = { id = hol_transvaal.1 hours = 6 } }

        remove_effect = { 
            hidden_effect = {
                HOL = { give_guarantee = SAF }
                SAF = { news_event = { id = _uprising.1 hours = 6 } }
                ENG = {
                    south_african_independence_independence = yes
                    declare_war_on = { target = SAF type = annex_everything }
                }
            }
            custom_effect_tooltip = tt_transvaal
        }
    }
    invite_the_french = {
        cost = 75
        fire_only_once = yes
        icon = generic_form_nation

        available = {
            not = { has_war_with = FRA }
            controls_state = 6
        }

        ai_will_do = { 
            base = 3
            modifier = { has_completed_focus = HOL_traditionalism add = 9 }
        }

        complete_effect = {
            FRA = { country_event = { id = hol_antwerp.1 hours = 6 random_hours = 6 } }
        }
    }
    escalate_tensions = {
        icon = generic_prepare_civil_war
        cost = 50
        days_remove = 90

        ai_will_do = {
            base = 0
            modifier = { 
                and = { has_completed_focus = HOL_adjust_the_government not = { has_government = monarchism } } 
                add = 6
            }
        }

        modifier = {
            war_support_weekly = 0.01
            monarchism_drift = 0.05
        }

        complete_effect = {
            hidden_effect = {
                random = { chance = 50 news_event = { id = hol_dunkirk.1 random_days = 60 } }
            }
        }
    }
}