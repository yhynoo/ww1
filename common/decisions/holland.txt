dc_aftermath_in_flanders = {
    invite_the_french = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { 
            base = 6
            modifier = { has_completed_focus = HOL_thorbeckes_legacy factor = 6 }
        }

        complete_effect = {
            FRA = { country_event = { id = holland_aftermath_in_flanders.1 hours = 12 random_hours = 6 } }
        }
    }
    escalate_tensions = {
        cost = 100
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { has_completed_focus = HOL_integrate_the_belgians factor = 3 }
        }

        modifier = {
            war_support_weekly = 0.005
            monarchism_drift = 0.05
        }
    }
}

dc_holland_alliances = {
    holland_england = {
        targets = { ENG }
        available = {
            is_in_faction = no
            not = { has_completed_focus = HOL_neutrality }

            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        ai_will_do = {
            base = 2
            modifier = { has_country_flag = we_stay_in_flanders factor = 6 }
        }

        cost = 75
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = holland_england.1 hours = 12 random_hours = 6 } }
        }
    }
    holland_germany = {
        targets = { GER }
        available = {
            is_in_faction = no
            not = { has_completed_focus = HOL_neutrality }

            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        ai_will_do = {
            base = 6

            modifier = { has_country_flag = we_stay_in_flanders factor = 6 }
            modifier = { has_completed_focus = HOL_thorbeckes_legacy factor = 0 }
        }

        cost = 75
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = holland_germany.1 hours = 12 random_hours = 6 } }
        }
    }
    holland_usa = {
        targets = { USA }
        available = {
            is_in_faction = no
            has_government = democratic
            
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
                has_completed_focus = USA_wilsons_fourteen_points
            }
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = holland_usa.1 hours = 12 random_hours = 6 } }
        }
    }
}

dc_world_trade_hegemony = {
    reorganize_indonesian_administration = {
        cost = 150
        fire_only_once = yes

        available = { 
            has_stability > 0.65
            any_state = {
                or = { state = 334 state = 335 state = 667 state = 668 state = 669 state = 672 state = 673 }
                is_fully_controlled_by = HOL
            }
        }

        every_state = {
            limit = {
                or = { state = 334 state = 335 state = 667 state = 668 state = 669 state = 672 state = 673 }
                is_fully_controlled_by = HOL
            }
            add_core_of = HOL
        }
    }
    solidarity_with_the_boers = {
        cost = 100
        fire_only_once = yes

        complete_effect = {
            news_event = { id = holland_world_trade_hegemony.1 hours = 12 random_hours = 6 }

            add_war_support = 0.1
            ENG = { add_opinion_modifier = { target = HOL modifier = medium_decrease } }

            ai_will_do = {
                base = 3
                modifier = { has_country_flag = we_stay_in_flanders factor = 3 }
            }

            hidden_effect = {
                set_country_flag = we_supported_boers
                ENG = { country_event = { id = england_third_boer_war.1 days = 28 random_days = 70 } }
            }
        }
    }
    koninklijke_shell = {
        icon = oil
        cost = 150
        fire_only_once = yes

        available = {
            412 = { is_owned_by = PER }
            413 = { is_owned_by = PER }
        }

        ai_will_do = { base = 10 }

        complete_effect = {
            PER = { country_event = { id = holland_world_trade_hegemony.2 hours = 12 random_hours = 6 } }
        }
    }
}

dc_politik_etis = {
    establish_the_volksraad = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            add_ideas = sww1_idea_volksraad_in_indonesia
        }
    }
    education_initiatives = {
        icon = generic_research
        cost = 75
        days_remove = 90

        ai_will_do = { base = 6 }

        modifier = {
            stability_weekly = 0.005
            research_speed_factor = 0.05
        }
    }
}

dc_depillarization = {
    establish_state_media = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            add_ideas = sww1_idea_dutch_state_media
        }
    }
    draft_the_historical_canon = {
        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 9 }

        complete_effect = {
            add_ideas = sww1_idea_inclusive_canon
        }

    }
    encourage_indonesian_immigration = {
        cost = 75
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { and = { has_stability > 0.5 has_manpower < 30000 } factor = 3 }
        }

        modifier = {
            stability_factor = -0.05
            weekly_manpower = 6000
        }
    }
}