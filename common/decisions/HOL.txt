after_the_intervention = {
    escalate_tensions = {
        visible = { has_country_flag = HOL_monarchist has_idea = idea_after_the_intervention }

        icon = generic_ignite_civil_war
        cost = 25

        days_remove = 90
        ai_will_do = { modifier = { has_country_flag = HOL_monarchist add = 6 } }

        modifier = {
            war_support_weekly = 0.005
            monarchism_drift = 0.05
        }

        targets = { HOL }
        ai_will_do = { modifier = { has_completed_focus = HOL_extend add = 9 } }
    }

    organize_local_administration = {
        visible = { has_country_flag = HOL_democratic controls_state = 6 }
        icon = generic_form_nation
        cost = 50

        days_remove = 90
        ai_will_do = { modifier = { has_country_flag = HOL_democratic add = 9 } }

        modifier = {
            political_power_factor = -0.15
            consumer_goods_factor = 0.1
        }

        remove_effect = {
            release_on_controlled = BEL
            hidden_effect = { 6 = { set_demilitarized_zone = no } }
        }
    }
    snijders_trial = {
        visible = { has_country_flag = HOL_democratic has_idea = idea_disobedient_army }

        icon = generic_civil_support
        cost = 50

        days_remove = 14
        available = { monarchism < 0.15 }
        ai_will_do = { modifier = { has_country_flag = HOL_democratic add = 12 } }

        remove_effect = {
            country_event = { id = HOL_trial.1 hours = 6 }
        }
    }
}
