echoes_of_agadir = {
    demand_alsace_flanders_wallonia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = { 
            any_state = {
                or = { state = 6 state = 28 state = 34 }
                not = { is_owned_by = FRA }
            }
        }

        ai_will_do = {
            base = 3
            modifier = {
                and = {
                    date > 1914.6.1 
                    and = { is_faction_leader = yes has_government = democratic }
                }
                add = 9
            }
            modifier = {
                has_government = monarchism
                add = 3
            }
        }

        complete_effect = {
            every_country = {
                limit = { or = { owns_state = 6 owns_state = 28 owns_state = 34 } }
                country_event = { id = _demand.1 hours = 6 random_hours = 6 }
            }
        }
    }
    service_aeronautique = {
        icon = generic_air
        fire_only_once = yes
        cost = 75

        ai_will_do = { base = 9 }

        complete_effect = {
            air_experience = 25

            add_tech_bonus = {
                name = air_bonus bonus = 1.0 uses = 1
                category = air_equipment_tech
            }
        }
    }
    expand_mining_atlas = {
        icon = generic_construction
        fire_only_once = yes
        cost = 75

        available = {
            any_state = {
                or = { state = 460 state = 461 }
                is_owned_and_controlled_by = FRA
            }
        }
        ai_will_do = { base = 6 }

        complete_effect = {
            if = {
                limit = { controls_state = 460 }
                460 = {
                    add_resource = { type = steel amount = 6 }
                    add_resource = { type = chromium amount = 2 }
                    add_building_construction = { type = infrastructure level = 1 instant_build = yes }
                }
            }

            if = {
                limit = { controls_state = 461 }
                461 = {
                    add_resource = { type = steel amount = 4 }
                    add_building_construction = { type = infrastructure level = 1 instant_build = yes }
                }
            }
        }
    }
}

look_to_the_near_east = {
    establish_arab_state = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 150

        available = {
            SYR = { exists = no }
            TUR = { has_stability < 0.5 }
        }
        cancel_trigger = { TUR = { has_stability > 0.5 } }

        ai_will_do = { 
            base = 6
            modifier = { has_war_with = TUR add = 6 }
        }

        days_remove = 90
        complete_effect = {
            news_event = { id = france_arab_state.1 hours = 6 random_hours = 6 }
            hidden_effect = { country_event = { id = france_teheran.1 days = 14 } }
        }

        remove_effect = { 
            custom_effect_tooltip = tt_arab_state
            hidden_effect = { 
                TUR = { news_event = { id = france_arab_state.2 hours = 6 random_hours = 6 } }
            }
        }
    }
    arm_the_greeks = {
        icon = generic_industry
        fire_only_once = yes
        cost = 100

        ai_will_do = { base = 9 }

        available = { 
            GRE = { exists = yes } 
            has_equipment = { infantry_equipment > 899 }
            has_equipment = { fighter_equipment > 11 }
        }

        complete_effect = {
            add_war_support = 0.05

            send_equipment = { equipment = infantry_equipment amount = 900 target = GRE }
            send_equipment = { equipment = fighter_equipment amount = 12 target = GRE }

            hidden_effect = { GRE = { news_event = { id = france_greece.1 hours = 6 } } }
        }
    }
}