echoes_of_agadir = {
    demand_flanders = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = { 
            can_declare_war_on = HOL
            any_state = {
                or = { is_core_of = FRA is_claimed_by = FRA }
                is_controlled_by = HOL
            }
        }

        ai_will_do = {
            base = 0
            modifier = { date > 1913.7.1 add = 9 }
        }

        complete_effect = {
            HOL = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    demand_wallonie = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = { 
            can_declare_war_on = GER
            any_state = {
                or = { is_core_of = FRA is_claimed_by = FRA }
                is_controlled_by = GER
            }
        }

        ai_will_do = {
            base = 3
            modifier = {
                and = {
                    date > 1914.6.1 
                    and = { is_faction_leader = yes has_government = democratic }
                }
                add = 9
            }
            modifier = {
                has_government = monarchism
                add = 3
            }
        }

        complete_effect = {
            GER = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
            
            # the 'opportunity' event
            hidden_effect = {
                GER = { country_event = { id = france_wallonia.1 days = 14 } }
            }
        }
    }
    expand_mining_atlas = {
        icon = generic_construction
        fire_only_once = yes
        cost = 75

        available = {
            any_state = {
                or = { state = 460 state = 461 }
                is_owned_and_controlled_by = FRA
            }
        }
        ai_will_do = { base = 9 }

        complete_effect = {
            if = {
                limit = { controls_state = 460 }
                460 = {
                    add_resource = { type = steel amount = 4 }
                    add_building_construction = { type = infrastructure level = 1 instant_build = yes }
                }
            }

            if = {
                limit = { controls_state = 461 }
                461 = {
                    add_resource = { type = steel amount = 4 }
                    add_building_construction = { type = infrastructure level = 1 instant_build = yes }
                }
            }

            if = {
                limit = { has_idea = idea_unrest_in_morocco }
                remove_ideas = idea_unrest_in_morocco
            }
        }
    }
}

mediterranean_hegemony = {
    establish_two_sicilies = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 150

        days_remove = 90
        war_with_on_remove = ITA

        available = {
            TTS = { exists = no }
            
            can_declare_war_on = ITA
            has_war_support > 0.5
        }

        ai_will_do = { 
            base = 9 
            modifier = { has_war = no }
        }

        complete_effect = {
            news_event = { id = france_sicily.1 hours = 6 random_hours = 6 }
        }

        remove_effect = {
            hidden_effect = { sicilian_independence = yes }
            give_guarantee = TTS

            hidden_effect = {
                TTS = { 
                    set_popularities = { monarchism = 100 }
                    set_politics = { ruling_party = monarchism elections_allowed = no }
                }
                every_country_with_original_tag = {
                    original_tag_to_check = ITA
                    declare_war_on = { target = TTS type = annex_everything }
                }
            }
        }
    }
    liberate_greeks = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 100

        ai_will_do = { base = 6 }

        available = {
            GRE = { exists = no }
            TUR = { owns_state = 47 owns_state = 186 }
        }

        complete_effect = {
            TUR = {
                country_event = { id = france_greece.1 hours = 6 random_hours = 6 }
            }
        }
    }
}

let_them_eat_cake = {
    develop_algeria = {
        icon = generic_naval
        fire_only_once = yes
        cost = 75

        available = {
            459 = { is_owned_and_controlled_by = FRA }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            add_stability = 0.025
        
            459 = { 
                add_building_construction = { type = naval_base level = 1 province = 1145 instant_build = yes } 
                add_building_construction = { type = dockyard level = 1 instant_build = yes }
            }
        }
    }
    appeal_to_the_church = {
        icon = generic_civil_support
        days_remove = 90
        cost = 50

        available = {
            has_government = monarchism
        }

        modifier = {
            stability_weekly = 0.005
            consumer_goods_factor = 0.05
        }
    }
}