# The Belgian Crisis

dc_belgian_crisis = {
    demand_flanders = {
        targets = { HOL }

        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 6 state = 34 }
                is_owned_by = HOL
            }
        }

        ai_will_do = {
            base = 12
            modifier = { date > 1913.7.1 factor = 3 }
        }

        complete_effect = {
            FROM = { country_event = { id = france_belgian_crisis.1 hours = 6 random_hours = 3 } }
        }
    }

    demand_wallonia = {
        targets = { GER }

        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 6 state = 34 }
                is_owned_by = GER
            }
        }

        ai_will_do = {
            base = 2
            modifier = { date > 1914.7.1 factor = 6 }
            modifier = { is_in_faction = yes factor = 2 }
        }

        complete_effect = {
            FROM = { country_event = { id = france_belgian_crisis.4 hours = 6 random_hours = 3 } }
        }
    }

    annex_belgium = {
        cost = 50
        fire_only_once = yes

        available = {
            6 = { is_controlled_by = FRA }
            34 = { is_controlled_by = FRA }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            every_state = {
                limit = { or = { state = 6 state = 34 } }
                add_core_of = FRA
            }

            if = {
                limit = { has_idea = sww1_idea_belgian_crisis }
                remove_ideas = sww1_idea_belgian_crisis
            }
        }
    }
}

# Alliances
dc_france_alliances = {
    france_colombia = {
        targets = { COL }
        available = { 
            FROM = { is_in_faction = no is_subject = no }
            has_completed_focus = FRA_across_the_atlantic
        }

        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 9
            modifier = { has_government = democratic factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = x_alliance.2 hours = 12 random_hours = 6 } }
        }
    }
    france_england = {
        targets = { ENG }
        available = {
            is_in_faction = no
            FROM = { OR = { is_in_faction = no is_faction_leader = yes } }
        }

        ai_will_do = {
            base = 6
            modifier = { has_government = monarchism factor = 0 }
        }

        cost = 75
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = x_alliance.1 hours = 12 random_hours = 6 } }
        }
    }
    france_russia = {
        targets = { SOV }
        available = { FROM = { is_in_faction = no is_subject = no } }

        cost = 75
        fire_only_once = yes

        ai_will_do = {
            base = 2
            modifier = { has_war = yes factor = 6 }
        }

        complete_effect = {
            FROM = { country_event = { id = x_alliance.2 hours = 12 random_hours = 6 } }
        }
    }
}

# Across the Atlantic

dc_across_the_atlantic = {
    develop_guyana = {
        icon = generic_construction

        cost = 75
        fire_only_once = yes

        available = {
            310 = { is_owned_and_controlled_by = FRA }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            310 = {
                add_resource = { type = aluminium amount = 24 }
                add_extra_state_shared_building_slots = 2
            }
        }
    }
    challenge_us_in_carribean = {
        cost = 100
        fire_only_once = yes

        available = {
            any_state = {
                or = { state = 315 state = 318 state = 319 }
                is_owned_by = USA
            }
        }

        ai_will_do = {
            base = 3
            modifier = { and = { has_government = monarchism has_war = no } factor = 3 }
        }

        complete_effect = {
            news_event = { id = france_across_the_atlantic.1 hours = 6 random_hours = 3 }

            every_state = {
                limit = { 
                    or = { state = 315 state = 318 state = 319 }
                    is_owned_by = USA
                }
                add_claim_by = FRA
            }
        }
    }
}

# They Shall Not Pass is generic

dc_let_them_eat_cake = {
    seek_unification_with_spain = {
        cost = 150
        fire_only_once = yes

        available = {
            has_government = monarchism
            SPR = { exists = yes has_government = monarchism }
        }

        ai_will_do = {
            base = 3
            modifier = { or = { is_in_faction = no is_faction_leader = yes } factor = 3 }
        }

        complete_effect = {
            # event
        }
    }
    international_trade_missions = {
        cost = 75
        fire_only_once = yes

        complete_effect = { add_ideas = sww1_idea_international_trade_missions }
    }
}

