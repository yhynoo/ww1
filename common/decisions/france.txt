echoes_of_agadir = {
    expand_mining_atlas = {
        icon = steel
        fire_only_once = yes
        cost = 75

        available = {
            controls_state = 460
            controls_state = 461
        }
        ai_will_do = { factor = 6 }

        complete_effect = {
            460 = {
                add_resource = { type = steel amount = 8 }
                add_resource = { type = tungsten amount = 2 }
                add_compliance = 6
            }
            461 = {
                add_resource = { type = steel amount = 4 }
                add_compliance = 12
            }
        }
    }
    service_aeronautique = {
        icon = generic_air
        fire_only_once = yes
        cost = 50

        ai_will_do = { factor = 9 }

        complete_effect = {
            air_experience = 25
            add_equipment_production = {
                equipment = { type = fighter_equipment_0 }   requested_factories = 2     efficiency = 0.65 
            }
            every_state = {
                limit = { or = { state = 18 state = 649 } }
                add_building_construction = { type = air_base level = 1 instant_build = yes } 
                add_building_construction = { type = arms_factory level = 1 instant_build = yes } 
            }
        }
    }
    ask_them_withdraw = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger =   { 
            can_declare_war_on = FROM
            any_state = { or = { state = 28 state = 34 }  is_owned_and_controlled_by = FROM } 
        }
        highlight_states = { highlight_states_trigger = { or = { state = 28 state = 34 }  is_owned_and_controlled_by = FROM } }

        ai_will_do = {
            factor = 2
            modifier = {
                and = { date > 1914.7.1 or = { is_ally_with = ENG is_ally_with = SOV } }
                factor = 6
            }
        }

        complete_effect = { FROM = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } } }
    }
}

look_to_the_near_east = {
    establish_arab_state = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            SYR = { exists = no }
            TUR = { has_stability < 0.5     controls_state = 554 }
        }
        cancel_trigger = { TUR = { has_stability > 0.5 } }

        ai_will_do = { 
            factor = 3
            modifier = { 
                and = { has_war_with = TUR  date > 1917.1.1 } 
                add = 6 
            }
        }
        days_remove = 90

        remove_effect = { 
            hidden_effect = {
                FRA = { give_guarantee = SYR }
                SYR = { news_event = { id = _uprising.1 hours = 6 } }
                TUR = {
                    syrian_independence = yes
                    declare_war_on = { target = SYR type = annex_everything }
                }
            }
            custom_effect_tooltip = tt_arab_state
        }
    }
    arm_the_greeks = {
        icon = generic_industry
        fire_only_once = yes
        cost = 75

        targets = { GRE }
        target_non_existing = yes

        available = { 
            FROM = { exists = yes } 
            has_equipment = { infantry_equipment > 899 }
        }
        ai_will_do = { factor = 9 }

        complete_effect = {
            send_equipment = { equipment = infantry_equipment amount = 900 target = FROM }
            hidden_effect = { FROM = { news_event = { id = _equipment.1 hours = 6 } } }
        }
    }
}