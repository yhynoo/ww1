dc_established_guarantees = {
    rus_approach_france = {
        available = {
            is_in_faction = no
            FRA = {
                OR = {
                    is_in_faction = no
                    is_faction_leader = yes
                }
            }
        }
        fire_only_once = yes

        cost = 50
        complete_effect = {
            FRA = { country_event = { id = established_guarantees.1 hours = 12 random_hours = 6 }}
        }
    }
    rus_approach_entente = {
        available = {
            is_in_faction = no
            ENG = { is_faction_leader = yes }
        }
        fire_only_once = yes

        cost = 50
        complete_effect = {
            ENG = { country_event = { id = established_guarantees.1 hours = 12 random_hours = 6 } }
        }
    }
}