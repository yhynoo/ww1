eng_mediterranean_expansion = {
    malta_cyprus = {
        icon = generic_naval
        cost = 100
        fire_only_once = yes

        available = {
            116 = { is_owned_and_controlled_by = ENG }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            add_tech_bonus = {
				name = capital_ships_bonus bonus = 0.5 uses = 2
				category = naval_equipment_tech
			}

            116 = { add_building_construction = { type = naval_base level = 2 province = 12003 instant_build = yes } }
        }
    }
    jaffa_unrest = {
        icon = generic_prepare_civil_war
        cost = 100
        fire_only_once = yes

        available = {
            TUR = { owns_state = 454 }
        }

        ai_will_do = {
            base = 0
            modifier = { has_war_with = TUR add = 12 }
        }

        complete_effect = {
            news_event = { id = england_jaffa.1 hours = 3 }
            TUR = {
                add_stability = -0.025
                hidden_effect = { country_event = { id = england_jaffa.2 days = 30 random_days = 30 } }
            }
        }
    }
}

change_alliances = {
    establish_arabia = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 150

        days_remove = 90
        available = {
            YEM = { exists = no }
            can_declare_war_on = TUR
        }

        ai_will_do = { 
            base = 9 
            modifier = { has_war = no }
        }

        complete_effect = {
            news_event = { id = england_arabia.1 hours = 6 random_hours = 6 }
        }

        remove_effect = {
            hidden_effect = { arab_independence = yes }
            puppet = YEM

            hidden_effect = {
                YEM = {
                    set_cosmetic_tag = YEM_arab
                    set_popularities = { monarchism = 100 }
                    set_politics = { ruling_party = monarchism elections_allowed = no }
                }
                every_country_with_original_tag = {
                    original_tag_to_check = TUR
                    declare_war_on = { target = YEM type = annex_everything }
                }
            }
        }
    }
    demand_east_indies = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            any_state = {
                or = { state = 289 state = 614 state = 286 state = 670 state = 671 }
                owner = { not = { tag = ENG } }
            }
        }

        ai_will_do = {
            base = 3
            modifier = { RAJ = { is_subject_of = ENG } factor = 3 }
        }

        complete_effect = {
            every_state = {
                limit = { or = { state = 289 state = 614 state = 286 state = 670 state = 671 } }
                add_claim_by = ENG
            }

            every_country = {
                limit = {
                    or = { controls_state = 289 controls_state = 614 controls_state = 286 controls_state = 670 controls_state = 671 }
                    not = { has_war_with = ENG }
                }
                country_event = { id = _demand.1 hours = 6 random_hours = 6 }
            }
        }
    }
    eng_align_afghanistan = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 75

        available = {
            can_declare_war_on = AFG
            AFG = { exists = yes is_subject = no }
        }

        ai_will_do = {
            base = 0
            modifier = { AFG = { is_in_faction = no } add = 9 }
        }

        complete_effect = {
            diplomatic_relation = { country = AFG relation = guarantee active = no }
            AFG = { country_event = { id = _align.1 hours = 6 random_hours = 6 } }
        }
    }
}

peoples_act = {
    release_saf = {
        icon = generic_form_nation
        cost = 100
        fire_only_once = yes

        available = {
            SAF = { exists = no }
            any_state = { is_core_of = SAF is_controlled_by = ENG }
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            add_stability = 0.05
            release_puppet_on_controlled = SAF
            
            hidden_effect = { news_event = { id = england_south_africa.1 hours = 6 random_hours = 6 } }
        }
    }
    wage_adjustments = {
        cost = 75
        days_remove = 90

        ai_will_do = { base = 6 }

        modifier = {
            production_factory_efficiency_gain_factor = 0.25
            stability_weekly = 0.005
        }
    }
}