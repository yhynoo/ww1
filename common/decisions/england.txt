dc_dominate_the_mediterranean = {
    develop_malta_and_cyprus = {
        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 6
            modifier = { has_war_with = TUR factor = 2 }
        }

        complete_effect = {
            116 = {
                add_building_construction = {
                    type = naval_base
                    level = 3
                    instant_build = yes
                }
            }
            183 = {
                add_building_construction = {
                    type = naval_base
                    level = 3
                    instant_build = yes
                }
            }
        }
    }
    balfour_declaration = {
        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 1
            modifier = { has_war_with = TUR factor = 9 }
        }

        complete_effect = {
            news_event = { id = england_dominate_the_mediterranean.1 hours = 12 random_hours = 6 }
            TUR = {
                add_opinion_modifier = {
                    target = ENG
                    modifier = medium_decrease
                }
            }
            hidden_effect = {
                if = {
                    limit = {
                        not = { 
                            has_idea = sww1_idea_looming_arab_revolt
                            has_country_flag = arab_revolt_done
                        }
                        TUR = { add_ideas = sww1_idea_looming_arab_revolt }
                    }
                }
                TUR = { news_event = { id = arabs_events.2 days = 70 random_days = 70 } }
            }
        }
    }
}

dc_affirm_the_entente = {
    announce_containment_policies = {
        icon = decision_generic_political_discourse

        cost = 50
        fire_only_once = yes

        ai_will_do = { 
            base = 9
            modifier = { has_completed_focus = ENG_change_the_alliances factor = 0 }
        }

        complete_effect = {
            news_event = { id = britain_affirm_the_entente.1 hours = 12 random_hours = 6 }
            hidden_effect = { set_country_flag = announced_containment }
        }
    }
    invite_to_entente = {
        cost = 100
        fire_only_once = yes

        available = { 
            or = { is_faction_leader = yes is_in_faction = no }
        }

        targets = { FRA JAP }
        target_trigger = { FROM = { is_in_faction = no } }

        ai_will_do = { 
            base = 6
            modifier = {
                or = { has_war = yes threat > 0.1 }
                factor = 2
            }
            modifier = { has_completed_focus = ENG_change_the_alliances factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = britain_affirm_the_entente.2 hours = 12 random_hours = 6 } }
        }
    }
}

dc_change_the_alliances = {
    divide_influence_in_the_far_east = {
        cost = 150
        fire_only_once = yes
        available = { not = { has_war_with = GER } }

        ai_will_do = { 
            base = 3
            modifier = { 
                or = { 
                    FRA = { is_faction_leader = yes } 
                    AUS = { is_faction_leader = yes }
                } 
                factor = 3
            }
        }

        complete_effect = {
            news_event = { id = britain_change_the_alliances.1 hours = 12 random_hours = 6 }
            if = {
                limit = { is_ai = yes }
                dismantle_faction = yes
            }
        }
    }
    allied_by_blood = {
        cost = 200
        fire_only_once = yes
        available = { 
            is_in_faction = no
            GER = { not = { has_war_with = ENG } }
        }

        ai_will_do = {
            base = 9
            modifier = { FRA = { is_ally_with = AUS } factor = 2 }
        }

        complete_effect = {
            GER = { country_event = { id = britain_change_the_alliances.2 hours = 12 random_hours = 6 } }
        }
    }
}

dc_your_country_needs_you = {
    establish_ministry_of_munitions = {
        icon = generic_industry

        cost = 150
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            add_ideas = sww1_idea_ministry_of_munitions
        }
    }
    draft_encouragement = {
        cost = 100
        days_remove = 90

        ai_will_do = { 
            base = 3
            modifier = { has_manpower < 100000 factor = 3 }
        }

        modifier = {
            mobilization_speed = 0.1
            conscription = 0.005
        }
    }
    dilution_of_labor = {
        icon = generic_industry
        
        cost = 75
        days_remove = 90

        ai_will_do = { base = 6 }

        modifier = {
            production_speed_industrial_complex_factor = 0.1
            production_speed_arms_factory_factor = 0.1
        }
    }
}

dc_announce_the_commons_act = {
    let_workers_own_the_industry = {
        cost = 150
        fire_only_once = yes
        available = { communism > 0.25 }

        ai_will_do = { base = 9 }

        complete_effect = {
            add_ideas = sww1_idea_syndicalist_economy
        }
    }

    encourage_efficiency_improvements = {
        cost = 100
        days_remove = 90

        ai_will_do = { base = 9 }

        modifier = {
            industrial_capacity_dockyard = 0.1
            industrial_capacity_factory = 0.1
        }
    }

    empower_the_unions = {
        cost = 75
        fire_only_once = yes
        available = { communism > 0.45 }

        ai_will_do = { base = 9 }

        complete_effect = {
            add_stability = 0.1
            hidden_effect = {
                country_event = { id = britain_commons_act.1 days = 7 random_days = 7 }
            }
        }
    }
}

dc_end_imperialism = {
    grant_india_independence = {
        visible = {
            RAJ = { is_subject_of = ENG }
        }

        cost = 200
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            news_event = { id = england_end_imperialism.1 hours = 12 random_hours = 6 }
            set_autonomy = {
                target = RAJ
                autonomous_state = autonomy_free
            }

            hidden_effect = {
                RAJ = { drop_cosmetic_tag = yes }
            }
        }
    }
    grant_australia_independence = {
        visible = {
            AST = { is_subject_of = ENG }
        }

        cost = 150
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            news_event = { id = england_end_imperialism.2 hours = 12 random_hours = 6 }
            set_autonomy = {
                target = AST
                autonomous_state = autonomy_free
            }

            hidden_effect = {
                AST = { drop_cosmetic_tag = yes }
            }
        }
    }
    grant_south_africa_independence = {
        visible = {
            SAF = { exists = no }
            any_state = {
                is_owned_by = ENG
                is_core_of = SAF
            }
        }

        cost = 100
        fire_only_once = yes

        ai_will_do = { base = 6 }

        complete_effect = {
            news_event = { id = england_end_imperialism.3 hours = 12 random_hours = 6 }
            release = SAF
        }
    } 
}