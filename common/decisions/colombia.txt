general_mobilization = {
    expand_oil_fields = {
        icon = oil
        cost = 75
        fire_only_once = yes

        available = { controls_state = 307 }
        complete_effect = {
            307 = { add_resource = { type = oil amount = 24 } }
        }
    }
    demand_andes = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            can_declare_war_on = PRU
            any_state = { is_claimed_by = COL is_owned_by = PRU }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            PRU = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
}

challenge_usa = {
    demand_panama_canal = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            can_declare_war_on = USA
            any_state = {
                or = { state = 304 state = 685 }
                is_owned_by = USA
            }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            USA = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    annex_mexico = {
        icon = generic_form_nation
        fire_only_once = yes
        cost = 150

        ai_will_do = { base = 9 }

        available = {
            has_stability > 0.5

            any_controlled_state = {
                is_core_of = MEX
                not = { is_core_of = COL }
            }
        }

        complete_effect = {
            every_controlled_state = {
                limit = { is_core_of = MEX }
                add_core_of = COL
            }

            USA = { create_wargoal = { target = COL type = liberate_wargoal } }
            news_event = { id = colombia_annex.1 hours = 6 random_hours = 6 }
        }
    }
}