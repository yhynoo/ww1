
default_unification = {
    announce_unification = {
        icon = generic_nationalism

        available = { 
            check_whether_unified = yes
        }

        ai_will_do = { modifier = { always = yes add = 24 } }

        complete_effect = {
            set_country_flag = assumed_legacy
            mark_focus_tree_layout_dirty = yes
        }
    }
    demand_submit = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 50

        targets_dynamic = yes
        target_trigger = {
            FROM = {
                not = { has_war_with = ROOT }
                check_unification_targets = yes
            }
        }

        ai_will_do = {
            modifier = { has_war = no add = 2 }
            modifier = { strength_ratio = { tag = FROM ratio > 2 } add = 3.5 } 
            modifier = {
                FROM = { 
                    or = {
                        is_subject = no
                        and = { is_subject = yes any_country = { has_subject = FROM has_war = yes } }
                    } 
                    factor = 2 
                }
            }
        }

        complete_effect = {
            FROM = {
                country_event = { id = unification.2 hours = 6 }
                effect_overlord_warning_country = yes
            }
        }
    }
}