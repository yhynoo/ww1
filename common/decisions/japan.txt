dc_hong_kong_appeal = {
    invite_naval_advisors_from_uk = {
        cost = 50
        fire_only_once = yes

        available = {
            ENG = { exists = yes }
            or = {
                is_guaranteed_by = ENG
                is_ally_with = ENG
            }
        }

        ai_will_do = {
            base = 3
            modifier = { has_completed_focus = JAP_navy factor = 3 }
        }

        complete_effect = {
            navy_experience = 100
            ENG = {
                add_opinion_modifier = {
                    target = JAP
                    modifier = medium_increase
                }
            }

            hidden_effect = {
                set_country_flag = invited_uk_experts
            }
        }
    }
    demand_tsingtao = {
        cost = 125
        fire_only_once = yes

        available = { GER = { owns_state = 743 } }

        ai_will_do = {
            base = 2
            modifier = { is_ally_with = ENG factor = 6 }
            modifier = {
                and = { date > 1915.7.1 has_war = no }
                factor = 3
            }
        }

        complete_effect = {
            GER = { country_event = { id = japan_events.4 hours = 12 random_hours = 6 } }
        }
    }
}

dc_japan_alliances = {
    japan_england = {
        targets = { ENG }
        available = {
            is_in_faction = no
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
            }
        }

        cost = 75
        fire_only_once = yes

        ai_will_do = {
            base = 6
            modifier = { ENG = { has_war_with = GER } factor = 2 }
            modifier = { ENG = { has_opinion = { target = JAP value < 25 } } factor = 0 }
        }

        complete_effect = {
            FROM = { country_event = { id = japan_england.1 hours = 12 random_hours = 6 } }
            hidden_effect = { set_country_flag = stop_influencing_uk }
        }
    }
    japan_india = {
        targets = { RAJ }
        available = { 
            FROM = { is_in_faction = no is_subject = no }
            has_completed_focus = JAP_twenty-one_demands
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = japan_india.1 hours = 12 random_hours = 6 } }
        }
    }
    japan_usa = {
        targets = { USA }
        available = {
            is_in_faction = no
            has_government = democratic
            
            FROM = { 
                OR = { is_in_faction = no is_faction_leader = yes }
                has_completed_focus = USA_wilsons_fourteen_points
            }
        }

        cost = 100
        fire_only_once = yes

        complete_effect = {
            FROM = { country_event = { id = japan_usa.1 hours = 12 random_hours = 6 } }
        }
    }
}

dc_twenty-one_demands = {
    manchu_debt = {
        cost = 100
        fire_only_once = yes

        available = {
            can_declare_war_on = MAN
        }

        ai_will_do = {
            base = 2
            modifier = { is_ally_with = ENG factor = 3 }
            modifier = {
                and = { date > 1914.7.1 has_war = no }
                factor = 3
            }
        }

        complete_effect = {
            MAN = { country_event = { id = japan_events.7 hours = 12 random_hours = 6 } }
        }
    }
    advisors_to_qing = {
        cost = 150
        fire_only_once = yes

        available = { MAN = { is_subject = no } }

        ai_will_do = {
            base = 2
            modifier = {
                and = {
                    date > 1914.7.1
                    has_war = no
                }
                factor = 3
            }
            modifier = {
                strength_ratio = {
                    tag = MAN
                    ratio > 1
                }
                factor = 5
            }
        }

        complete_effect = {
            MAN = { country_event = { id = japan_events.10 hours = 12 random_hours = 6 } }
        }
    }
}

dc_embrace_american_industrialism = {
    open_experimental_facilities = {
        icon = generic_research

        cost = 100
        fire_only_once = yes

        complete_effect = {
            add_tech_bonus = {
				name = industrial_bonus
				bonus = 1.0
				uses = 2
				category = industry
			}
        }
    }
    increase_trade_exchange = {
        cost = 75
        days_remove = 90

        modifier = {
            trade_opinion_factor = 0.5
        }
    }
    challenge_the_syndicates = {
        cost = 75
        days_remove = 90

        modifier = {
            production_speed_infrastructure_factor = 0.15
            production_speed_industrial_complex_factor = 0.15
        }
    }
}

dc_constitution = {
    empower_agricultural_communities = {
        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 6
        }

        complete_effect = {
            add_ideas = sww1_idea_agricultural_communities
        }
    }
    introduce_peace_preservation_law = {
        cost = 100
        fire_only_once = yes

        ai_will_do = {
            base = 6
        }

        complete_effect = {
            add_ideas = sww1_idea_peace_preservation_law
        }
    }
}