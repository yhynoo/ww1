_head_of_state = {
    # war support
    territorial_auxilliaries = {
        visible = { or = { has_idea = volunteer_only has_idea = limited_conscription } }

        icon = generic_prepare_civil_war    
        days_remove = 90
        cost = 50
    
        available = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
        ai_will_do = { modifier = { or = { has_war_support < 0.6 has_manpower < 1000 } add = 6 } }

        modifier = {
            war_support_weekly = 0.005
            weekly_manpower = 600
        }
    }
    honor_the_heroes = {
        visible = { not = { or = { has_idea = volunteer_only has_idea = limited_conscription } } }

        icon = generic_nationalism    
        days_remove = 90
        cost = 25

        available = { has_war = yes }
        ai_will_do = { modifier = { has_war_support < 0.6 add = 6 } }

        modifier = {
            war_support_weekly = 0.005
            resistance_growth_on_our_occupied_states = 0.1
        }
    }

    # propaganda
    expand_information_services = {
        visible = { has_government = monarchism }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            modifier = {
                or = {
                    has_stability < 0.2
                    and = { 
                        has_government = monarchism monarchism < 0.6
                        not = { or = { has_idea = idea_communists_organize has_idea = idea_democrats_organize } }
                    }
                }
                add = 10
            }
        }
        modifier = { stability_weekly = 0.005 }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    support_liberal_journalists = {
        visible = { has_government = democratic }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            modifier = {
                or = {
                    has_stability < 0.2
                    and = { 
                        has_government = democratic democratic < 0.6
                        not = { or = { has_idea = idea_communists_organize has_idea = idea_monarchists_organize } }
                    }
                }
                add = 10
            }
        }
        modifier = { stability_weekly = 0.005 }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    intensify_agitation = {
        visible = { has_government = communism }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            modifier = {
                or = {
                    has_stability < 0.2
                    and = { 
                        has_government = communism communism < 0.6
                        not = { or = { has_idea = idea_democrats_organize has_idea = idea_monarchists_organize } }
                    }
                }
                add = 10
            }
        }
        modifier = { stability_weekly = 0.005 }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
}