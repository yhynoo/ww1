_head_of_state = {
    # war support
    territorial_auxilliaries = {
        visible = { 
            is_subject = no
            or = { has_idea = volunteer_only has_idea = limited_conscription }
        }

        icon = generic_prepare_civil_war    
        days_remove = 90
        cost = 50
    
        available = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
    
        ai_will_do = {
            factor = 3
            modifier = { and = { threat > 0.1 has_war_support < 0.5 } factor = 2 }
            modifier = { has_war_support > 0.8 factor = 0 }
        }

        modifier = {
            war_support_weekly = 0.005
            weekly_manpower = 600
        }
    }
    honor_the_heroes = {
        visible = { 
            is_subject = no
            not = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
        }

        icon = generic_nationalism    
        days_remove = 90
        cost = 25

        available = { has_war = yes }

        ai_will_do = {
            factor = 6
            modifier = { and = { threat > 0.1 has_war_support < 0.3 } add = 6 }
            modifier = { has_war_support > 0.8 factor = 0 }
        }

        modifier = {
            war_support_weekly = 0.005
            resistance_growth_on_our_occupied_states = 0.1
        }
    }

    # propaganda
    expand_information_services = {
        visible = { 
            is_subject = no
            has_government = monarchism
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            factor = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    support_liberal_journalists = {
        visible = { 
            is_subject = no
            has_government = democratic
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            factor = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    intensify_agitation = {
        visible = { 
            is_subject = no
            has_government = communism
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            factor = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }

    # mobilization
    announce_mobilization = {
        visible = { has_country_flag = mobilization_allowed }

        icon = generic_nationalism
        days_remove = 90
        cost = 25

        fire_only_once = yes
        ai_will_do = {
            factor = 0 
            modifier = { 
                or = { any_country = { has_wargoal_against = ROOT } has_war = yes }
                factor = 12 
            }
        }

        available = { not = { has_idea = volunteer_only } }

        modifier = {
            mobilization_speed = 0.25
            training_time_factor = -0.25
        }

        complete_effect = {
            add_named_threat = { name = "[ROOT.GetName] Announces Mobilization" threat = 2 }
            news_event = { id = _mobilization.1 hours = 6 }
        }
    }
    shorten_training = {
        visible = { has_country_flag = mobilization_allowed }

        icon = ger_military_buildup
        days_remove = 180
        cost = 25

        modifier = {
            minimum_training_level = -0.25
        }
    }
}