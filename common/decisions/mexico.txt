defense_abilities = {
    ultimatum_to_colombia = {
        icon = generic_prepare_civil_war
        cost = 100
        fire_only_once = yes

        available = {
            can_declare_war_on = COL
            or = {
                has_war_support > 0.5
                any_core_state = { is_owned_and_controlled_by = COL }
            }
        }

        ai_will_do = { base = 6 }

        complete_effect = {
            every_state = {
                limit = { or = { state = 313 state = 312 state = 314 state = 317 } }
                add_claim_by = MEX
            }
            COL = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    demand_belize = {
        icon = generic_prepare_civil_war
        cost = 75
        fire_only_once = yes

        available = {
            311 = { is_controlled_by = ENG }
            can_declare_war_on = ENG
        }

        ai_will_do = { base = 3 }

        complete_effect = {
            311 = { add_claim_by = MEX }
            ENG = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } }
        }
    }
    establish_mountain_troops = {
        icon = generic_prepare_civil_war
        cost = 50
        fire_only_once = yes

        ai_will_do = { base = 12 }

        complete_effect = {
            army_experience = 50
            add_tech_bonus = {
				name = special_forces_bonus bonus = 1.0 uses = 1
				technology = mountaineers_1
                technology = mountaineers_2
			}

            hidden_effect = {
                add_ai_strategy = { type = role_ratio id = mountaineers value = 10 }
            }
        }
    }
}