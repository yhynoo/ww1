_missing_territories = {

    # missing territories
    demand_missing_territories = {
        dynamic_targets = yes
        target_trigger = {
            FROM = {
                and = { 
                    has_idea = idea_war_loser 
                    not = { has_war_with = ROOT }
                }
                any_owned_state = {
                    is_core_of = ROOT
                }
            }
        }

        icon = generic_form_nation
        cost = 25

        fire_only_once = yes
        ai_will_do = { modifier = { always = yes add = 10000 } }

        complete_effect = {
            FROM = {
                every_owned_state = {
                    limit = { is_core_of = ROOT }
                    send_ultimatum_multiple_states = yes
                }
            }
        }
    }
}