eight_banners = {
    align_mongolia = {
        icon = generic_civil_support
        fire_only_once = yes
        cost = 75

        targets = { MON }
        available = {
            can_declare_war_on = FROM
            FROM = { exists = yes is_subject = no }
        }

        target_non_existing = yes
        ai_will_do = {
            factor = 0
            modifier = { FROM = { has_war = no is_in_faction = no } add = 9 }
        }

        complete_effect = { FROM = { country_event = { id = _align.1 hours = 6 random_hours = 6 } } }
    }
    demand_dalian = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        available = {
            72 = { 
                NOT = { is_owned_by = MAN } 
                controller = { can_declare_war_on = MAN }
            }
        }

        ai_will_do = {
            base = 0
            modifier = { any_country = { owns_state = 72 has_war = yes } add = 12 }
        }

        complete_effect = {
            72 = { controller = { country_event = { id = _demand.1 hours = 6 random_hours = 6 } } }
        }
    }
    develop_shanghai = {
        icon = generic_naval
        cost = 50
        fire_only_once = yes

        available = {
            controls_state = 613
        }

        ai_will_do = { base = 9 }

        complete_effect = {
            613 = {
                add_building_construction = { type = naval_base level = 2 province = 7014 instant_build = yes }
                add_building_construction = { type = dockyard level = 2 instant_build = yes }
            }
        }
    }
}

asia_for_asians = {
    american_guarantees = {
        icon = generic_prepare_civil_war
        fire_only_once = yes
        cost = 100

        targets = { USA }
        target_non_existing = yes

        available = {
            FROM = { exists = yes }
            and = { not = { has_war_with = FROM } not = { is_ally_with = FROM } }
        }

        ai_will_do = {
            factor = 0
            modifier = { FROM = { has_government = ROOT } add = 9 }
        }

        complete_effect = {
            FROM = { country_event = { id = _guarantees.1 hours = 6 random_hours = 6 } }
        }
    }
    withdraw_from_treaties = {
        icon = generic_ignite_civil_war
        fire_only_once = yes
        cost = 50

        ai_will_do = {
            factor = 0
            modifier = { is_in_faction = yes add = 12 }
        }

        complete_effect = { 
            add_named_threat = { name = "[MAN.GetName] Withdraws from Treaties" threat = 6 }
            remove_ideas = idea_unequal_treaties

            every_country = {
                limit = { or = { original_tag = ENG original_tag = FRA original_tag = GER original_tag = JAP original_tag = USA } }
                # event
            }
        }
    }
}