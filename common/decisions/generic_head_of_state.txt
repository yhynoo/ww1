dc_head_of_state_decisions = {
    # heroes
    honor_the_heroes = {
        visible = { 
            is_subject = no
            not = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
        }

        icon = generic_nationalism    
        days_remove = 90
        cost = 50

        available = { has_war = yes }

        ai_will_do = {
            base = 3
            modifier = { and = { threat > 0.1 has_war_support < 0.3 } factor = 2 }
            modifier = { has_war_support > 0.8 factor = 0 }
        }

        modifier = {
            war_support_weekly = 0.005
            resistance_growth_on_our_occupied_states = 0.1
        }
    }

    # war support
    territorial_auxilliaries = {
        visible = { 
            is_subject = no
            not = { has_government = communism } 
            or = { has_idea = volunteer_only has_idea = limited_conscription }
        }

        icon = generic_prepare_civil_war    
        days_remove = 90
        cost = 50
    
        available = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
    
        ai_will_do = {
            base = 3
            modifier = { and = { threat > 0.1 has_war_support < 0.3 } factor = 2 }
            modifier = { has_war_support > 0.8 factor = 0 }
        }

        modifier = {
            war_support_weekly = 0.005
            weekly_manpower = 300
        }
    }
    workers_militias = {
        visible = { 
            is_subject = no
            has_government = communism
            or = { has_idea = volunteer_only has_idea = limited_conscription }
        }

        icon = generic_prepare_civil_war    
        days_remove = 90
        cost = 50
    
        available = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
    
        ai_will_do = {
            base = 3
            modifier = { and = { threat > 0.1 has_war_support < 0.3 } factor = 2 }
        }

        modifier = {
            war_support_weekly = 0.005
            training_time_factor = -0.1
        }
    }

    # propaganda
    expand_information_services = {
        visible = { 
            is_subject = no
            or = { has_government = authoritarianism has_government = monarchism }
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    support_liberal_journalists = {
        visible = { 
            is_subject = no
            has_government = democratic
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }
    intensify_agitation = {
        visible = { 
            is_subject = no
            has_government = communism
        }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            stability_weekly = 0.005
        }

        remove_effect = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
        }
    }

    # puppet decisions
    empower_loyalists = {
        visible = { is_subject = yes }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = { 
            base = 6
            modifier = { has_idea = idea_underground_state factor = 0 }
        }

        modifier = {
            stability_weekly = 0.005
            master_ideology_drift = 0.05
        }
    }
    develop_independents = {
        visible = { 
            is_subject = yes
            not = { has_idea = idea_underground_state }
        }

        icon = generic_prepare_civil_war
        days_remove = 90
        cost = 50

        modifier = {
            stability_weekly = -0.005
        }

        ai_will_do = { 
            base = 3
            modifier = { OVERLORD = { has_stability < 0.5 } add = 6 }
        }

        remove_effect = {
            add_autonomy_score = { value = 100 }
            OVERLORD = { add_stability = -0.025 }

            random = {
                chance = 25
                add_ideas = idea_underground_state
            }
        }
    }
    launch_independence_war = {
        visible = { 
            is_subject = yes
            has_idea = idea_underground_state
        }

        icon = generic_form_nation
        fire_only_once = yes
        cost = 100

        ai_will_do = {
            base = 0
            modifier = { OVERLORD = { has_war = yes } add = 6 }
        }

        complete_effect = {
            OVERLORD = {
                hidden_effect = { set_autonomy = { target = ROOT autonomous_state = autonomy_free } }
                declare_war_on = { target = ROOT type = annex_everything }
            }
        }
    }
}