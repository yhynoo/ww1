dc_head_of_state_decisions = {
    # war support
    encourage_voluntary_draft = {
        icon = generic_prepare_civil_war    
        days_remove = 90
        cost = 50
    
        available = { or = { has_idea = volunteer_only has_idea = limited_conscription } }
    
        ai_will_do = {
            base = 3
            modifier = { and = { threat > 0.1 has_war_support < 0.3 } factor = 2 }
        }

        modifier = {
            war_support_weekly = 0.005
            weekly_manpower = 600
        }
    }

    # propaganda
    expand_state_information_auth = {
        visible = { has_government = authoritarianism }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            authoritarianism_drift = 0.05
            stability_weekly = 0.005
        }
    }
    expand_state_information_com = {
        visible = { has_government = communism }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            communism_drift = 0.05
            stability_weekly = 0.005
        }
    }
    expand_state_information_dem = {
        visible = { has_government = democratic }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            democratic_drift = 0.05
            stability_weekly = 0.005
        }
    }
    expand_state_information_mon = {
        visible = { has_government = monarchism }

        icon = generic_civil_support
        days_remove = 90
        cost = 50

        ai_will_do = {
            base = 3
            modifier = { has_stability < 0.5 factor = 2 }
        }

        modifier = {
            monarchism_drift = 0.05
            stability_weekly = 0.005
        }
    }    

    # not our war
    ask_for_armistice = {
        visible = { 
            has_country_flag = war_of_another_government
            has_civil_war = no
        }

        icon = generic_form_nation
        cost = 150
        days_re_enable = 90

        targets_dynamic = yes
        target_trigger = {
            FROM = {
                has_war_with = ROOT
                or = { is_faction_leader = yes is_in_faction = no }
            }
        }

        ai_will_do = { 
            base = 6
            modifier = { 
                or = { has_war_support < 0.5 surrender_progress > 0.1 }
                factor = 2 
            }
        }

        days_remove = 7
        remove_effect = {
            FROM = { country_event = { id = x_civil_war.1 hours = 6 random_hours = 6 } }
        }
    }
}