# war weariness
war_weariness = {
    war_weariness_mission = {
        icon = generic_army_support
        days_mission_timeout = 180
        is_good = no

        available = {
            has_stability > 0.5
        }

        activation = { has_idea = idea_war_weariness }
        complete_effect = { remove_ideas = idea_war_weariness }
        timeout_effect = { news_event = x_war_weariness.2 }
    }
    decorate_veterans = {
        icon = generic_prepare_civil_war
        cost = 25

        days_remove = 30

        modifier = {
            initiative_factor = 0.05
        }

        ai_will_do = {
            base = 6
            modifier = { has_stability > 0.4 factor = 2 }
        }

        complete_effect = { add_stability = 0.05 }
    }
    postcards_from_the_trenches = {
        icon = border_war
        cost = 50
        days_remove = 30

        modifier = {
            supply_consumption_factor = -0.05
            army_morale_factor = 0.1
        }

        ai_will_do = { base = 9 }

        complete_effect = { add_stability = 0.05 }      
    }
}