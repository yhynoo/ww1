focus_tree = {
    id = "FER_focus"
    country = {
        factor = 0
        modifier = { add = 20   original_tag = FER }
    }

    # army
    shared_focus = XXX_address_troops
    shared_focus = XXX_general_staff

    # development
    shared_focus = XXX_land_use
    shared_focus = XXX_infrastructure

    # special
    shared_focus = XXX_civil_war

    continuous_focus_position = { x = 88 y = 770 }

# state
    focus = {
        id = FER_government
        icon = GFX_focus_FER_government

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = { factor = 2 }

        x = 0 y = 0
        cost = 10

        completion_reward = {
            add_political_power = 50
        }
    }

    focus = {
        id = FER_chokepoints
        icon = GFX_focus_FER_chokepoints
        prerequisite = { focus = FER_government }

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = { modifier = { has_war = yes factor = 0 } }        # don't start new wars.

        relative_position_id = FER_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            add_equipment_to_stockpile = { type = train_equipment_1   amount = 10     producer = FER }
        }
    }

    focus = {
        id = FER_mukden
        icon = GFX_focus_generic_extend
        prerequisite = { focus = FER_chokepoints }

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = { modifier = { has_war = yes factor = 0 } }        # don't start new wars.

        relative_position_id = FER_chokepoints
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { or = { state = 72 state = 610 state = 619 } }
                add_claim_by = ROOT
            }
        }
    }

    focus = {
        id = FER_expand_guard
        icon = GFX_focus_FER_expand_guard
        prerequisite = { focus = FER_chokepoints }

        available = {
            has_war_support > 0.4
            civil_war_government_lock = yes
        }

        relative_position_id = FER_chokepoints
        x = 1 y = 1
        cost = 10

        completion_reward = {
            country_lock_all_division_template = no
        }
    }

    focus = {
        id = FER_hidden_army
        icon = GFX_focus_FER_hidden_army
        prerequisite = { focus = FER_expand_guard }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_expand_guard
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            add_ideas = idea_hidden_army
        }
    }

    focus = {
        id = FER_transit_authorities
        icon = GFX_focus_FER_transit_authorities
        prerequisite = { focus = FER_government }
        mutually_exclusive = { focus = FER_accept_chinese }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_stability = 0.05
            add_timed_idea = { idea = idea_transit_authorities years = 2 }
        }
    }

    focus = {
        id = FER_limit_chinese
        icon = GFX_focus_FER_limit_chinese
        prerequisite = { focus = FER_transit_authorities }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_transit_authorities
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_limit_chinese years = 1 }
        }
    }

    focus = {
        id = FER_accept_chinese
        icon = GFX_focus_FER_accept_chinese
        prerequisite = { focus = FER_government }
        mutually_exclusive = { focus = FER_transit_authorities }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_government
        x = 4 y = 1
        cost = 10

        completion_reward = {
            custom_effect_tooltip = tt_accept_chinese
            MAN = { add_opinion_modifier = { target = FER modifier = large_increase } }

            set_country_flag = accepted_chinese
        }
    }

    focus = {
        id = FER_duties
        icon = GFX_focus_FER_duties
        prerequisite = { focus = FER_accept_chinese }

        available = {
            civil_war_government_lock = yes
            not = { has_idea = closed_economy }
        }

        relative_position_id = FER_accept_chinese
        x = -1 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_abandon_duties years = 2 }
        }
    }

    focus = {
        id = FER_sika
        icon = GFX_focus_FER_sika
        prerequisite = { focus = FER_accept_chinese }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_accept_chinese
        x = 2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.1
            remove_ideas = idea_chinese_eastern_railway
            
            every_country = {
                limit = { has_subject = ROOT }
                country_event = { id = diplomacy.5 hours = 6 }
            }
        }
    }

    focus = {
        id = FER_manchuria
        icon = GFX_focus_FER_manchuria
        prerequisite = { focus = FER_limit_chinese focus = FER_duties }

        available = {
            has_stability > 0.6
            civil_war_government_lock = yes
        }

        relative_position_id = FER_limit_chinese
        x = 2 y = 1 
        cost = 10

        completion_reward = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
            add_political_power = 100
        }
    }

    focus = {
        id = FER_shared_fate
        icon = GFX_focus_FER_shared_fate
        prerequisite = { focus = FER_manchuria }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FER_manchuria
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_our_shared_fate
        }
    }
}