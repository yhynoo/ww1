focus_tree = {
    id = ITA_focus
    country = { factor = 0  modifier = { add = 20   original_tag = ITA } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = ITA_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = ITA }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 100
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
        }
    }

    focus = {
        id = ITA_claims
        icon = GFX_focus_ITA_claims
        prerequisite = { focus = ITA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ITA_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { or = { state = 186 state = 204 state = 208 state = 273 } }
                custom_effect_tooltip = tt_ultimatum
                hidden_effect = {
                    if = {
                        limit = { not = { is_claimed_by = ROOT } }
                        add_claim_by = ROOT
                    }
                }
                owner = {
                    hidden_effect = { country_event = { id = diplomacy.15 hours = 6 } }
                    effect_overlord_warning = yes
                }
            }
        }
    }

    focus = {
        id = ITA_men
        icon = GFX_focus_ITA_men
        prerequisite = { focus = ITA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ITA_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { or = { state = 33 state = 103 } }
                add_claim_by = ITA
            }
        }
    }

    focus = {
        id = ITA_take_ours
        icon = GFX_focus_ITA_take_ours
        prerequisite = { focus = ITA_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ITA_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            create_wargoal = { target = AUS type = take_claimed_state }
        }
    }
}