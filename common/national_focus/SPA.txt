focus_tree = {
    id = SPA_focus
    country = { factor = 0  modifier = { add = 20   original_tag = SPA } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = SPA_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = SPA }

        available = {
            is_subject = no
            controls_state = 290
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
            290 = {
                add_core_of = SPA
                add_building_construction = { type = bunker   level = 2   instant_build = yes     province = { all_provinces = yes limit_to_victory_point > 0 } }
            }
        }
    }

    focus = {
        id = SPA_claims
        icon = GFX_focus_SPA_claims
        prerequisite = { focus = SPA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SPA_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            MOR = {
                country_event = { id = demands.3 hours = 6 }
            }
        }
    }

    focus = {
        id = SPA_men
        icon = GFX_focus_SPA_men
        prerequisite = { focus = SPA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SPA_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { or = { state = 469 state = 471 } }
                add_claim_by = SPA
            }
        }
    }

    focus = {
        id = SPA_take_ours
        icon = GFX_focus_SPA_take_ours
        prerequisite = { focus = SPA_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SPA_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            create_wargoal = { target = POR type = annex_everything }
            add_war_support = 0.05
        }
    }
}