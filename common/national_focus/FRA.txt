focus_tree = {
    id = FRA_focus
    country = { factor = 0  modifier = { add = 20   original_tag = FRA } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = FRA_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = FRA }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            create_wargoal = { target = MOR type = puppet_wargoal_focus }
        }
    }

    focus = {
        id = FRA_claims
        icon = GFX_focus_FRA_claims
        prerequisite = { focus = FRA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { or = { state = 28 } }
                
                custom_effect_tooltip = tt_ultimatum
                owner = {
                    hidden_effect = { country_event = { id = diplomacy.15 hours = 6 } }
                    effect_overlord_warning = yes
                }
            }
        }
    }

    focus = {
        id = FRA_men
        icon = GFX_focus_FRA_men
        prerequisite = { focus = FRA_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            459 = {
                add_core_of = ROOT
                add_building_construction = { type = industrial_complex level = 1 instant_build = yes }
            }
            460 = {
                add_core_of = ROOT
                add_resource = { type = tungsten amount = 8 }
            }
        }
    }

    focus = {
        id = FRA_take_ours
        icon = GFX_focus_FRA_take_ours
        prerequisite = { focus = FRA_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { is_core_of = EGY }
                add_claim_by = FRA
            }
        }
    }
}