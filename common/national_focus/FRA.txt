focus_tree = {
    id = "FRA_focus"
    country = {
        factor = 0
        modifier = { add = 20   original_tag = FRA }
    }

    # shared
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    # special
    shared_focus = XXX_civil_war

    continuous_focus_position = { x = 88 y = 770 }

# state
    focus = {
        id = FRA_government
        icon = GFX_focus_FRA_government

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = { factor = 2 }

        x = 0 y = 0
        cost = 10

        completion_reward = {
            custom_effect_tooltip = tt_agadir_trials
            hidden_effect = { news_event = { id = france.3 hours = 6 } }
        }
    }

    focus = {
        id = FRA_morocco
        icon = GFX_focus_FRA_morocco
        prerequisite = { focus = FRA_government }
        mutually_exclusive = { focus = FRA_atlas }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            give_guarantee = MOR
        }
    }

    focus = {
        id = FRA_spain
        icon = GFX_focus_FRA_spain
        prerequisite = { focus = FRA_morocco }

        available = {
            and = { has_government = monarchism }
            civil_war_government_lock = yes
        }

        ai_will_do = { modifier = { has_war = yes factor = 0 } }        # don't start new wars.

        relative_position_id = FRA_morocco
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            SPA = { country_event = { id = diplomacy.10 hours = 6 } }
        }
    }

    focus = {
        id = FRA_algeria
        icon = GFX_focus_FRA_algeria
        prerequisite = { focus = FRA_morocco }

        available = {
            and = {
                459 = { is_owned_and_controlled_by = ROOT }
                460 = { is_owned_and_controlled_by = ROOT }
            }
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_morocco
        x = 1 y = 1
        cost = 10

        completion_reward = {
            459 = {
                add_resource = { type = tungsten amount = 12 }
                add_core_of = FRA
            }
            460 = {
                add_resource = { type = steel amount = 6 }
                add_core_of = FRA
            }
        }
    }

    focus = {
        id = FRA_egypt
        icon = GFX_focus_FRA_egypt
        prerequisite = { focus = FRA_algeria }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_algeria
        x = 0 y = 1
        cost = 10

        available = { has_war_support > 0.5 }

        completion_reward = {
            every_state = {
                limit = { is_core_of = EGY }
                add_claim_by = FRA
            }
        }
    }

    focus = {
        id = FRA_atlas
        icon = GFX_focus_generic_extend
        prerequisite = { focus = FRA_government }
        mutually_exclusive = { focus = FRA_morocco }

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = { modifier = { has_war = yes factor = 0 } }        # don't start new wars.

        relative_position_id = FRA_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            create_wargoal = { 
                target = MOR 
                type = puppet_wargoal_focus
            }
            460 = { 
                add_building_construction = { type = bunker level = 2 province = 7132 instant_build = yes } 
                add_building_construction = { type = infrastructure level = 1 instant_build = yes } 
            }

            # news event
            hidden_effect = { news_event = { id = france.6 hours = 6 } }
        }
    }

    focus = {
        id = FRA_entente
        icon = GFX_focus_FRA_entente
        prerequisite = { focus = FRA_atlas }
        mutually_exclusive = { focus = FRA_action }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_atlas
        x = 0 y = 1
        cost = 10

        completion_reward = {
            create_faction = "Entente"
            every_country = {
                limit = { or = { tag = ENG tag = RUS tag = SPA } not = { or = { is_in_faction = yes has_war_with = FRA } } }
                country_event = { id = diplomacy.12 days = 2 }
            }

            # news event
            hidden_effect = { news_event = { id = france.7 hours = 6 } }
        }
    }

    focus = {
        id = FRA_south_unrest
        icon = GFX_focus_FRA_south_unrest
        prerequisite = { focus = FRA_government }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_government
        x = 4 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_deal_with_unrest_south years = 1 }
            20 = {
                add_resource = { type = tungsten amount = 6 }
                add_building_construction = { type = arms_factory level = 1 instant_build = 1 }
            }
        }
    }

    focus = {
        id = FRA_action
        icon = GFX_focus_FRA_action
        prerequisite = { focus = FRA_south_unrest }
        mutually_exclusive = { focus = FRA_entente }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_south_unrest
        x = -1 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 100
            add_popularity = { ideology = monarchism popularity = 0.05 }
        }
    }

    focus = {
        id = FRA_union
        icon = GFX_focus_generic_stability
        prerequisite = { focus = FRA_south_unrest }

        available = {
            has_war = yes
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_south_unrest
        x = 2 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_union_sacree
        }
    }

    focus = {
        id = FRA_on_les_aura
        icon = GFX_focus_FRA_on_les_aura
        prerequisite = { focus = FRA_entente focus = FRA_action }

        available = {
            civil_war_government_lock = yes
            any_core_state = { is_owned_by = GER }
        }

        ai_will_do = { modifier = { has_war = yes factor = 0 } }        # don't start new wars.

        relative_position_id = FRA_entente
        x = 2 y = 1
        cost = 10

        completion_reward = {
            custom_effect_tooltip = tt_on_les_aura
            hidden_effect = {
                28 = {
                    save_event_target_as = demand_target
                    owner = { country_event = { id = diplomacy.1 hours = 6 } }
                }
            }

            # news event
            hidden_effect = { news_event = { id = france.8 hours = 6 } }
        }
    }

    focus = {
        id = FRA_east
        icon = GFX_focus_FRA_east
        prerequisite = { focus = FRA_on_les_aura }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = FRA_on_les_aura
        x = 0 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { not = { is_claimed_by = ROOT } or = { state = 553 state = 554 state = 411 state = 350 state = 414 } }
                add_claim_by = ROOT
            }
        }
    }
}