focus_tree = {
    id = GRE_focus
    country = { 
        factor = 0  
        modifier = { 
            add = 20   
            or = {
                original_tag = GRE 
                and = { is_greek_state = yes has_country_flag = completed_unification }
            }
        } 
    }
    
    continuous_focus_position = { x = 88 y = 770 }

    # state
    shared_focus = XXX_government

    # army
    shared_focus = XXX_address_troops
    shared_focus = XXX_general_staff

    # development
    shared_focus = XXX_land_use
    shared_focus = XXX_infrastructure

    # civil war
    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = GRE_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = GRE is_subject = no }
        available = { civil_war_government_lock = yes }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            
            custom_effect_tooltip = tt_unite_greece
            set_country_flag = allow_unification_decisions

            custom_effect_tooltip = tt_extend
            hidden_effect = { set_rule = { can_create_factions = yes } }
        }
    }

    focus = {
        id = GRE_claims
        icon = GFX_focus_GRE_claims
        prerequisite = { focus = GRE_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GRE_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { or = { state = 341 state = 340 state = 339 state = 164 } }
                add_claim_by = GRE
            }
        }
    }

    focus = {
        id = GRE_men
        icon = GFX_focus_GRE_men
        prerequisite = { focus = GRE_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GRE_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            47 = {
                add_extra_state_shared_building_slots = 1
                add_building_construction = { type = arms_factory level = 1 instant_build = yes }
            }
            186 = {
                add_resource = { type = steel amount = 6 }
                add_extra_state_shared_building_slots = 1
            }
        }
    }

    focus = {
        id = GRE_take_ours
        icon = GFX_focus_GRE_take_ours
        prerequisite = { focus = GRE_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GRE_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { state = 183 }
                save_event_target_as = demand_target

                owner = { 
                    country_event = { id = demands.1 hours = 6 } 
                    effect_overlord_warning = yes
                }
            }
        }
    }
}