# TARGETS
    shared_focus = {
        id = XXX_autonomy
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_allegiance }

        allow_branch = {
            has_dominion_focuses = yes
        }

        available = {
            civil_war_government_lock = yes
        }

        ai_will_do = {
            modifier = { tag = ROM factor = 2 }
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            custom_effect_tooltip = tt_autonomy

            hidden_effect = { OVERLORD = { country_event = { id = dominion.1 hours = 6 } } }
        }
    }

    shared_focus = {
        id = XXX_seek_aid
        icon = GFX_focus_XXX_seek_aid
        prerequisite = { focus = XXX_autonomy }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_autonomy
        x = -2 y = 1
        cost = 10

        completion_reward = {
            every_country = {
                limit = {
                    not = { has_subject = ROOT }
                    is_major = yes has_opinion = { target = ROOT    value > 0 }
                }
            }
            country_event = { id = dominion.2 hours = 6 }
        }
    }

    shared_focus = {
        id = XXX_hidden_mobilization
        icon = GFX_focus_XXX_hidden_mobilization
        prerequisite = { focus = XXX_autonomy }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_autonomy
        x = 1 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            add_timed_idea = { idea = idea_hidden_mobilization years = 1 }
        }
    }

    shared_focus = {
        id = XXX_awake
        icon = GFX_focus_XXX_awake
        prerequisite = { focus = XXX_hidden_mobilization }

        available = {
            has_war_support > 0.6
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_hidden_mobilization
        x = 0 y = 1
        cost = 10

        completion_reward = {
            hidden_effect = { leave_faction = yes }

            every_country = {
                limit = { has_subject = ROOT }
                country_event = { id = dominion.4 hours = 6 }

                hidden_effect = { 
                    set_autonomy = { target = FROM  autonomous_state = autonomy_free } 
                    country_event = dominion.8
                }
            }
        }
    }

# DETERRENCE
    shared_focus = {
        id = XXX_allegiance
        icon = GFX_focus_XXX_allegiance
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_autonomy }

        allow_branch = {
            has_dominion_focuses = yes
        }

        relative_position_id = XXX_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            custom_effect_tooltip = tt_allegiance

            hidden_effect = { OVERLORD = { country_event = { id = dominion.20 hours = 6 } } }
        }
    }

    shared_focus = {
        id = XXX_colonial_investments
        icon = GFX_focus_XXX_colonial_investments
        prerequisite = { focus = XXX_allegiance }
        mutually_exclusive = { focus = XXX_ethnocentrism }

        available = {
            civil_war_government_lock = yes
        }

        bypass = { is_subject = no }

        relative_position_id = XXX_allegiance
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_colonial_investments months = 24 }
            random_owned_controlled_state = {
                limit = { is_in_home_area = yes impassable = no }
                hidden_effect = { add_extra_state_shared_building_slots = 1 }   add_building_construction = { type = industrial_complex level = 1 instant_build = yes }
            }
        }
    }
#