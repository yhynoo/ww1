focus_tree = {
    id = ENG_focus
    country = { factor = 0  modifier = { add = 20   original_tag = ENG } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = ENG_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = ENG }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
            every_controlled_state = {
                limit = { or = { state = 116 state = 118 state = 183 } }
                add_building_construction = { type = naval_base level = 2 instant_build = yes }
                add_building_construction = {  type = coastal_bunker   level = 2   instant_build = yes  province = { all_provinces = yes limit_to_naval_bases = yes } }
            }
        }
    }

    focus = {
        id = ENG_claims
        icon = GFX_focus_ENG_claims
        prerequisite = { focus = ENG_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ENG_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            AFG = {
                country_event = { id = diplomacy.10 hours = 6 }
            }
        }
    }

    focus = {
        id = ENG_men
        icon = GFX_focus_ENG_men
        prerequisite = { focus = ENG_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ENG_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            every_country = {
                limit = { any_controlled_state = { is_core_of = EGY } }
                country_event = { id = diplomacy.15 hours = 6 }
                effect_overlord_warning = yes
            }
        }
    }

    focus = {
        id = ENG_take_ours
        icon = GFX_focus_ENG_take_ours
        prerequisite = { focus = ENG_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = ENG_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            every_state = {
                limit = { or = { state = 414 state = 421 state = 413 } }
                add_claim_by = ENG
            }
        }
    }
}