focus_tree = {
    id = GER_focus
    country = { factor = 0  modifier = { add = 20   original_tag = GER } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = GER_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = GER_deterrence }

        allow_branch = { original_tag = GER }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { or = { state = 8 state = 28 state = 34 } }
                add_core_of = ROOT
            }
        }
    }

    focus = {
        id = GER_claims
        icon = GFX_focus_GER_claims
        prerequisite = { focus = GER_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GER_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            HOL = {
                country_event = { id = diplomacy.10 hours = 6 }
                effect_overlord_warning = yes
            }
        }
    }

    focus = {
        id = GER_men
        icon = GFX_focus_GER_men
        prerequisite = { focus = GER_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GER_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            34 = {
                add_building_construction = { type = supply_node    level = 1   instant_build = yes     province = 9536 }
                build_railway = { start_province = 587 target_province = 9536 level = 2 }
            }
            51 = {
                add_building_construction = { type = synthetic_refinery    level = 1   instant_build = yes }
                add_extra_state_shared_building_slots = 2
            }
        }
    }

    focus = {
        id = GER_take_ours
        icon = GFX_focus_GER_take_ours
        prerequisite = { focus = GER_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = GER_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            create_wargoal = { target = FRA type = puppet_wargoal_focus }
        }
    }

# DETERRENCE
    focus = {
        id = GER_deterrence
        icon = GFX_focus_GER_deterrence
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = GER_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        allow_branch = {
            original_tag = GER
        }

        ai_will_do = {
            modifier = { has_war_support < 0.4  factor = 2 }
            modifier = { has_war_support > 0.65 factor = 0.4 }
        }

        relative_position_id = XXX_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            ENG = { country_event = { id = germany.1 hours = 6 } }
        }
    }
}