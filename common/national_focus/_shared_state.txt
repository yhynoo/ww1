## state
    shared_focus = {
        id = XXX_government
        icon = GFX_focus_XXX_government

        available = {
            uprising_government_lock = yes
            civil_war_government_lock = yes
        }

        x = 0 y = 0
        cost = 10

        completion_reward = {
            custom_original_question = yes
        }
    }

    shared_focus = {
        id = XXX_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { has_custom_expansion_targets = no }
        ai_will_do = { modifier = { has_war_support < 0.6 factor = 0.2 } }      # if you don't want to fight, then don't.

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
        }
    }

    shared_focus = {
        id = XXX_claims
        icon = GFX_focus_XXX_claims
        prerequisite = { focus = XXX_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            random_state = {
                limit = { 
                    not = { is_claimed_by = ROOT } any_neighbor_state = { is_owned_by = ROOT }
                    ROOT = { can_declare_war_on = PREV.owner }
                }
                add_claim_by = ROOT
            }
        }
    }

    shared_focus = {
        id = XXX_men
        icon = GFX_focus_XXX_men
        prerequisite = { focus = XXX_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_manpower_act     months = 6 }
        }
    }

    shared_focus = {
        id = XXX_take_ours
        icon = GFX_focus_XXX_take_ours
        prerequisite = { focus = XXX_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            random_state = {
                limit = { 
                    not = { is_claimed_by = ROOT } any_neighbor_state = { is_owned_by = ROOT }
                    ROOT = { can_declare_war_on = PREV.owner }
                }
                save_event_target_as = demand_target

                hidden_effect = { add_claim_by = ROOT }
                owner = { 
                    country_event = { id = diplomacy.1 hours = 6 } 
                    effect_overlord_warning = yes
                }
            }
        }
    }

    shared_focus = {
        id = XXX_deterrence
        icon = GFX_focus_XXX_deterrence
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { 
            focus = XXX_extend 
            focus = AUS_extend      focus = ENG_extend      focus = FRA_extend      focus = GER_extend      focus = RUS_extend      focus = TUR_extend   
            focus = JAP_extend      focus = MAN_extend      focus = SPA_extend      focus = GRE_extend      focus = HOL_extend      focus = SER_extend
            focus = ITA_extend
        }

        available = {
            civil_war_government_lock = yes
        }

        allow_branch = { not = { has_country_flag = starts_as_dominion } }

        relative_position_id = XXX_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            add_timed_idea = { idea = idea_deterrence months = 12 }
        }
    }

    shared_focus = {
        id = XXX_idealism
        icon = GFX_focus_XXX_idealism
        prerequisite = { focus = XXX_deterrence }
        mutually_exclusive = { focus = XXX_ethnocentrism }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_deterrence
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_idealism
        }
    }

    shared_focus = {
        id = XXX_stability
        icon = GFX_focus_generic_stability
        prerequisite = { focus = XXX_government }

        available = {
            civil_war_government_lock = yes
        }

        allow_branch = { not = { has_country_flag = starts_as_dominion } }

        relative_position_id = XXX_government
        x = 4 y = 1
        cost = 10

        completion_reward = {
            add_popularity = { ideology = ROOT popularity = 0.05 }
            add_stability = 0.05
        }
    }

    shared_focus = {
        id = XXX_ethnocentrism
        icon = GFX_focus_XXX_ethnocentrism
        prerequisite = { focus = XXX_stability }
        mutually_exclusive = { focus = XXX_idealism }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_stability
        x = -1 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_ethnocentrism
        }
    }

    shared_focus = {
        id = XXX_agrarianism
        icon = GFX_focus_generic_agrarian
        prerequisite = { focus = XXX_stability }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_stability
        x = 2 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            add_timed_idea = { idea = idea_agrarians years = 2 }
        }
    }

    shared_focus = {
        id = XXX_administration
        icon = GFX_focus_XXX_administration
        prerequisite = { focus = XXX_idealism focus = XXX_ethnocentrism }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_idealism
        x = 2 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { any_controlled_state = { is_claimed_by = ROOT not = { is_core_of = ROOT } } }
                every_controlled_state = {
                    limit = { is_claimed_by = ROOT not = { is_core_of = ROOT } }
                    add_core_of = ROOT
                }
            }
            else = {
                custom_effect_tooltip = tt_administration
            }
            add_stability = 0.05
        }
    }

    shared_focus = {
        id = XXX_unity
        icon = GFX_focus_generic_unity
        prerequisite = { focus = XXX_administration }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_administration
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_strength_in_unity
        }
    }

## dominion
    shared_focus = {
        id = XXX_allegiance
        icon = GFX_focus_XXX_allegiance
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_on_our_own }

        available = {
            is_subject = yes
            civil_war_government_lock = yes
        }

        allow_branch = { has_country_flag = starts_as_dominion }

        relative_position_id = XXX_government
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            add_stability = 0.05
        }
    }

    shared_focus = {
        id = XXX_colonial_investments
        icon = GFX_focus_XXX_colonial_investments
        prerequisite = { focus = XXX_allegiance }

        available = {
            civil_war_government_lock = yes
        }

        bypass = { is_subject = no }

        relative_position_id = XXX_allegiance
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_timed_idea = { idea = idea_colonial_investments months = 24 }
            random_owned_controlled_state = {
                limit = { is_in_home_area = yes impassable = no }
                hidden_effect = { add_extra_state_shared_building_slots = 1 }   add_building_construction = { type = industrial_complex level = 1 instant_build = yes }
            }
        }
    }

    shared_focus = {
        id = XXX_on_our_own
        icon = GFX_focus_XXX_on_our_own
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_allegiance }

        available = {
            is_subject = yes
            civil_war_government_lock = yes
        }

        bypass = { is_subject = no }

        allow_branch = { has_country_flag = starts_as_dominion }

        relative_position_id = XXX_government
        x = 4 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
        }
    }

    shared_focus = {
        id = XXX_seek_aid
        icon = GFX_focus_XXX_seek_aid
        prerequisite = { focus = XXX_on_our_own }

        available = {
            civil_war_government_lock = yes
        }

        bypass = { is_subject = no }

        relative_position_id = XXX_on_our_own
        x = -1 y = 1
        cost = 10

        completion_reward = {
            every_country = {
                limit = {
                    not = { has_subject = ROOT }
                    is_major = yes has_opinion = { target = ROOT    value > 0 }
                }
            }
            country_event = { id = diplomacy.3 hours = 6 }
        }
    }

    shared_focus = {
        id = XXX_awake
        icon = GFX_focus_XXX_awake
        prerequisite = { focus = XXX_on_our_own }

        available = {
            civil_war_government_lock = yes
        }

        bypass = { is_subject = no }

        relative_position_id = XXX_on_our_own
        x = 2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.1
            every_country = {
                limit = { has_subject = ROOT }
                country_event = { id = diplomacy.5 hours = 6 }
            }
        }
    }

    shared_focus = {
        id = XXX_administration_dominion
        icon = GFX_focus_XXX_administration
        prerequisite = { focus = XXX_colonial_investments focus = XXX_seek_aid }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_colonial_investments
        x = 2 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { any_controlled_state = { is_claimed_by = ROOT not = { is_core_of = ROOT } } }
                every_controlled_state = {
                    limit = { is_claimed_by = ROOT not = { is_core_of = ROOT } }
                    add_core_of = ROOT
                }
            }
            else = {
                custom_effect_tooltip = tt_administration
            }
            add_stability = 0.05
        }
    }

    shared_focus = {
        id = XXX_unity_dominion
        icon = GFX_focus_generic_unity
        prerequisite = { focus = XXX_administration_dominion }

        available = {
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_administration_dominion
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_ideas = idea_strength_in_unity
        }
    }