focus_tree = {
    id = JAP_focus
    country = { factor = 0  modifier = { add = 20   original_tag = JAP } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = JAP_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = JAP }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
            every_controlled_state = {
                limit = { is_core_of = KOR }
                add_core_of = JAP
                add_building_construction = { type = dockyard level = 1 instant_build = yes }
            }
        }
    }

    focus = {
        id = JAP_claims
        icon = GFX_focus_JAP_claims
        prerequisite = { focus = JAP_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = JAP_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_political_power = 50
            MAN = {
                country_event = { id = diplomacy.10 hours = 6 }
            }
        }
    }

    focus = {
        id = JAP_men
        icon = GFX_focus_JAP_men
        prerequisite = { focus = JAP_extend }

        available = {
            is_subject = no
            controls_state = 524
            civil_war_government_lock = yes
        }

        relative_position_id = JAP_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { state = 524 }
                add_core_of = JAP
                add_resource = { type = tungsten amount = 8 }
            }
        }
    }

    focus = {
        id = JAP_take_ours
        icon = GFX_focus_JAP_take_ours
        prerequisite = { focus = JAP_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = JAP_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            431 = {
                save_event_target_as = demand_target
                hidden_effect = { add_claim_by = ROOT }

                owner = { 
                    country_event = { id = diplomacy.1 hours = 6 } 
                }
                effect_overlord_warning = yes
            }
        }
    }
}