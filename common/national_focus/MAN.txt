focus_tree = {
    id = MAN_focus
    country = { factor = 0  modifier = { add = 20   original_tag = MAN } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = MAN_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = MAN is_subject = no }
        available = { civil_war_government_lock = yes }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }
            every_state = {
                limit = { is_core_of = FER }
                
                custom_effect_tooltip = tt_ultimatum
                owner = {
                    hidden_effect = { country_event = { id = demands.2 hours = 6 } }
                    effect_overlord_warning = yes
                }
            }
        }
    }

    focus = {
        id = MAN_claims
        icon = GFX_focus_MAN_claims
        prerequisite = { focus = MAN_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = MAN_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            create_wargoal = { target = MON type = annex_everything }
        }
    }

    focus = {
        id = MAN_men
        icon = GFX_focus_MAN_men
        prerequisite = { focus = MAN_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = MAN_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            every_state = {
                limit = { is_core_of = KOR }
                add_claim_by = MAN
            }
        }
    }

    focus = {
        id = MAN_take_ours
        icon = GFX_focus_MAN_take_ours
        prerequisite = { focus = MAN_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = MAN_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            524 = {
                save_event_target_as = demand_target
                hidden_effect = { add_claim_by = ROOT }

                owner = { 
                    country_event = { id = demands.1 hours = 6 } 
                    effect_overlord_warning = yes
                }
            }
        }
    }
}