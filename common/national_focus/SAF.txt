focus_tree = {
    id = SAF_focus
    country = { factor = 0  modifier = { add = 20   original_tag = SAF } }
    continuous_focus_position = { x = 88 y = 770 }

    shared_focus = XXX_government
    shared_focus = XXX_general_staff
    shared_focus = XXX_infrastructure

    shared_focus = XXX_civil_war

# TARGETS
    focus = {
        id = SAF_extend
        icon = GFX_focus_generic_extend
        prerequisite = { focus = XXX_government }
        mutually_exclusive = { focus = XXX_deterrence }

        allow_branch = { original_tag = SAF }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = XXX_government
        x = -4 y = 1
        cost = 10

        completion_reward = {
            if = {
                limit = { is_subject = no }
                custom_effect_tooltip = tt_extend
                hidden_effect = { set_rule = { can_create_factions = yes } }
            }

            every_state = {
                limit = { or = { state = 42 state = 53 } }
                add_claim_by = SAF
            }
        }
    }

    focus = {
        id = SAF_claims
        icon = GFX_focus_SAF_claims
        prerequisite = { focus = SAF_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SAF_extend
        x = -2 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            random_state = {
                limit = { state = 541 }
                save_event_target_as = demand_target

                owner = { 
                    country_event = { id = demands.1 hours = 6 } 
                    effect_overlord_warning = yes
                }
            }
        }
    }

    focus = {
        id = SAF_men
        icon = GFX_focus_SAF_men
        prerequisite = { focus = SAF_extend }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SAF_extend
        x = 1 y = 1
        cost = 10

        completion_reward = {
            346 = {
                add_extra_state_shared_building_slots = 1
                add_building_construction = { type = arms_factory level = 1 instant_build = yes }
            }
            544 = {
                add_resource = { type = steel amount = 6 }
                add_extra_state_shared_building_slots = 2
            }
        }
    }

    focus = {
        id = SAF_take_ours
        icon = GFX_focus_SAF_take_ours
        prerequisite = { focus = SAF_men }

        available = {
            is_subject = no
            civil_war_government_lock = yes
        }

        relative_position_id = SAF_men
        x = 0 y = 1
        cost = 10

        completion_reward = {
            add_war_support = 0.05
            every_state = {
                limit = { state = 542 }
                hidden_effect = { 
                    add_claim_by = ROOT
                    SAF = { create_wargoal = { target = PREV.owner type = take_claimed_state } } 
                }

                custom_effect_tooltip = tt_wargoal_against_owner
            }
        }
    }
}