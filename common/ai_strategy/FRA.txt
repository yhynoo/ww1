FRA_army_composition = {
	allowed = { original_tag = FRA }
	enable = { always = yes }
	abort_when_not_enabled = yes

	## Land troops
	ai_strategy = {
		type = role_ratio
		id = infantry
		value = 90
	}

	ai_strategy = {
		type = role_ratio
		id = cyclist
		value = 10
	}

	ai_strategy = {
		type = role_ratio
		id = mountaineers
		value = 5
	}
}

# don't work for ITA or RUS, we need those troops at home
FRA_stay_at_home = {
	allowed = { tag = FRA }
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = dont_defend_ally_borders
		id = ITA
		value = 100
	}

    ai_strategy = {
		type = dont_defend_ally_borders
		id = SOV
		value = 100
	}

	# we're also scared of Germany
	ai_strategy = {
		type = prepare_for_war
		id = GER
		value = 30
	}
}

FRA_careful = {
	allowed = { tag = FRA }
	enable = { HOL = { is_guaranteed_by = GER } is_in_faction = no }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = declare_war
		id = HOL
		value = -100
	}
}

FRA_likes_COL_and_SOV = {
	allowed = { tag = FRA }
	enable = { has_country_flag = chose_alternative }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = support
		id = COL
		value = 60
	}

	ai_strategy = {
		type = support
		id = SOV
		value = 60
	}
}