FRA_against_germany = {
    allowed = { original_tag = FRA }
    enable = { always = yes }

    ai_strategy = { type = alliance     id = POL    value = 30 }
    ai_strategy = { type = alliance     id = RUS    value = 60 }

    # GER
    ai_strategy = { type = contain     id = GER    value = 60 }
}

FRA_supports = {
    allowed = { not = { original_tag = FRA } }
    enable = { has_war_with = GER }

    enable_reverse = {
        FRA = {
            has_completed_focus = XXX_idealism
            has_government = democratic
        }
    }

    reversed = yes
    abort_when_not_enabled = yes

    ai_strategy = { type = support                  id = FRA    value = 60 }
    ai_strategy = { type = send_volunteers_desire   id = FRA    value = 30 }
}