FRA_against_GER = {
    allowed = { original_tag = FRA }
    enable = { always = yes }

    ai_strategy = { type = alliance     id = ENG    value = 60 }
    ai_strategy = { type = alliance     id = SPA    value = 60 }
    ai_strategy = { type = alliance     id = TUR    value = 30 }

    # GER
    ai_strategy = { type = contain        id = GER    value = 20 }
    ai_strategy = { type = antagonize     id = GER    value = 60 }
}