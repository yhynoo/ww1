SER_stay_alone = {
    allowed = { original_tag = SER }
    enable = { and = { has_war_with = TUR is_in_faction = no } }
    reversed = yes

    ai_strategy = { type = alliance     id = SER        value = -120 }
}

SER_anti_ottoman = {
    allowed = { original_tag = SER }
    enable = { 
        has_country_flag = anti_ottoman_alliance 
        ROM = { not = { is_subject = no not = { has_war_with = TUR } } }
    }

    ai_strategy = { type = protect     id = ROM        value = 60 }
}

SER_alliances = {
	allowed = { original_tag = SER }
    enable = { not = { has_country_leader = { character = SER_konstantin_konstantinovic ruling_only = yes } } }
	abort_when_not_enabled = yes

	# alliances
	ai_strategy = { type = alliance		id = SOV		value = 60 }
    ai_strategy = { type = alliance		id = FRA		value = 40 }
}

SER_alliances_alternative = {
	allowed = { original_tag = SER }
	enable = { has_country_leader = { character = SER_konstantin_konstantinovic ruling_only = yes } }

	ai_strategy = { type = alliance		id = AUS		value = 100 }
    ai_strategy = { type = befriend		id = AUS		value = 60 }

	ai_strategy = { type = alliance		id = GER		value = 40 }
}