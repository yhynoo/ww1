# CENTRAL POWERS

central_powers_acceptance = {
    allowed = {
        original_tag = MEX
    }
    enable = {                                          
        and = {                                                             # mexico
            original_tag = MEX
            has_war_with = USA
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 20
    }
}

central_powers_invite = {
    allowed = { original_tag = GER }
    enable = {                                        
        and = {                                                             # mexico
            original_tag = MEX
            has_war_with = USA
        }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 25
    }
}

# SOUTH AMERICAN NEUTRALITY

argentine_neutrality = {
    allowed = { original_tag = ARG }
    enable = { 
        not = { original_tag = FRA }
        not = { ARG = { has_completed_focus = ARG_traditionalism } }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = ARG
        value = -100
    }
}

mexican_neutrality = {
    allowed = { original_tag = MEX }
    enable = { not = { original_tag = GER } }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = MEX
        value = -100
    }
}

# hate

russia_hates = {
    allowed = { original_tag = SOV }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = antagonize
        id = AUS
        value = 50
    }

    ai_strategy = {
        type = protect
        id = SER
        value = 50
    }
}