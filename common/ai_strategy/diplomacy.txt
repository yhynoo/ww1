# central powers

central_powers_acceptance = {
    allowed = {
        OR = {
            original_tag = AUS
            original_tag = TUR
        }
    }
    enable = {
        or = {
            original_tag = AUS
            and = { 
                original_tag = TUR
                or = { 
                    has_government = monarchism 
                    and = { has_country_flag = cotton_deal owns_state = 447 }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 100
    }
}

central_powers_invite = {
    allowed = { original_tag = GER }
    enable = {
        or = {
            original_tag = AUS
            and = {
                original_tag = TUR
                or = { 
                    has_government = monarchism 
                    and = { has_country_flag = cotton_deal owns_state = 447 }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 100
    }
}


# entente

entente_acceptance = {
    allowed = {
        OR = {
            original_tag = ENG
            original_tag = GRE
            original_tag = ITA
            original_tag = SOV
            original_tag = USA
        }
    }
    enable = {
        or = {
            has_idea = idea_containment                                         # england
            original_tag = GRE                                                  # greece
            and = {                                                             # italy
                and = { 
                    not = { has_country_flag = neutral_italy }
                    not = { has_completed_focus = ITA_on_our_own }
                }
                FRA = { war_length_with = { tag = AUS months > 6 } }
            }
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism }
            }
            and = {                                                             # usa
                has_completed_focus = USA_hearts_and_minds
                has_war_support > 0.55 
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = FRA
        value = 100
    }
}

entente_invite = {
    allowed = { original_tag = FRA }
    enable = {
        or = {
            has_idea = idea_containment                                         # england
            original_tag = GRE                                                  # greece
            and = {                                                             # italy
                and = { 
                    not = { has_country_flag = neutral_italy }
                    not = { has_completed_focus = ITA_on_our_own }
                }
                FRA = { war_length_with = { tag = AUS months > 6 } }
            }
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism }
            }
            and = {                                                             # usa
                has_completed_focus = USA_hearts_and_minds
                has_war_support > 0.55 
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = { FRA = { is_faction_leader = yes } }

    ai_strategy = {
        type = alliance
        id = FRA
        value = 100
    }
}

    # OTHER THINGS #

# ambitions

greece_and_her_lands = {
    allowed = { original_tag = GRE }
    enable = {
        war_length_with = { tag = TUR months > 6 }
        not = { has_government = communism }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = {
        any_state = { is_core_of = GRE is_owned_by = TUR }
    }

    ai_strategy = {
        type = alliance
        id = GRE
        value = 100
    }
}

italy_and_france = {
    allowed = { original_tag = ITA }
    enable = {
        war_length_with = { tag = FRA months > 3 }
        not = { has_government = communism }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = {
        has_completed_focus = ITA_on_our_own
        has_war_support > 0.5
    }

    ai_strategy = {
        type = alliance
        id = ITA
        value = 100
    }

    ai_strategy = {
        type = support
        id = ITA
        value = 100
    }
}

italy_and_tirol = {
    allowed = { original_tag = ITA }
    enable = {
        war_length_with = { tag = AUS months > 6 }
        not = { has_government = communism }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = {
        has_completed_focus = ITA_traditionalism
        has_war_support > 0.5
    }

    ai_strategy = {
        type = alliance
        id = ITA
        value = 100
    }

    ai_strategy = {
        type = support
        id = ITA
        value = 100
    }
}

japan_and_islands = {
    allowed = { original_tag = JAP }
    enable = { 
        has_completed_focus = JAP_deterrence
        GER = { 
            or = { controls_state = 431 controls_state = 633 controls_state = 647 controls_state = 684 }
            has_war = yes
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = declare_war
        id = GER
        value = 100
    }
}

# hate

austria_hates_russia = {
    allowed = { original_tag = AUS }
    enable = { or = { original_tag = SOV is_ally_with = SOV } }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = antagonize
        id = AUS
        value = 300
    }
}

italy_hates_austria = {
    allowed = { original_tag = ITA }
    enable = { or = { original_tag = AUS is_ally_with = AUS } }
    abort_when_not_enabled = yes

    enable_reverse = {                                          # unless we get our claims, we won't back up Austrians.
        has_completed_focus = ITA_traditionalism
        not = { has_country_flag = neutral_italy }
    }
    reversed = yes

    ai_strategy = {
        type = antagonize
        id = ITA
        value = 300
    }
}

italy_hates_turkey = {
    allowed = { original_tag = ITA }
    enable = { or = { original_tag = TUR is_ally_with = TUR } }
    abort_when_not_enabled = yes

    enable_reverse = { always = yes }
    reversed = yes

    ai_strategy = {
        type = antagonize
        id = ITA
        value = 300
    }
}

russia_hates_austria_and_turkey = {                     # due to south-east europe
    allowed = { original_tag = SOV }
    enable = { 
        or = { 
            original_tag = AUS is_ally_with = AUS 
            original_tag = TUR is_ally_with = TUR 
        } 
    }

    reversed = yes

    ai_strategy = {
        type = antagonize
        id = SOV
        value = 300
    }
}

turkey_hates_italy = {
    allowed = { original_tag = TUR }
    enable = { or = { original_tag = ITA is_ally_with = ITA } }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = antagonize
        id = TUR
        value = 300
    }
}

turkey_hates_russia = {
    allowed = { original_tag = TUR }
    enable = { or = { original_tag = SOV is_ally_with = SOV } }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = antagonize
        id = TUR
        value = 300
    }
}