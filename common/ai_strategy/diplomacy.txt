# central powers

central_powers_acceptance = {
    allowed = {
        OR = {
            original_tag = AUS
            original_tag = HOL
            original_tag = TUR
        }
    }
    enable = {
        or = {
            original_tag = AUS
            and = {
                original_tag = HOL
                has_country_flag = ignored_france
            }
            and = { 
                original_tag = TUR
                or = { 
                    has_government = monarchism 
                    and = { has_country_flag = cotton_deal owns_state = 447 }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 20
    }
}

central_powers_invite = {
    allowed = { original_tag = GER }
    enable = {
        or = {
            original_tag = AUS
            and = {
                original_tag = HOL
                has_country_flag = ignored_france
            }
            and = {
                original_tag = TUR
                or = { 
                    has_government = monarchism 
                    and = { has_country_flag = cotton_deal owns_state = 447 }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 25
    }
}


# entente

entente_acceptance = {
    allowed = {
        OR = {
            original_tag = ENG
            original_tag = GRE
            original_tag = ITA
            original_tag = SOV
            original_tag = USA
        }
    }
    enable = {
        or = {
            has_completed_focus = ENG_seek_alliances                            # england
            original_tag = GRE                                                  # greece
            and = {                                                             # italy
                not = { has_country_flag = neutral_italy }
                not = { has_completed_focus = ITA_on_our_own }
                FRA = { war_length_with = { tag = AUS months > 6 } }
            }
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism }
            }
            and = {                                                             # usa
                has_completed_focus = USA_traditionalism
                and = { 
                    has_war_support > 0.55
                    FRA = { war_length_with = { tag = GER months > 24 } }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = FRA
        value = 25
    }
}

entente_invite = {
    allowed = { original_tag = FRA }
    enable = {
        or = {
            has_completed_focus = ENG_seek_alliances                            # england
            original_tag = GRE                                                  # greece
            and = {                                                             # italy
                not = { has_country_flag = neutral_italy }
                not = { has_completed_focus = ITA_on_our_own }
                FRA = { war_length_with = { tag = AUS months > 6 } }
            }
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism }
            }
            and = {                                                             # usa
                has_completed_focus = USA_traditionalism
                and = { 
                    has_war_support > 0.55
                    FRA = { war_length_with = { tag = GER months > 24 } }
                }
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = { FRA = { is_faction_leader = yes } }

    ai_strategy = {
        type = alliance
        id = FRA
        value = 30
    }
}

# OTHER THINGS #

# ambitions

greece_and_her_lands = {
    allowed = { original_tag = GRE }
    enable = {
        war_length_with = { tag = TUR months > 6 }
        not = { has_government = communism }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = {
        any_state = { is_core_of = GRE is_owned_by = TUR }
    }

    ai_strategy = {
        type = alliance
        id = GRE
        value = 25
    }
}

italy_and_austria = {
    allowed = { original_tag = ITA }
    enable = {
        and = { has_completed_focus = ITA_traditionalism has_war_support > 0.5 }
        not = { has_country_flag = neutral_italy }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = contain
        id = AUS
        value = 15
    }
}

japan_and_islands = {
    allowed = { original_tag = JAP }
    enable = { 
        has_completed_focus = JAP_deterrence
        GER = { 
            or = { controls_state = 431 controls_state = 633 controls_state = 647 controls_state = 684 }
            has_war = yes
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = contain
        id = GER
        value = 60
    }
}

# hate

austria_hates_russia = {
    allowed = { original_tag = AUS }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = contain
        id = SOV
        value = 50
    }

    ai_strategy = {
        type = antagonize
        id = SOV
        value = 60
    }
}

italy_hates = {
    allowed = { original_tag = SOV }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = conquer
        id = TUR
        value = 30
    }
}

russia_hates = {
    allowed = { original_tag = SOV }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = antagonize
        id = AUS
        value = 50
    }

    ai_strategy = {
        type = protect
        id = SER
        value = 50
    }
}

turkey_hates = {
    allowed = { original_tag = TUR }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = contain
        id = ITA
        value = 50
    }

    ai_strategy = {
        type = contain
        id = SOV
        value = 30
    }
}