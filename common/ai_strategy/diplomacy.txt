# CENTRAL POWERS

central_powers_acceptance = {
    allowed = {
        OR = {
            original_tag = HOL
            original_tag = ITA
            original_tag = MEX
        }
    }
    enable = {
        or = {                                              
            and = {                                                             # holland
                original_tag = HOL
                has_country_flag = ignored_france
            }
            and = {                                                             # italy
                has_completed_focus = ITA_on_our_own
                not = { GER = { is_ally_with = TUR } }                                
            }
            and = {                                                             # mexico
                original_tag = MEX
                has_war_with = USA
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 20
    }
}

central_powers_invite = {
    allowed = { original_tag = GER }
    enable = {
        or = {                                              
            and = {                                                             # holland
                original_tag = HOL
                has_country_flag = ignored_france
            }
            and = {                                                             # italy
                has_completed_focus = ITA_on_our_own
                not = { GER = { is_ally_with = TUR } }                                
            }
            and = {                                                             # mexico
                original_tag = MEX
                has_war_with = USA
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 25
    }
}


# ENTENTE

entente_acceptance = {
    allowed = {
        OR = {
            original_tag = ENG
            original_tag = ITA
            original_tag = SOV
        }
    }
    enable = {
        or = {
            has_completed_focus = ENG_seek_alliances                            # england
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism has_completed_focus = SOV_seek_alliances }
                not = { has_truce_with = GER }
            }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = FRA
        value = 25
    }
}

entente_invite = {
    allowed = { original_tag = FRA }
    enable = {
        or = {
            has_completed_focus = ENG_seek_alliances                            # england
            and = {                                                             # russia
                original_tag = SOV 
                or = { has_government = democratic has_government = monarchism has_completed_focus = SOV_seek_alliances }
                not = { has_truce_with = GER }
            }
        }
    }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = { FRA = { is_faction_leader = yes } }

    ai_strategy = {
        type = alliance
        id = FRA
        value = 30
    }
}

# OTHER THINGS #


# SOUTH AMERICAN NEUTRALITY

argentine_neutrality = {
    allowed = { original_tag = ARG }
    enable = { 
        not = { original_tag = FRA }
        not = { ARG = { has_completed_focus = ARG_traditionalism } }
    }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = ARG
        value = -100
    }
}

mexican_neutrality = {
    allowed = { original_tag = MEX }
    enable = { not = { original_tag = GER } }
    abort_when_not_enabled = yes

    reversed = yes

    ai_strategy = {
        type = alliance
        id = MEX
        value = -100
    }
}

# hate

russia_hates = {
    allowed = { original_tag = SOV }
    enable = { always = yes }
    abort = { always = no }

    ai_strategy = {
        type = antagonize
        id = AUS
        value = 50
    }

    ai_strategy = {
        type = protect
        id = SER
        value = 50
    }
}