GRE_stay_alone = {
    allowed = { original_tag = GRE }
    enable = { date < 1916.1.1 }
    reversed = yes

    ai_strategy = { type = alliance     id = GRE        value = -120 }
}

GRE_anti_ottoman = {
    allowed = { original_tag = GRE }
    enable = { 
        has_country_flag = anti_ottoman_alliance 
        ROM = { not = { is_subject = no not = { has_war_with = TUR } } }
    }

    ai_strategy = { type = protect     id = ROM        value = 60 }
}