GRE_alliances = {
    allowed = { original_tag = GRE }
    enable = { has_completed_focus = GRE_traditionalism }
    abort = {               
        has_completed_focus = GRE_the_everlasting_spirit                        # once we realize the Megali Idea, we will be neutral again.
        not = { GRE = { any_core_state = { not = { is_owned_and_controlled_by = GRE } } } }
    }

    reversed = yes
    enable_reverse = {
        any_enemy_country = { original_tag = TUR }
    }

    ai_strategy = { type = alliance     id = GRE        value = 60 }
}

GRE_schism = {
    allowed = { original_tag = GRE }
    enable = { has_idea = idea_the_great_schism }
    abort_when_not_enabled = yes

    reversed = yes
    enable_reverse = { always = yes }

    ai_strategy = { type = alliance		id = GRE		value = -200 }
}