italy_war_for_libya = {
	allowed = { tag = ITA }
	enable = { has_completed_focus = ITA_deterrence }
	abort = {
		or = {
			TUR = { is_in_faction = yes }
			any_country = { has_guaranteed = TUR }
		}
	}

	ai_strategy = {
		type = declare_war
		id = TUR
		value = 150
	}
}

italy_war_for_austria = {
	allowed = { tag = ITA }
	enable = { 
		has_completed_focus = ITA_traditionalism
		any_state = {
			is_core_of = ITA
			is_controlled_by = AUS
		}
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = contain
		id = AUS
		value = 300
	}
}