ITA_alliances = {
	allowed = { original_tag = ITA }
    enable = { 
        has_completed_focus = ITA_traditionalism
    }
    abort = {               
        has_completed_focus = ITA_the_everlasting_spirit                        # once we get hold of all our claims, we will be neutral again.
        not = { ITA = { any_core_state = { not = { is_owned_and_controlled_by = ITA } } } }
    }

    reversed = yes
    enable_reverse = {
		or = {
			any_enemy_country = { original_tag = AUS }
        	any_enemy_country = { original_tag = TUR }
		}
    }

    ai_strategy = {
        type = alliance     id = ITA        value = 60
    }
}

ITA_expansion_in_africa = {
	allowed = { original_tag = ITA }
	enable = {
		has_completed_focus = ITA_deterrence
	}
	abort = {
		 not = { ITA = { any_claimed_state = { not = { is_owned_and_controlled_by = TUR } } } }
	}

	ai_strategy = {
        type = declare_war     id = TUR        	value = 100
    }

	# execution
	ai_strategy = {
		type = naval_invasion_focus				value = 250
	}

	ai_strategy = {
		type = invasion_unit_request	value = 100
		state_trigger = {
			or = { state = 44 state = 186 state = 204 }
		}
	}
}