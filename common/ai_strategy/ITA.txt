italy_war_for_libya = {
	allowed = { tag = ITA }
	enable = { has_completed_focus = ITA_deterrence }
	abort = {
		or = {
			TUR = { is_in_faction = yes }
			any_country = { has_guaranteed = TUR }
		}
	}

	ai_strategy = {
		type = declare_war
		id = TUR
		value = 150
	}

	# add tactics for the invasion of libya here:
	ai_strategy = {
		type = naval_invasion_focus
		value = 30
	}

	ai_strategy = {
		type = invasion_unit_request
		area = libyan_invasion_area
		value = 300
	}
}