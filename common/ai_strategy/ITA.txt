ITA_alliances = {
    allowed = { original_tag = ITA }
    enable = { 
        has_completed_focus = ITA_traditionalism 
        GER = { has_war_with = TUR }
    }

	ai_strategy = { type = alliance     id = GER        value = 40 }
}

ITA_alliances_alternative = {
    allowed = { original_tag = ITA }
    enable = { 
        has_civil_war = no
        has_country_flag = monarchist_orientation 
        has_idea = idea_war_enthusiasm
    }
    abort_when_not_enabled = yes

    ai_strategy = { type = alliance     id = ENG        value = 60 }
    ai_strategy = { type = alliance     id = FRA        value = 60 }
}

ITA_not_with_TUR = {
    allowed = { original_tag = TUR }
	enable = { always = yes }

    reverse_enable = { or = { is_ally_with = TUR has_guaranteed = TUR } }
    reversed = yes

    ai_strategy = { type = alliance     id = ITA        value = -200 }
}