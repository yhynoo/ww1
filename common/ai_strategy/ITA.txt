ITA_dislikes_TUR = {
	allowed = { tag = ITA }
	enable = { has_completed_focus = ITA_deterrence }
	abort = {
		or = {
			TUR = { is_in_faction = yes }
			any_country = { has_guaranteed = TUR }
		}
	}

	ai_strategy = {
		type = declare_war
		id = TUR
		value = 150
	}
}