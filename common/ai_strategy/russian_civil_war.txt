isolated_conflict = {
    allowed = { 
        or = { original_tag = SOV tag = CRI tag = POL = tag = UBD tak = UKR }
    }
    enable = { SOV = { has_civil_war = yes } }
    abort_when_not_enabled = yes

    reverse = yes

    ai_strategy = {
	    type = alliance
	    id = ROOT
	    value = -600
	}
}