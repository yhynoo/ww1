# DIPLOMACY

MAN_alliances = {
    allowed = { original_tag = MAN }
    enable = { has_completed_focus = MAN_deterrence }
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = ENG		value = 40 }
	ai_strategy = { type = alliance		id = FRA		value = 20 }

    # aggressive wargoals: none
}

MAN_attention = {
    allowed = { original_tag = MAN }
    enable = { or = { is_ally_with = ENG is_ally_with = FRA } }
    abort_when_not_enabled = yes

    ai_strategy = { type = avoid_starting_wars      value = -100 }
}

MAN_isolation = {
    allowed = { original_tag = MAN }
    enable = { always = yes }

    # don't like Japan
    ai_strategy = { type = antagonize		id = JAP		value = 20 }

    # don't tolerate cliques
    ai_strategy = { type = conquer      id = ANH		value = 200 }
    ai_strategy = { type = conquer      id = SIK		value = 200 }
    ai_strategy = { type = conquer      id = YUN		value = 200 }
}

# ARMY

MAN_asia = {
    allowed = { tag = MAN }
    enable = { always = yes }

    # areas
    ai_strategy = { type = area_priority    id = europe         value = -200 }
}

MAN_warlords = {
    allowed = { or = { tag = ANH tag = MON tag = SIK tag = YUN } }
    enable = { surrender_progress < 0.2 }                               # once the warlord holds most of his land, he won't attack that much.
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control
        tag = MAN
        ratio = 0.5 ordertype = front execution_type = careful execute_order = yes   
    }
}