MAN_is_scared = {
    allowed = { original_tag = MAN }
    enable = { 
        or = { 
            JAP = { is_ally_with = ENG }
            SOV = { has_completed_focus = SOV_far_east_affairs }
        } 
    }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = alliance
        id = GER
        value = 300
    }

    ai_strategy = {
		type = diplo_action_acceptance
		id = GER
		value = 300
		target = join_ally
	}
}