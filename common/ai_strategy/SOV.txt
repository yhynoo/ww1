# DIPLOMACY

SOV_alliances = {
	allowed = { original_tag = SOV }
	enable = { 
		not = { has_government = communism }
		or = { is_guaranteed_by = FRA is_ally_with = FRA } 
	}
	abort_when_not_enabled = yes

	# alliances
	ai_strategy = { type = alliance		id = FRA		value = 100 }
}