# DIPLOMACY

SOV_alliances = {
	allowed = { original_tag = SOV }
	enable = { or = { is_guaranteed_by = FRA is_ally_with = FRA } }
	abort_when_not_enabled = yes

	# alliances
	ai_strategy = { type = alliance		id = FRA		value = 100 }

	# aggressive wargoals
	ai_strategy = { type = conquer		id = AUS		value = 40 }
	ai_strategy = { type = conquer		id = TUR		value = 20 }
}

# ARMY

SOV_militarisation = {
	allowed = { original_tag = SOV }
	enable = { always = yes }

	# more infantry, less cyclists
	ai_strategy = {	type = role_ratio	id = infantry		value = 60 }
	ai_strategy = {	type = role_ratio	id = breakthrough	value = -30 }
}

SOV_areas = {
	allowed = { original_tag = SOV }
    enable = { not = { has_completed_focus = SOV_on_our_own } }
	abort_when_not_enabled = yes
	
	# planning the positions
	ai_strategy = {
		type = frontarea_priority_unit_request
		id = europe
		value = 200
	}

	ai_strategy = {
		type = area_priority
		id = asia
		value = -100
	}
}