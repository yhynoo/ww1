# DIPLOMACY

SOV_alliances = {
	allowed = { original_tag = SOV }
	enable = { 
		not = { has_government = communism }
		or = { is_guaranteed_by = FRA is_in_faction_with = FRA } 
	}
	abort_when_not_enabled = yes

	# alliances
	ai_strategy = { type = alliance		id = FRA		value = 200 }

	# aggressive wargoals
	ai_strategy = { type = conquer		id = AUS		value = 80 }
	ai_strategy = { type = conquer		id = TUR		value = 60 }
}

SOV_communist_expansion = {
	allowed = { original_tag = SOV }
	enable = { 
		and = {
			or = { tag = ARM tag = POL tag = TMS tag = UBD tag = UKR }
			is_in_faction = no
		}
	}
	
	reversed = yes
	enable_reverse = { 
		has_government = communism 
		has_civil_war = no
	}

	# aggressive wargoals
	ai_strategy = { type = conquer		id = SOV		value = 200 }
	ai_strategy = { type = declare_war	id = SOV		value = 200 }
}

# ARMY

SOV_militarisation = {
	allowed = { original_tag = SOV }
	enable = { always = yes }

	# more infantry, less cyclists
	ai_strategy = {	type = role_ratio	id = infantry		value = 60 }
	ai_strategy = {	type = role_ratio	id = breakthrough	value = -30 }

	ai_strategy = { type = garrison 	value = 30 }
	ai_strategy = {	type = dont_defend_ally_borders		id = FRA		value = 1 }
}

SOV_ignore_asia = {
	allowed = { original_tag = SOV }
    enable = { not = { has_completed_focus = SOV_on_our_own } }
	abort_when_not_enabled = yes
	
	# planning the positions
	ai_strategy = {	type = area_priority		id = asia			value = -60 }
}