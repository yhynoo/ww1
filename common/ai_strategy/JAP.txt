# DIPLOMACY

JAP_alliances = {
    allowed = { original_tag = JAP }
    enable = { 
		ENG = { 
			has_guaranteed = JAP 
			not = { is_ally_with = MAN }
		} 
	}
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = ENG		value = 40 }
	ai_strategy = { type = alliance		id = FRA		value = 20 }

    # aggressive wargoals
    ai_strategy = { type = declare_war	id = GER		value = 120 }
}

JAP_alliances_alternative = {
    allowed = { original_tag = JAP }
    enable = {
		or = {
			ENG = { is_ally_with = MAN }
		  	FRA = { is_ally_with = MAN }
		}
	}
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = ENG		value = -40 }
    ai_strategy = { type = alliance		id = GER		value = 60 }

    # aggressive wargoals
	ai_strategy = { type = conquer		id = MAN		value = 100 }
}

JAP_attention = {
    allowed = { original_tag = JAP }
    enable = { or = { is_ally_with = ENG is_ally_with = FRA } }
    abort_when_not_enabled = yes

    ai_strategy = { type = avoid_starting_wars      value = -100 }
}

# ARMY

JAP_asia = {
    allowed = { tag = JAP }
    enable = { always = yes }

    # areas
    ai_strategy = { type = area_priority    id = europe         value = -200 }
}

JAP_island_invasions = {
    allowed = { tag = JAP }
    enable = { has_war_with = GER }

    # focus
    ai_strategy = { type = naval_invasion_focus     value = 120 }
    ai_strategy = { type = invade     id = GER      value = 60 }

    # tsingtao and papua
    ai_strategy = { type = invasion_unit_request    state = 431     value = 200 }
    ai_strategy = { type = invasion_unit_request    state = 523     value = 100 }
}