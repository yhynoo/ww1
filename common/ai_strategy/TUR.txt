# DIPLOMACY

TUR_alliances = {
    allowed = { original_tag = TUR }
    enable = { 
        and = {
            or = { has_government = monarchism has_government = communism }
            or = { GER = { has_guaranteed = TUR } has_completed_focus = TUR_on_our_own }
        }
    }
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = GER		value = 40 }

    # aggressive wargoals
    ai_strategy = { type = conquer		id = ARM		value = 100 }
    ai_strategy = { type = conquer		id = GRE		value = 100 }
    ai_strategy = { type = conquer		id = SOV		value = 40 }
}

TUR_alliances_alternative = {
    allowed = { original_tag = TUR }
    enable = { has_government = democratic }
    abort_when_not_enabled = yes

    # alliances
    ai_strategy = { type = alliance		id = FRA		value = 20 }
    ai_strategy = { type = alliance		id = GER		value = -40 }

    # aggressive wargoals: none
}

# ARMY

TUR_middle_east = {
    allowed = { tag = TUR }
    enable = { is_ally_with = GER }
    abort_when_not_enabled = yes

    # areas
    ai_strategy = { type = area_priority    id = europe         value = -100 }
    ai_strategy = { type = area_priority    id = middle_east    value = 200 }
}