# DIPLOMACY

TUR_alliances = {
    allowed = { original_tag = TUR }
    enable = { 
        GER = { has_war_with = ROM not = { is_ally_with = ITA } }
        not = { has_government = democratic }
    }
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = GER		value = 60 }
}

TUR_alliances_alternative = {
    allowed = { original_tag = TUR }
    enable = { has_government = democratic }
    abort_when_not_enabled = yes

    # alliances
    ai_strategy = { type = alliance		id = ENG		value = 60 }
    ai_strategy = { type = alliance		id = FRA		value = 20 }
}

# ARMY

TUR_europe = {
	allowed = { original_tag = TUR }
    enable = { always = yes }
	
	# planning the positions
	ai_strategy = {	type = area_priority		id = europe			        value = 80 }
    ai_strategy = {	type = area_priority		id = middle_east			value = 40 }

    # don't get too involved
    ai_strategy = { type = dont_defend_ally_borders     id = GER        value = 10 }
    ai_strategy = { type = dont_defend_ally_borders     id = FRA        value = 10 }
}