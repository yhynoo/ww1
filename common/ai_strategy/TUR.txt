# DIPLOMACY

TUR_alliances = {
    allowed = { original_tag = TUR }
    enable = { 
        GER = { has_war_with = ROM }
        not = { has_government = democratic }
    }
    abort_when_not_enabled = yes

    # alliances
	ai_strategy = { type = alliance		id = GER		value = 60 }
}

TUR_alliances_alternative = {
    allowed = { original_tag = TUR }
    enable = { has_government = democratic }
    abort_when_not_enabled = yes

    # alliances
    ai_strategy = { type = alliance		id = ENG		value = 60 }
    ai_strategy = { type = alliance		id = FRA		value = 20 }
}