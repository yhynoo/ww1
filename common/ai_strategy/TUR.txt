TUR_likes_FRA = {
    allowed = { tag = TUR }
    enable = { has_completed_focus = TUR_on_our_own not = { has_war_with = FRA } }
	abort_when_not_enabled = yes

    # focus on planes
    ai_strategy = {
        type = air_factory_balance
        value = 50
    }
}

turkey_middle_east = {
    allowed = { tag = TUR }
    enable = { is_ally_with = GER }
    abort_when_not_enabled = yes

    ai_strategy = {
		type = area_priority
		id = europe
		value = -100
	}

    ai_strategy = {
		type = area_priority
		id = middle_east
		value = 180
	}
}