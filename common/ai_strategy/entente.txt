FRA_dont_get_involved_in_russia = {
    allowed = { original_tag = FRA }
    enable = { RUS = { has_civil_war = yes } }
    abort_when_not_enabled = yes

    ai_strategy = { type = alliance id = JAP value = -600 }

    ai_strategy = { type = alliance id = SOD value = -600 }
    ai_strategy = { type = alliance id = SOT value = -600 }

    ai_strategy = { type = alliance id = KAZ value = -600 }
    ai_strategy = { type = alliance id = UKR value = -600 }
}

FRA_entente = {
    allowed = { original_tag = FRA }
    enable = { is_historical_focus = yes }

    ai_strategy = { type = alliance id = SPA value = 300 }
    ai_strategy = { type = prepare_for_war id = GER value = 40 }
}

POL_entente = {
    allowed = { original_tag = POL }
    enable = { is_historical_focus = yes has_war = no has_global_flag = great_war }

    ai_strategy = { type = prepare_for_war id = GER value = 200 }
}

SPA_entente = {
    allowed = { original_tag = SPA }
    enable = { is_historical_focus = yes }

    ai_strategy = { type = alliance id = FRA value = 300 }
}