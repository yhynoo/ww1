army_priorities = {
    allowed = {
        not = {
            or = {
                original_tag = AUS
                original_tag = FRA
                original_tag = GER
                original_tag = ITA
                original_tag = JAP
                original_tag = SOV
                original_tag = TUR
            }
        }
    }

    enable = {
        always = yes
    }

    ai_strategy = {             # Basic armies should get basic units.
        type = role_ratio
        id = infantry
        value = 40
    }

    ai_strategy = {
        type = role_ratio
        id =  cyclist
        value = 10
    }
}

adjust_garrisoning = {
    enable = {                  # Don't garrison so much when the threat is high - go to the borders.
        threat > 0.1
    }

    ai_strategy = {
        type = garrison
        value = -25
    }
}

area_priorities = {
    enable = {
        always = yes
    }

    ai_strategy = {
        type = area_priority
        id = mainland_europe
        value = 100
    }

    ai_strategy = {
        type = area_priority
        id = asia
        value = -50
    }

    ai_strategy = {
        type = area_priority
        id = africa
        value = -100
    }
}