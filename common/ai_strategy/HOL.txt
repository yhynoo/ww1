# DIPLOMACY

HOL_neutrality = {
    allowed = { original_tag = HOL }
    enable = { not = { HOL = { has_completed_focus = HOL_adjust_the_government } } }
    
    abort = { HOL = { has_completed_focus = HOL_adjust_the_government } }
    reversed = yes

    ai_strategy = { type = alliance		id = HOL		value = -100 }
}

HOL_alternative = {
    allowed = { original_tag = HOL }
    enable = { has_government = monarchism }
    abort_when_not_enabled = yes

    # alliances
    ai_strategy = { type = alliance     id = GER        value = 60 }

    # aggressive war goals
    ai_strategy = { type = contain      id = ENG        value = 60 }
}