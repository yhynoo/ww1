HOL_wants_flanders_solved = {
    allowed = { original_tag = HOL }
    enable = { not = { has_country_flag = we_stay_in_flanders } }

    abort_when_not_enabled = yes

    ai_strategy = {
        type = befriend
        id = FRA
        value = 60
    }
}