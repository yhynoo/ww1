# DIPLOMACY

TMS_alliances = {
    allowed = { original_tag = TMS }
    enable = { always = yes }
    reversed = yes

    ai_strategy = { type = alliance     id = TMS        value = -100 }
}

# ARMY

TMS_dont_expand = {
    allowed = { original_tag = TMS }
    enable = { always = yes }                             
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control    ratio = 0.5     ordertype = front   execute_order = no   
        state_trigger = { not = { is_core_of = TMS } }
    }
}