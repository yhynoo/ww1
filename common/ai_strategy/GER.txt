# germany extra militarization
germany_army_production = {
	allowed = { original_tag = GER }
	enable = { always = yes }
	abort = { always = no }

	# prepare more units
	ai_strategy = {
		type = role_ratio
		id = infantry
		value = 30
	}

	# focus on military factories
	ai_strategy = {
		type = added_military_to_civilian_factory_ratio
		value = 15
	}

	ai_strategy = {
		type = equipment_production_factor
		id = infantry
		value = 30
	}

}

# -- other stuff
 
GER_army_composition = {
	allowed = { original_tag = GER }
	enable = { always = yes }

	abort_when_not_enabled = yes

	# we're also scared of France and Russia
	ai_strategy = {
		type = prepare_for_war
		id = FRA
		value = 50
	}

	ai_strategy = {
		type = prepare_for_war
		id = SOV
		value = 20
	}
}

GER_protects_AUS = {
	allowed = { original_tag = GER }
	enable = { or = { is_ally_with = AUS has_guaranteed = AUS } }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = support
		id = AUS
		value = 100
	}
}

GER_protects_UBD = {
	allowed = { original_tag = GER }
	enable = { UBD = { exists = yes } }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = support
		id = UBD
		value = 100
	}
}

GER_second_line_airfields = {
	allowed = { original_tag = GER }
	enable = { has_war = yes }

	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 50			# Baden
		value = 2
	}

	ai_strategy = {
		type = build_building
		id = air_base
		target = 86			# Posen
		value = 1
	}

	ai_strategy = {
		type = build_building
		id = air_base
		target = 763		# Königsberg
		value = 2
	}
}