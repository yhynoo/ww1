GER_army_composition = {
	allowed = {
		original_tag = GER
	}

	enable = {
		always = yes
	}

	abort_when_not_enabled = yes

	## Land troops
	ai_strategy = {
		type = role_ratio
		id = infantry
		value = 120
	}

	ai_strategy = {
		type = role_ratio
		id = cyclist
		value = 15
	}

	ai_strategy = {
		type = role_ratio
		id = mountaineers
		value = 5
	}

	## Air
	ai_strategy = {
		type = unit_ratio
		id = tactical_bomber
		value = 35
	}
}

GER_protects_AUS = {
	allowed = { original_tag = GER }
	enable = { or = { is_ally_with = AUS has_guaranteed = AUS } }

	abort_when_not_enabled = yes

	ai_strategy = {
		type = support
		id = AUS
		value = 100
	}

	ai_strategy = {
		type = diplo_action_acceptance
		id = AUS
		value = 300
		target = join_allies
	}
}

GER_protects_UBD = {
	allowed = { original_tag = GER }
	enable = { UBD = { exists = yes } }

	abort_when_not_enabled = yes

	ai_strategy = {
		type = protect
		id = UBD
		value = 100
	}

	ai_strategy = {
		type = support
		id = UBD
		value = 100
	}
}

GER_likes_HOL_if_they_are_bold = {
	allowed = { original_tag = GER }
	enable = { HOL = { has_country_flag = we_stay_in_flanders } }

	ai_strategy = {
		type = alliance
		id = HOL
		value = 100
	}
}

GER_second_line_airfields = {
	allowed = { original_tag = GER }
	enable = { has_war = yes }

	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = air_base
		target = 50			# Baden
		value = 2
	}

	ai_strategy = {
		type = build_building
		id = air_base
		target = 86			# Posen
		value = 1
	}

	ai_strategy = {
		type = build_building
		id = air_base
		target = 763		# Königsberg
		value = 2
	}
}