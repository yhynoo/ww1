england_western_front = {
	allowed = { 
		or = { tag = AST tag = ENG }
	}
	enable = { has_war_with = GER is_ally_with = FRA }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_unit_request
		area = western_front
		value = 120
	}
}