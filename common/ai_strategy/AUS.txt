# don't work for others, they will manage
AUS_stay_at_home = {
	allowed = { original_tag = AUS }
	enable = { always = yes }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = dont_defend_ally_borders
		id = GER
		value = 10
	}

	ai_strategy = {
		type = dont_defend_ally_borders
		id = TUR
		value = 10
	}
}

AUS_scared_of_ITA = {
	allowed = { original_tag = AUS }
	enable = { not = { has_non_aggression_pact_with = ITA } }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = prepare_for_war
		id = ITA
		value = 50
	}
}

AUS_hates_SOV = {
	allowed = { original_tag = AUS }
	enable = { 
		has_idea = idea_unrest_in_galicia
		not = { has_country_flag = chose_alternative }
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = declare_war
		id = SOV
		value = 300
	}
}

AUS_likes_FRA = {
	allowed = { original_tag = AUS }
	enable = { has_country_flag = chose_alternative }
	abort_when_not_enabled = yes

	ai_strategy = {
		type = support
		id = FRA
		value = 100
	}
}