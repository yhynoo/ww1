AUS_alliances = {
	allowed = { original_tag = AUS }
	enable = { not = { has_completed_focus = AUS_on_our_own } }
	abort_when_not_enabled = yes

	# alliances
	ai_strategy = { type = alliance		id = GER		value = 100 }
}

AUS_alliances_alternative = {
	allowed = { original_tag = AUS }
	enable = { has_completed_focus = AUS_on_our_own }

	ai_strategy = { type = alliance		id = FRA		value = 20 }
	ai_strategy = { type = alliance		id = GER		value = -200 }
}

AUS_attention = {
    allowed = { original_tag = AUS }
    enable = { not = { is_ally_with = GER } }
    abort_when_not_enabled = yes

    ai_strategy = { type = avoid_starting_wars      value = -100 }
}

# ARMY

AUS_militarization = {
	allowed = { original_tag = AUS }
	enable = { always = yes }

	# more infantry, less cyclists
	ai_strategy = {	type = role_ratio	id = infantry		value = 25 }
	ai_strategy = {	type = role_ratio	id = maneuver		value = -5 }

	# don't help germany
	ai_strategy = { type = dont_defend_ally_borders		id = GER		value = 1 }
}

AUS_alps = {
	allowed = { original_tag = AUS }
	enable = { ITA = { can_declare_war_on = AUS } }
	abort_when_not_enabled = yes

	# planning the positions
	ai_strategy = { type = prepare_for_war		id = ITA		value = 60 }
} 