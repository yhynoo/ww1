# DIPLOMACY

UBD_alliances = {
    allowed = { original_tag = UBD }
    enable = { always = yes }
    reversed = yes

    ai_strategy = { type = alliance     id = UBD        value = -100 }
}

# ARMY

UBD_dont_expand = {
    allowed = { original_tag = UBD }
    enable = { always = yes }                             
    abort_when_not_enabled = yes

    ai_strategy = {
        type = front_control    ratio = 0.2   ordertype = front   execute_order = no   
        state_trigger = { not = { is_core_of = UBD } }
    }
}