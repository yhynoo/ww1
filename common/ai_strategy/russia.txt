rus_army_priorities = {
    allowed = {
        original_tag = SOV
    }

    enable = {
        always = yes
    }

    ai_strategy = {                 # Russia wants big army - but has no roads.
        type = role_ratio
        id = infantry
        value = 55
    }

    ai_strategy = {
        type = role_ratio
        id =  cyclist
        value = 5
    }
}

# don't garrison asia unless you plan to do something there:
russia_far_east = {
    allowed = {
        original_tag = SOV
    }

    enable = {
        not = { has_completed_focus = SOV_far_east_affairs }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = area_priority
        id = asia
        value = -100
    }
}

# don't garrison far north unless you're at war with UK, Japan, or USA:
russia_far_north = {
    allowed = {
        original_tag = SOV
    }

    enable = {
        or = {
            not = { has_war_with = ENG }
            not = { has_war_with = JAP }
            not = { has_war_with = USA }
        }
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = area_priority
        id = far_north
        value = -300
    }
}