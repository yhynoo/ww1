austria_likes_germany = {
    allowed = {
        original_tag = AUS
    }

    enable = {
        threat > 0.1
    }

    abort = {
        has_completed_focus = AUS_second_vienna_treaty          # until they decide to go their own way.
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = befriend
        id = GER
        value = 50
    }

    ai_strategy = {
        type = alliance
        id = GER
        value = 100
    }
}

austria_hates_russia = {
    allowed = {
        original_tag = AUS
    }

    enable = {
        has_idea = sww1_idea_slavic_unrest
    }
    abort_when_not_enabled = yes

    ai_strategy = {
        type = prepare_for_war
        id = SOV
        value = 50
    }

    ai_strategy = {
        type = alliance
        id = GER
        value = 50
    }
}