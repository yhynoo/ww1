avoid_wars_at_low_specs = {
    allowed = { always = yes }
    enable = { 
        or = { 
            has_stability < 0.4 
            has_war_support < 0.5
        } 
    }
    abort_when_not_enabled = yes

    ai_strategy = { type = avoid_starting_wars value = -100 }
}