on_actions = {
    on_state_control_changed = {
        effect = {
            
        }
    }

    on_civil_war_end = {
        effect = {
            if = {
                limit = { tag = GER }
                news_event = { id = germany_civil_war.6 hours = 6 random_hours = 6 }

                if = {
                    limit = { has_government = monarchism }
                    set_cosmetic_tag = GEM
                    promote_character = GER_wilhelm_ii
                }

                if = {
                    limit = { has_government = communism }
                    set_capital = { state = 64 }
                }
            }
        }
    }
}