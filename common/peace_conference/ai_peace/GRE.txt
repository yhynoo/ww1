# - romanian war

peace_ai_desires = {
    GRE_war_with_TUR_limits = {
        peace_action_type = { take_states }
        enable = {
            tag = GRE
            FROM.FROM.FROM = { not = { is_core_of = ROOT } }
        }
        ai_desire = -600
    }
}