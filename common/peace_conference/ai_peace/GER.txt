peace_ai_desires = {

    # MITTELEUROPA

    GER_puppet_POL = {
        peace_action_type = { puppet }
        enable = {
            original_tag = GER
            FROM.FROM.FROM = { 
                is_core_of = POL 

                not = { state = 90 }            # we take Augustów anyway
                not = { is_core_of = GER }      # if we did 'Drang nach Osten', don't do this
            }
        }
        ai_desire = 600
    }

    GER_puppet_UBD = {
        peace_action_type = { puppet }
        enable = {
            original_tag = GER
            FROM.FROM.FROM = { 
                is_core_of = UBD 
                not = { is_core_of = GER }      # if we did 'Drang nach Osten', don't do this
            }
        }
        ai_desire = 240
    }

    GER_dont_take_nonsense_in_the_east = {
        peace_action_type = { take_states }
        enable = {
            and = {                             # if we're still at war...
                original_tag = GER
                has_war_with = FRA
            }
            FROM.FROM.FROM = { 
                is_core_of = SOV 
                not = { is_core_of = POL is_core_of = UBD }           
            }
        }
        ai_desire = -600
    }

    # WEST
    GER_dont_take_nonsense_in_the_west = {
        peace_action_type = { take_states }
        enable = {
            original_tag = GER
            FROM.FROM.FROM = { 
                not = {
                    or = {
                        state = 17 state = 18 state = 29                    # France
                        state = 286 state = 199 state = 624                 # Indochina
                        is_on_continent = africa
                    }
                }      
            }
        }
        ai_desire = -600
    }

    # OTHER

    GER_dont_liberate_or_force_governments = {
        peace_action_type = { liberate force_government }
        enable = { original_tag = GER }
        ai_desire = -200
    }
}