peace_ai_desires = {

    ## DOMINIONS ##

    GER_dominions_east = {
        peace_action_type = { puppet }
        enable = {
            and = {
                original_tag = GER
                not = { has_country_flag = german_drang_nach_osten }
            }
            FROM = { or = { tag = POL tag = UBD } }
            FROM.FROM.FROM = { 
                not = { state = 90 }            # we take Augustów anyway
            }
        }
        ai_desire = 360
    }

    ## OTHER ##

    GER_dont_take_east = {
        peace_action_type = { take_states }
        enable = {
            original_tag = GER
            FROM.FROM.FROM = { 
                is_core_of = SOV 
                not = { state = 90 }            # we take Augustów anyway

                if = {
                    limit = { has_country_flag = german_drang_nach_osten }
                    not = { is_core_of = POL is_core_of = UBD }      
                }     
            }
        }
        ai_desire = -200
    }
}