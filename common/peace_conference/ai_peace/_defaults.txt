peace_ai_desires = {

    # TAKING STATES
    take_cores = {
        peace_action_type = { take_states }
        enable = {
            not = { original_tag = AUS original_tag = GER }
            FROM.FROM.FROM = { is_core_of = ROOT }         # for now, just grab cores and neighboring states.
        }
        ai_desire = 200
    }

    take_around = {
        peace_action_type = { take_states }
        enable = {
            not = { original_tag = AUS original_tag = GER }
            or = {
                and = {
                    is_coastal = yes
                    not = { is_on_continent = europe }
                }
                any_neighboring_state = {
                    or = {
                        is_owned_by = ROOT
                        is_claimed_by = ROOT
                        pc_is_state_claimed_and_taken_by = ROOT
                    }
                }
            }
        }
        ai_desire = 60
    }

    take_farther = {
        peace_action_type = { take_states }
        enable = {
            not = { original_tag = AUS original_tag = GER }
            any_neighboring_state = {
                pc_is_state_claimed_and_taken_by = ROOT
            }
        }
        ai_desire = 10
    }

    # OTHER
    dont_do_other_things = {
        peace_action_type = { puppet liberate force_government }
        enable = { not = { original_tag = AUS original_tag = GER } }
        ai_desire = -100
    }
}