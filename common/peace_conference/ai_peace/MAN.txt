peace_ai_desires = {
    MAN_annex_warlords = {
        peace_action_type = { take_states }
        enable = {
            tag = MAN
            FROM = { or = { tag = EAT tag = FEN tag = GUA tag = XMA tag = MON tag = TIB tag = ANH tag = SHA tag = YUN } }
        }
        ai_desire = 200
    }

    # curb the warlords too
    MAN_warlords_dont_spread = {
        peace_action_type = { take_states }
        enable = {
            or = { tag = EAT tag = FEN tag = GUA tag = XMA tag = TIB tag = ANH tag = SHA tag = YUN }
            FROM.FROM.FROM = { not = { is_core_of = ROOT } }
        }
        ai_desire = -600
    }
}